# AGENTS.md

## Project Context

ADAM-EDU is a teacher authoring + preview MVP for pedagogical business cases.

This repository currently supports:

- teacher form suggestions via `/api/suggest`
- asynchronous authoring job intake via `/api/authoring/jobs`
- LangGraph-based case generation
- Supabase Realtime progress updates (`postgres_changes`)
- teacher preview of the generated case

This repository does not currently include a student runtime, full authentication, or a hardened production deployment surface.

## Repo Rules

- Treat `main` as protected even when GitHub cannot enforce every rule automatically.
- Never push directly to `main`.
- Every change must go through a branch and pull request.
- Default merge mode is `Squash and merge`.
- If you change setup, contracts, workflows, or contributor behavior, update the relevant documentation in the same change.

## Documentation Map

- `README.md`: onboarding, local setup, Docker, Alembic, runtime commands
- `CONTRIBUTING.md`: branch, PR, validation, and collaboration workflow
- `docs/runbooks/`: runbooks operativos, incluido el setup local canonico de auth y authoring
- `docs/adr/`: accepted architecture decisions, including the Fase 1 auth perimeter ADR
- `docs/repo-governance.md`: repository governance and merge policy
- `CLAUDE.md`: equivalent agent guidance for Claude-oriented tooling

## Shared Agent Tooling

- This repo is Codex-first. Claude remains a supported compatibility path, but it does not define the canonical repo layout.
- The repo-scoped routing skill lives in `.agents/skills/adam-orchestrator/`.
- Repo-scoped custom subagents live in `.codex/agents/`.
- `scripts/agents/gstack.lock.json` pins the upstream gstack repository, ref, commit, and version used by the team.
- `.agents/skills/gstack*` and `.claude/skills/*` are generated local runtimes. Keep them out of git and rebuild them with:
  - `pwsh -File scripts/agents/bootstrap.ps1`
  - or `./scripts/agents/bootstrap.sh`
- Changes to agent tooling belong in dedicated `agent/...` branches and PRs.
- If agent tooling changes, update `README.md`, `CONTRIBUTING.md`, `AGENTS.md`, and `CLAUDE.md` in the same PR.

## Skill Routing

- For substantial implementation, debugging, review, QA, release, ideation, design, or security work, invoke `adam-orchestrator` first.
- `adam-orchestrator` routes the request into the right gstack workflow. Do not make the user memorize individual skills.
- Small read-only questions, code explanations, and narrow factual requests can be answered directly without a workflow.
- Dispatch defaults:
  - ideas and brainstorming -> `office-hours`, then `autoplan` or `plan-*`
  - bugs, errors, regressions -> `investigate`
  - review of a diff, branch, or PR -> `review`
  - QA or staging verification -> `qa` or `qa-only`
  - release preparation -> `ship`, then `land-and-deploy`, `canary`, `document-release`
  - visual work -> `design-*`
  - security review -> `cso`
  - browser-heavy QA -> `browse`, `connect-chrome`, `setup-browser-cookies`
- One agent owns the branch and final decision path. Use repo-scoped subagents only for bounded read-only sidecars such as `pr_explorer`, `reviewer`, `code_mapper`, independent report-only QA, benchmark, read-only exploration, or post-ship docs.
- Do not use subagents for merge or deploy authority, scope decisions, conflicting writes, or parallel edits in `backend/src/case_generator/**`.
- Do not run `document-release` before `uv run --directory backend pytest -q` is green. Pre-PR docs sync belongs in the implementation diff itself.

## Architecture Boundaries

- `backend/src/case_generator/` owns authoring business logic, prompts, schemas, and graph execution.
- `backend/src/shared/` owns FastAPI app composition, DB, ORM, shared contracts, and progress snapshot endpoints.
- `frontend/src/` follows the stable top-level split `app / features / shared`.
- `shared/` must not become a catch-all for new product logic.
- Use absolute imports by domain. Do not reintroduce `sys.path` hacks or deep relative import chains.
- Alembic is the schema mechanism for the application runtime. Do not rely on ad hoc table creation in normal app startup.

## Auth Error Precedence

- For protected backend authz routes in `backend/src/shared/**`, evaluate auth failures in this order:
  - `verified_identity -> profile_state -> membership_state -> password_rotation -> role/context -> handler`
- `profile_incomplete` belongs only to profile-state failures, including missing profile rows or missing required profile fields.
- `membership_required` belongs only to membership-state failures.
- `password_rotation_required` blocks protected business routes after identity/profile/membership pass.
- `GET /api/auth/me` remains bootstrap-safe, bypasses shared required-profile-field checks and `password_rotation_required`, and still returns actor state including `must_rotate_password` when the profile row exists.
- `POST /api/auth/change-password` is explicitly exempt from the shared password-rotation guard so it cannot self-block.

## Supabase Infrastructure Guardrails

- ADAM-EDU production progress infrastructure is Supabase-native: Postgres durability + Supabase Realtime (`postgres_changes` on `public.authoring_jobs`).
- Treat Supavisor transaction mode (`:6543`) as the default production connection path for backend database access.
- Do not introduce manual SSE pub/sub systems, in-memory progress buses, or custom long-lived stream fanout layers for teacher authoring progress.
- Do not introduce complex queue reclaimers/orchestrators for this progress path unless an approved ADR explicitly changes the architecture.
- If a change proposes moving away from Supabase Realtime or Supavisor defaults, require a dedicated ADR and synchronized updates to `README.md`, `CONTRIBUTING.md`, `AGENTS.md`, and `CLAUDE.md` in the same PR.

## Authoring Job Dispatch — Cloud Run split (ADR 0004)

- Production runs TWO Cloud Run services from the SAME image: `public-api` (SPA + API, REQUEST-based billing / `--cpu-throttling`) and `authoring-worker` (`shared.worker_app`, receives jobs via Cloud Tasks, `--no-cpu-throttling`, `min-instances=0` — bills only during real generations). Progress stays 100% Supabase-native (this split only moves WHERE the job executes, never the progress path).
- Dispatch is governed by `AUTHORING_DISPATCH` (Settings `authoring_dispatch`, default `inline`; producer `shared/job_dispatch.py`). `inline` = FastAPI BackgroundTasks in-process (dev/local/tests, byte-identical pre-ADR behavior). `cloud_tasks` = enqueue via Cloud Tasks REST (httpx + metadata-server token, zero new deps) with OIDC + `dispatchDeadline=1800s`; enqueue failure raises and the job stays `pending` (retryable) — never a silent inline fallback.
- INVARIANT: `AUTHORING_DISPATCH=cloud_tasks` goes WITH `--cpu-throttling` on `public-api`, and `inline` goes WITH `--no-cpu-throttling`. A mixed state (inline + throttled CPU) silently stalls generations. The worker's `--no-cpu-throttling` is load-bearing: the internal handler HOLDS the request open for the whole job, and if Cloud Tasks drops the connection at its 30-min deadline the coroutine still finishes at full CPU (`GRAPH_EXECUTION_TIMEOUT_SECONDS=1900`).
- The worker handler (`shared/internal_tasks.py`) validates Cloud Tasks OIDC and applies the idempotency barrier; `InternalTaskPayload.kind` (`"run"` default | `"regenerate_notebook"`) routes regeneration of COMPLETED cases past the run-status barrier. Cloud Run IAM (`--no-allow-unauthenticated`, invoker = `adam-run@`) is the outer boundary.
- Rollback without redeploy: `gcloud run services update public-api --update-env-vars=AUTHORING_DISPATCH=inline --no-cpu-throttling`. Do not re-hardcode job execution into request handlers; all three dispatch sites (intake, retry, regenerate-notebook) go through `dispatch_authoring_job`.

## Authoring Live Preview (module-by-module)

- During generation the graph persists a PARTIAL `assignments.canonical_output` per completed canonical step so teachers read each module as it lands instead of only watching the step loader. This stays Supabase-native — no new SSE/bus/table/Realtime publication. `assignments` is NOT in the `supabase_realtime` publication, so the partial write does not bloat the Realtime broadcast.
- Transport split: the SIGNAL rides the existing Realtime progress (`current_step`/`progress_seq` on `authoring_jobs.task_payload`); the heavy CONTENT travels by HTTP `GET /api/authoring/jobs/{id}/preview` (owner-gated, served only while `processing`/`failed*`). The raw dataset (`content.datasetRows`) is stripped from the preview and deferred to `/result`.
- The per-node partial write (`_persist_partial_preview` in `core/authoring.py`) is BEST-EFFORT in its own short session AFTER the critical progress write — it swallows all errors and must never fail a job (mirrors `CostCallbackHandler`). The authoritative full write stays in MICRO-SESSION 2 at completion; `/result` still 409s until `completed`.
- Kill-switch: `AUTHORING_LIVE_PREVIEW` (env, default true) gates the partial write. Off → backend writes no partials and the frontend degrades cleanly to the step loader. The frontend reuses the partial-tolerant `CaseContentRenderer` via a read-only `LiveCasePreview` (zero mutating actions on an incomplete case).

## Validation Commands

Run the relevant checks before opening a PR. Full default set:

```powershell
uv run --directory backend pytest -q
uv run --directory backend mypy src
npm --prefix frontend run lint
npm --prefix frontend run test
npm --prefix frontend run build
```

The backend test suite runs against a **dedicated test database** (`adam_test`), never your
development database. `backend/tests/conftest.py` (via `tests/_bootstrap_test_db.py`) rewrites
`DATABASE_URL` to `adam_test` on the same local Postgres (host `5434`) before importing the app,
so `pytest` — which does `DROP SCHEMA ... CASCADE` to rebuild the schema — can never wipe the
users, profiles, and courses you created in the dev `postgres` database. CI keeps its own
ephemeral `postgres` database (`GITHUB_ACTIONS=true` skips the rewrite). Override the local
target with `TEST_DATABASE_URL`; the `_assert_local_schema_reset_target` guard refuses to reset
anything but `adam_test` locally. To (re)create a working dev baseline after a deliberate reset or
a fresh clone, run `uv run --directory backend python scripts/seed_dev.py` (idempotent).

Issue 23 adds a migration test that creates and drops temporary databases. In the default
local Docker Postgres this works with the `postgres` user. On other Postgres environments,
the backend test suite now assumes `CREATE DATABASE` and `DROP DATABASE` privileges.

Ordinary backend DB-backed tests now run under a per-test dedicated connection + outer
transaction + `SAVEPOINT` session contract. Keep shared seed fixtures flush-only by default.
Use explicit pytest markers for carve-outs:

- `ddl_isolation` for temp-database or DDL-heavy tests
- `shared_db_commit_visibility` for tests that require real cross-connection committed visibility and fall back to `TRUNCATE` cleanup

Issue 23 also introduces `backend/sql/rls_policies.sql` as a separate artifact. Alembic
does not apply that file. Treat it as an explicit secondary-RLS deployment step only for
Supabase or another environment that exposes compatible Auth helpers like `auth.uid()`.

## Sensitive Areas

- `backend/src/case_generator/**` is the most sensitive part of the repo.
- Prompt boundaries, graph orchestration, and LLM-facing payload handling need extra caution.
- Database setup and migrations must stay aligned with `backend/alembic/` and `backend/.env.example`.

## Authoring Algorithm Picks (Issue #230)

- The teacher form picks algorithms from a canonical catalog instead of accepting up to five free-text chips.
- `POST /api/authoring/jobs` accepts the breaking fields `algorithm_mode` (`"single" | "contrast"`), `algorithm_primary`, and `algorithm_challenger`. The legacy `suggested_techniques` body field has been removed; do not reintroduce it.
- Algorithm picks are validated server-side at intake by `_validate_techniques_strict` whenever `case_type == "harvard_with_eda"` or `student_profile == "ml_ds"`. They are persisted into `task_payload` as `algorithm_mode` plus `algoritmos: list[str]` of length 0, 1, or 2.
- `GET /api/authoring/algorithm-catalog?profile=...&case_type=...` returns the canonical declarative catalog as `{profile, case_type, items: [{name, family, family_label, tier, learning_type}]}` where `tier ∈ {"baseline", "challenger"}`, `family ∈ {clasificacion, regresion, clustering}` (3 active families — `serie_temporal` is retired and never returned by this endpoint), and `learning_type ∈ {"supervised", "unsupervised"}` (Issue #244 — clasificacion and regresion are supervised; clustering is unsupervised). By default (`ALGORITHM_CATALOG_REDUCED` ON) the forward-facing view hides the `regresion` family and the `clustering` challenger (DBSCAN): `profile=business` returns 2 baseline items (Logistic Regression, K-Means); `profile=ml_ds` returns 3 items (Logistic Regression, Random Forest, K-Means). With the kill-switch OFF the full catalog is returned (business 3 / ml_ds 6). The endpoint is open (no PII), `Literal`-validated, and re-checked at intake.
- **Catalog reduction (kill-switch `ALGORITHM_CATALOG_REDUCED`, Settings `algorithm_catalog_reduced`, default `true`; `backend/.env.example`):** the forward-facing catalog is filtered in `get_algorithm_catalog` + `_technique_items` (`suggest_service.py`) so the form selector, the suggester taxonomy, AND `_validate_techniques_strict` all see only classification (both tiers) + the clustering baseline (K-Means); the `regresion` family (Linear Regression, Gradient Boosting Regressor) and the clustering challenger (DBSCAN) are hidden. The flag is read in the public `get_algorithm_catalog` wrapper and passed as an `lru_cache` KEY to `_get_algorithm_catalog_cached` (never inside the cached body), so toggling it is honored without `cache_clear()`. **Entries are NOT removed from `ALGORITHM_CATALOG`** → `family_of` / `get_dispatch_meta` / `resolve_legacy_family` / `classify_tier` keep resolving historical regresion/clustering-challenger jobs byte-identically; intake stays the no-leak guarantee (a hidden pick → 422 "fuera del catálogo"). Frontend needs no runtime change (the catalog-driven `AlgorithmSelector` auto-adapts and its coercion drops a now-hidden pick); the `algorithmCatalog` query `staleTime` was lowered so an open form self-heals at deploy. Off → full catalog byte-identical (env-only revert). Tests: `backend/tests/test_algorithm_catalog_reduced.py`.
- Family-coherence rule: in `contrast` mode the baseline and the challenger MUST belong to the same `family`. The backend rejects cross-family contrast picks (e.g. Logistic Regression vs Prophet) at intake with a 422 and a teacher-friendly Spanish message. The frontend `AlgorithmSelector` filters the challenger options to the baseline family. The LLM suggester is taught the same rule via prompt boundary AND the post-LLM `_snap_item` filter, so it cannot cross families even if the model strays.
- LSTM has been removed from the canonical ml_ds time-series catalog. Do not reintroduce LSTM (or other heavy DL surrogates) without an ADR.
- The `business` profile may legitimately expose only baseline items (no challengers in any family). The frontend HIDES the "2 algoritmos" mode entirely in that case (the catalog has no challengers to contrast against); the backend rejects contrast picks with a teacher-friendly message. Do not silently fall back to `single` on the backend.
- **TEMP — `contrast` oculto en el formulario (ambos perfiles, frontend-only):** el modo `contrast` ("2 algoritmos (baseline + challenger)") está OCULTO en el formulario del docente para AMBOS perfiles, dejando solo "1 algoritmo (deep dive)". Dos razones distintas convergen en el mismo gate: para `ml_ds` es un cierre **TEMPORAL** de producto (el contraste solo está pedagógicamente completo para clasificación; primero se abren/mejoran algoritmos individuales) gobernado por `algorithmFeatureFlags.mlDsContrastEnabled` (default `false`); para `business` es **PERMANENTE** porque su catálogo no expone challengers (no hay con qué contrastar). El gate vive en `frontend/src/features/teacher-authoring/algorithmContrastGate.ts` (`isContrastHiddenForProfile(profile)` = solo la regla del flag de ml_ds); `AlgorithmSelector.tsx` lo combina con la realidad del catálogo — `contrastHidden = isContrastHiddenForProfile(profile) || challengerItems.length === 0` — para ocultar el toggle entero y forzar single (vía `effectiveMode` + la coerción del `useEffect`). `AuthoringForm` reusa `isContrastHiddenForProfile` para forzar `algorithm_mode: "single"` + `algorithm_challenger: null` en el payload (business ya enviaba single por catálogo sin challengers). **El backend NO cambia** (respecto al gate de contraste): `GET /api/authoring/algorithm-catalog` sigue devolviendo el catálogo forward (reducido a 3 ítems para `ml_ds` por `ALGORITHM_CATALOG_REDUCED`, ver arriba) y `_validate_techniques_strict` sigue ACEPTANDO `contrast` (decisión explícita de alcance solo-frontend; un cliente cacheado o llamada directa con `contrast` + `ml_ds` aún se procesaría). En modo single el docente `ml_ds` sigue pudiendo elegir cualquiera de los 3 algoritmos del catálogo reducido (incl. el challenger Random Forest) como algoritmo único; solo se quita el EMPAREJAMIENTO. Reactivar `ml_ds` = poner el flag en `true` (y revertir los overrides de test que lo encienden); `business` permanece oculto por catálogo. Casos `ml_ds` con `contrast` ya generados no se ven afectados (esto solo toca la creación de casos nuevos).

## M3 Notebook Per-Family Dispatch (Issue #233)

- The M3 notebook generator (`m3_notebook_generator` in `backend/src/case_generator/graph.py`) dispatches to ONE specialized prompt per algorithm family instead of a single monolithic prompt.
- Within `clasificacion`, Issue #230 adds notebook variants for teacher intent: `lr_only`, `rf_only`, and `lr_rf_contrast`. `PROMPT_BY_FAMILY["clasificacion"]` and `M3_NOTEBOOK_ALGO_PROMPT_CLASSIFICATION` remain the back-compat contrast prompt, while `graph.py` selects from `CLASSIFICATION_NOTEBOOK_PROMPT_BY_VARIANT` using `algorithm_mode` plus `algoritmos`.
- `case_generator.prompts.PROMPT_BY_FAMILY` exposes exactly 4 keys: `clasificacion`, `regresion`, `clustering`, `serie_temporal`. The legacy `M3_NOTEBOOK_ALGO_PROMPT` symbol is preserved as a back-compat alias for `M3_NOTEBOOK_ALGO_PROMPT_CLASSIFICATION`.
- Family is resolved with `family_of(name)` first, then `resolve_legacy_family(name)` for historical task_payloads (XGBoost, Ridge, NLP, etc.). On no match the dispatcher falls back to `clasificacion` and emits a `legacy_warning` appended to the data-gap block.
- After the LLM call, `_validate_notebook_family_consistency(family, code)` checks the output against `_FAMILY_PROHIBITED_PATTERNS` (other-family API tokens like `train_test_split(` in clustering, `roc_auc_score` in regression). On violation the dispatcher reprompts ONCE with the explicit forbidden-tokens list; if the second attempt also violates, it raises `RuntimeError` and the job is marked failed. Never ship a runtime-broken notebook.
- For classification variants, `_validate_notebook_family_consistency(family, code, notebook_variant=...)` also enforces variant-specific required sentinels/APIs and rejects executable references to the unselected LR/RF model.
- The single registry of truth lives in `case_generator.suggest_service.ALGORITHM_CATALOG`. The legacy `ALGORITHM_REGISTRY` dict in `graph.py` has been removed.
- Deprecated families (`nlp`, `recomendacion`, `grafos`, `anomalias`, `segmentacion`, `clasificacion_tabular`, `regresion_tabular`, `nlp_text_mining`) are no longer exposed by the catalog and degrade to `clasificacion` via the legacy resolver. Do not reintroduce them without an ADR.
- Issue #240 había ampliado el contrato con tuning (`tuning_lr/tuning_rf`, GridSearchCV/RandomizedSearchCV) e interpretabilidad avanzada (`interp_lr` VIF+odds ratios, `interp_rf` permutation importance + PDP). **Issue #353 RECORTA ese deep-dive a su núcleo esencial** (ver sección dedicada abajo): esas celdas/sentinelas/APIs salieron de la superficie OBLIGATORIA. La cascada de modo rápido y el fallback VIF de #240 solo aplican si se reintroduce el deep-dive como capa opcional (requiere ADR).
- Issue #353 — el notebook ml_ds+clasificación se recorta a un **NÚCLEO** de 8 sentinelas (contrast) / 7 (single-model) y **2 figuras**. `_FAMILY_REQUIRED_SENTINELS["clasificacion"]` = `dummy_baseline, pipeline_lr, pipeline_rf, cv_scores, comparison_table, confusion_matrix, cost_matrix, metrics_summary_json` (8). `_FAMILY_REQUIRED_APIS["clasificacion"]` = `DummyClassifier, ColumnTransformer, StratifiedKFold, cross_val_score, train_test_split(, confusion_matrix(, predict_proba(, ConfusionMatrixDisplay` (8). `_CLASSIFICATION_REQUIRED_SENTINELS_BY_VARIANT` quita el pipeline del modelo no seleccionado (7 por single-model). `roc_curves/pr_curves/tuning_*/interp_*` ya no se emiten ni se exigen. Figuras: matriz de confusión + matriz de costos (Rule M variante: "máximo DOS celdas con render"). `_CLASSIFICATION_PROHIBITED_PATTERNS_BY_VARIANT` conserva solo las prohibiciones de modelo cruzado. Cero cambios a otras familias, a `business`, ni a `_FAMILY_PROHIBITED_PATTERNS`. Las celdas roc/pr/tuning/interp permanecen en el legacy `notebook.py` SOLO para reversibilidad (las elimina `build_classification_notebook_prompt`).
- Issue #353 — `top_features` (ancla de M4/M5) se RE-SOURCEA barato dentro de las celdas de pipeline tras el recorte de `interp_*`: `pipeline_lr` deriva `or_df` desde `coef_` (odds ratios) y `pipeline_rf` deriva `perm_df` desde `feature_importances_`. Las celdas `metrics_summary_json` (en `notebooks/_shared.py`) siguen leyendo `or_df`/`perm_df` sin cambios → `build_computed_metrics_block` mantiene `auc/f1/prevalence/top_features` byte-equivalente.
- **Descripciones conceptuales por celda (ml_ds+clf notebook):** cada celda de análisis lleva una descripción markdown **para estudiante/docente** — lenguaje llano, sin nombres de clases/funciones sklearn (`ColumnTransformer`…), sin variables internas (`target_col`/`X_raw`…), sin claves de contrato (`fp_cost`/`fn_cost`/path `dataset_schema_required…`) ni referencias a reglas de autoría (`Regla L`). El eje es **autoría**, no "análisis vs andamiaje", con reglas OPUESTAS por sitio: §1/2/2.1 (carga/inspección/detección) se editan en `M3_NOTEBOOK_BASE_TEMPLATE` (`prompts/__init__.py`, sitio `.replace()` → **prosa sin escapar**, determinista, 0 tokens LLM, **afecta el M3 de TODAS las familias**); §3.0 EDA + las 8 celdas algorítmicas viven en `clasificacion/notebook.py` (compartidas) y en los dicts por-variante de `notebooks/_shared.py` (sitio `.format()` → **prosa sin llaves o `{{}}` escapadas**, LLM-reproducidas best-effort — una directiva imperativa en «Reglas absolutas» pide emitirlas verbatim, mismo camino que toda celda markdown ya existente). Single-model: nombrar SOLO el modelo seleccionado (`test_single_model_prompts_do_not_seed_unselected_model_text` veta el otro sobre TODO el prompt crudo; el validador runtime ignora markdown). **EXCEPCIÓN:** la cabecera de `cost_matrix` conserva los literales `extraer los costos` + `business_cost_matrix` porque `test_variant_cost_cell_restores_legacy_pedagogy` y la instrucción de extracción del contrato al LLM dependen de ellos (el LLM consume ese bloque sin reimprimirlo). Solo-markdown: sin nueva sección numerada (TOC byte-idéntica), sin `plt.show()` ni líneas `# %%` en la prosa, sin nueva clave canónica/state/migración/`case_sanitization`, sin kill-switch (precedente solo-prompt). Render: markdown formateado en el `.ipynb` descargado (Colab, superficie dirigida), comentario resaltado en el panel in-app (igual que toda celda markdown). Casos ya generados NO se rellenan retroactivamente. Tests: `test_m3_notebook_conceptual_markdown.py` (guardas nuevas: pin de una frase conceptual por celda + render `.format()` + aislamiento belt-and-suspenders); los locks autoritativos `test_single_model_prompts_do_not_seed_unselected_model_text` y `test_variant_cost_cell_restores_legacy_pedagogy` viven en `test_issue230_classification_notebook_variants.py`.
- Issue #348 (hecho DENTRO de #353) — la celda ejecutada `dummy_baseline` resuelve el target **CONTRACT-FIRST**: `target_col = (contract_target if in df.columns else None) or label_aliases → churn_aliases → último categórico`. El nombre del contrato se inyecta vía el placeholder `{contract_target_name}` resuelto en `_prepare_m3_notebook_generation_context` desde `dataset_schema_required.target_column.name`. Target del contrato ausente del dataset → REQUISITO FALTANTE/skip (no entrena otra columna). Target churn/`categoria` → byte-idéntico.
- Issue #239 adds `m3_notebook_executor` after `m3_notebook_generator` and before `m3_sync`. It runs only for `studentProfile == "ml_ds"`, `output_depth == "visual_plus_notebook"`, and family `clasificacion`; other families/business noop and must not receive classification metrics or warnings. The executor uses `nbclient` in a subprocess with a minimal env, `TemporaryDirectory` HOME/cwd, no `shell=True`, hard timeout, AST scrub, and one execution-crash correction pass. `_FAMILY_REQUIRED_SENTINELS["clasificacion"]` includes `# === SECTION:metrics_summary_json ===`, which must remain atomic with the executor/parser that consumes `ADAM_M3_METRICS_SUMMARY_JSON=`.

## Narrative Grounding (Issue #243)

- Narrative grounding applies only when `studentProfile == "ml_ds"` AND `family == "clasificacion"` for M3-content, M4, and M5. The other three families and the `business` profile keep their existing prompt strings and must not receive `{computed_metrics_block}`.
- Issue #239 populates `m3_metrics_summary` from executed classification notebooks for downstream grounding. If metrics are absent or anchorless, `build_computed_metrics_block(None)` still emits a clear fallback placeholder, validation is disabled for that run, and `narrative_grounding_warning` is persisted.
- `validate_narrative_grounding` rejects citations with `CITA:` and unanchored model-metric numbers with `UNANCHORED:`. Business numbers from M2, Exhibits, or M4 are allowed. Numeric tolerance is ±2 percentage points for percentage-like values and ±2% relative for scalar values.
- The narrative nodes reprompt once with the `CITA:` / `UNANCHORED:` bullet list. A second violation raises `RuntimeError` so the job fails instead of shipping fabricated metrics.

## M4/M5 Narrative Variant Dispatch (Issue #330)

- The M4 and M5 narrative nodes (`m4_content_generator`, `m5_content_generator` in `backend/src/case_generator/graph.py`) are variant-aware for `studentProfile == "ml_ds"` AND `family == "clasificacion"`, mirroring the M3-content dispatch. They resolve teacher intent with `_resolve_classification_notebook_variant(algorithm_mode, algoritmos)` and override ONLY the `"clasificacion"` key of an `_effective_prompt_by_family` before calling `_select_narrative_prompt`. Every other case (the `business` profile, and the `regresion`/`clustering`/`serie_temporal` families) passes the original `M4_PROMPT_BY_FAMILY`/`M5_PROMPT_BY_FAMILY` unchanged.
- Symbols: `M4_NARRATIVE_PROMPT_CLASSIFICATION_BY_VARIANT` and `M5_NARRATIVE_PROMPT_CLASSIFICATION_BY_VARIANT`, each keyed by `lr_only` / `rf_only` / `lr_rf_contrast`. The `lr_rf_contrast` value IS the canonical `M4/M5_NARRATIVE_PROMPT_CLASSIFICATION` (the back-compat aliases `M4_PROMPT_CLASSIFICATION` / `M5_PROMPT_CLASSIFICATION` still point to it). M4's contrast block contrasts LR vs RF on the financial decision; `lr_only`/`rf_only` never name the unselected model. M5 keeps the decision-matrix header `| acción | KPI esperado | riesgo | modelo soporte |` byte-identical across all three variants and only varies the `modelo soporte` guidance line.
- Invocation contracts are unchanged: M4 still invokes `_invoke_narrative_with_grounding` (reprompt-once-then-fail on grounding) and M5 still invokes `_invoke_m5_content_with_contract(..., require_decision_matrix=_is_ml_ds_classification(...))`. The classification grounding block (`{computed_metrics_block}`) stays active in all three variants, and the three variants of each phase share the SAME placeholder set (base keys plus `{pregunta_eje}` and `{computed_metrics_block}`).
- The `business` swap `_maybe_business_classification_prompt` stays byte-identical; the variant override is exclusive to `ml_ds` + `clasificacion`. Do not reintroduce LR/RF naming into single-model variants.
- Issue #337 — defense-in-depth: a pure, asymmetric post-generation validator `detect_unselected_model_mentions(prose, variant)` (in `narrative_grounding.py`) flags the UNSELECTED model in `lr_only`/`rf_only` prose with the `MODELO_NO_SELECCIONADO:` prefix (prose-safe word-boundary regex over the full model names + Spanish equivalents; the bare `RF`/`LR` acronyms are intentionally NOT matched to avoid false positives). It feeds the SAME reprompt-once-then-`RuntimeError` loop as grounding (one failure policy, no parallel loop) and runs INDEPENDENT of `grounding_enabled`: it is evaluated BEFORE the grounding early-return inside `_invoke_narrative_with_grounding`, and UNCONDITIONALLY in M5's decision-matrix layer 2 (`leak_violations_2`, since a matrix reprompt can reintroduce a leak in the `modelo soporte` cell). It is the narrative mirror of the notebook `_validate_notebook_family_consistency` guard. `lr_rf_contrast` / `business` / non-classification pass `variant=None` → no-op (`[]`) → byte-identical (zero extra reprompt). M3-content is a documented fast-follow (its grounding is structurally disabled per #336; `variant` is not wired at its call site here).
- M4 deployment-recommendation redundancy fix (ml_ds + clasificación): the per-variant M4 coherence blocks (`_M4_CLASSIFICATION_COHERENCE_BLOCK_LR_ONLY` / `_RF_ONLY` / `_LR_RF_CONTRAST` in `prompts/clasificacion/M4_clasificacion/narrative.py`) now **ANCHOR guidance INSIDE the base §4.1–§4.5** instead of appending extra sections. They previously said *"Además del formato base… secciones con títulos EXACTOS"* and stacked `## Recomendación de despliegue (un solo modelo)` (single-model) / `## Modelo recomendado para la decisión` (contrast) on top of a complete 4.1–4.5, so the narrative emitted the deployment recommendation TWICE (the canonical `### 4.5 Recomendación de Despliegue` from the base `M4_CONTENT_GENERATOR_PROMPT`, plus the duplicate). The base **§4.5 is now the SINGLE deployment recommendation**; the variant blocks fold the single-model / contrast framing (cost-matrix threshold, drift, LR-vs-RF) into §4.1–§4.2 (value) and §4.5 (verdict), and explicitly forbid a second deployment heading. M4 questions (`m4_section_ref ∈ {4.1..4.5}`) were already coupled to the canonical 4.1–4.5, so this only improves narrative↔question coherence. Invariants preserved: `{pregunta_eje}` + escaped `{{computed_metrics_block}}`, placeholder parity, the #337 leak constraints (`lr_only` keeps LR/no RF, `rf_only` keeps RF/no LR, contrast names both). Prompt-only root-cause fix (no kill-switch; precedent #375/#420).
- Backstop (logger-only, user decision): `case_generator.m4_grounding.detect_duplicate_deployment_sections(prose)` is a pure detector (scans ATX headings, normalizes accents/section-number, flags `DUPLICATE_DEPLOYMENT_SECTION:<n>` when >1 deployment heading; zero-FP — §4.3 "Viabilidad de despliegue" and the business "Recomendación Ejecutiva Final" never match). `log_duplicate_deployment_sections(...)` runs it in `m4_content_generator` AFTER `_invoke_narrative_with_grounding`, gated on the resolved ml_ds+clf `variant` (non-`None` only there) and the `M4_DEPLOYMENT_DEDUP` kill-switch (Settings `m4_deployment_dedup`, default `true`; `backend/.env.example`). It NEVER reprompts, mutates `m4`, or fails a job — a residual duplicate (cosmetic) only emits a structured `logger.warning`. The DETERMINISTIC guarantee on the frozen golden set is `tests/golden_eval.check_m4_deployment_section_unique` (reuses the detector) wired into `NodeEvalInputs.m4_deployment_section_unique_ok` + `evaluate_downgrade_gate`. Tests: `tests/test_m4_deployment_dedup.py` + the section-15 guards in `tests/test_m4_clasificacion_dispatch.py`.

## M4 Financial Charts — Sensitivity/Tornado chart removed (2 charts)

- Scope: ALL cases (ambos perfiles, todas las familias). El Módulo 4 ("Impacto y Finanzas") emite **2 gráficos financieros** (Payback / Flujo de Caja + Comparativa de Escenarios A/B/C) en lugar de 3. Se retiró el **Gráfico de Sensibilidad (Tornado)**: era HUÉRFANO (sin sección §4.x de narrativa ni pregunta M4 que lo anclara — la narrativa/preguntas M4 no referencian ningún gráfico), el de mayor RIESGO DE FABRICACIÓN (swings ±20% sin ancla; en su momento los charts no pasaban por ningún guard de grounding — ese gap se cerró después para clasificación, ver «M4 Financial Charts — Grounding» abajo), y REDUNDANTE con el Payback (decoraba los mismos flujos de la opción única). Sobreviven el Payback (ancla §4.1–4.2) y la Comparativa A/B/C (ancla §4.5).
- **Root-cause prompt-only + backstop determinista (defensa en profundidad).** Las 3 superficies de prompt de charts —genérico `M4_CHART_GENERATOR_PROMPT`, ml_ds+clf `M4_CHART_PROMPT_CLASSIFICATION`, y el bloque business+clf `M4_CHART_LR_BUSINESS_BLOCK`— instruyen 2 gráficos (sección Tornado eliminada, "Gráfico 3"→"Gráfico 2", "EXACTAMENTE 3"→"2"). `m4_chart_generator` (`graph.py`) corre además `m4_grounding.drop_sensitivity_charts`: filtro puro/best-effort/**identity-based** (matchea `\b(tornado|sensibilidad|sensitivity)\b` accent-insensitive sobre `title`+`subtitle` SOLO; NUNCA un truncate-a-N ciego que tiraría la Comparativa) que elimina un gráfico de sensibilidad residual que el LLM emita pese al prompt. Se omite `academic_rationale`/`notes` para no falsear con la métrica *recall* ("sensibilidad") → zero-FP contra los 2 charts vivos. Corre dentro de un INNER `try` → un fallo del backstop nunca vacía los charts ni cae al `except` externo del nodo (que devolvería `[]`).
- **Revert instantáneo single-flag (`M4_CHART_DROP_SENSITIVITY`, Settings `m4_chart_drop_sensitivity`, default `true`; `backend/.env.example`):** off → el nodo selecciona los prompts `*_LEGACY` (3 gráficos, Tornado incluido) **y** salta el backstop → comportamiento **byte-idéntico** al previo, sin redeploy. Los `*_LEGACY` se conservan verbatim (`M4_CHART_GENERATOR_PROMPT_LEGACY`, `M4_CHART_PROMPT_CLASSIFICATION_LEGACY`, `M4_CHART_LR_BUSINESS_BLOCK_LEGACY`, `M4_CHART_BUSINESS_PROMPT_CLASSIFICATION_LEGACY` + tabla `M4_CHARTS_PROMPT_BY_FAMILY_LEGACY`); las tablas vigentes mantienen identidad de objeto (los tests de dispatch usan `is`).
- **Sin cambio de frontend/schema/sanitization/hash.** El renderer (`M4Finance.tsx` → `PlotlyChartsRenderer`) ya tolera un array de longitud variable; `m4Charts` ya está allowlisted (`case_sanitization.py`/`teacher_reads.py`). Cambio **solo de generación**: los casos ya generados conservan sus 3 gráficos (no se reprocesan). El architect (`_MLDS_ARCHITECT_PROMPT_SHA256`) NO se toca. Se suavizó además una copia estática de `M4Finance.tsx` que prometía "análisis de sensibilidad".
- **Anti-regresión:** drift-locks de prompt (vigentes = "EXACTAMENTE 2" / sin tornado / sin "Gráfico 3"; LEGACY = "EXACTAMENTE 3" / con "Tornado") + golden oracle `check_m4_charts_no_sensitivity` (`NodeEvalInputs.m4_charts_no_sensitivity_ok` → `evaluate_downgrade_gate`). Tests: `backend/tests/test_m4_chart_removal.py`.

## M4 Financial Charts — Grounding (ml_ds+clf · business+clf)

- Scope: family `clasificacion` para AMBOS perfiles, gateado por `_is_classification_family(state)` dentro de `m4_chart_generator` (`graph.py`), detrás del kill-switch `M4_CHART_GROUNDING`. Cierra el gap documentado arriba ("los charts no pasaban por los guards de #243/#337"): el nodo inyectaba `computed_metrics_block` pero NO validaba la salida, y los prompts SANCIONABAN la fabricación ("Valores estimados basados en benchmarks de {industria}" — el tell exacto del caso real adjunto, junto a un "AUC de 0.8637" nunca contrastado contra el M3 ejecutado). No-op byte-idéntico para no-clasificación y con el switch en off.
- Validador puro `m4_grounding.validate_m4_chart_grounding(charts, metrics_block, variant)` → `[(index, [violations])]`: 3 checks por gráfico NO-sensibilidad (los de tornado los posee `drop_sensitivity_charts`), cada uno REUSANDO un primitivo endurecido. (A) **Métrica del modelo no anclada** `detect_unanchored_chart_metrics` — sibling chart-local de `narrative_grounding.detect_unanchored_adjacent_metrics` que ENSANCHA el separador keyword→valor para aceptar el conector español "de"/"del" (el bug insignia es literal "el AUC **de** 0.8637", que la adyacencia estricta de M5 PIERDE como FN documentado); reusa `_extract_anchor_numbers`/`_within_tolerance`/guard de miles/`>200`; keyword-set sin overloads de negocio (`baseline`/`feature`/`variable`/`dummy` fuera); **exige forma de métrica** (decimal `0.86` o porcentaje `86%`, NUNCA entero pelado) para que el conector `de`/`del` no marque conteos de negocio (`"precisión de 30 años"`, `"importancia de 3 factores"`); SOLO activo si `has_metric_anchors(metrics_block)` → ml_ds+clf con M3 ejecutado, no-op honesto para business/degradado. (B) **Fuga de modelo no seleccionado** `detect_unselected_model_mentions` (#337; no-op para `lr_rf_contrast`/business donde `variant=None`). (C) **Tell de fabricación** `detect_benchmark_fabrication` (NUEVO; regex ACOTADO disclaimer-shape `estimad*[\s\S]{0,40}benchmark` / `benchmark*[\s\S]{0,40}(sector|industria|extern*)`, zero-FP contra un "benchmark interno (Exhibit 1)" anclado). Escanea SOLO los campos de PROSA (`title/subtitle/description/notes/academic_rationale`); `traces`/`layout` NO se chequean (las barras de negocio se derivan legítimamente). Las cifras de negocio CO-UBICADAS (ROI 22.2%, 15%) NUNCA se marcan porque (A) es keyword-anclado (su token previo no es métrica).
- **Cobertura determinista por cohorte:** ml_ds+clf = (A)+(B)+(C); business+clf = (C) + endurecimiento de prompt (sin M3 ejecutado que anclar → (A)/(B) son no-op honestos). Las cifras $/ROI/payback↔narrativa quedan cubiertas SOLO por prompt (un check numérico determinista sería alto-FP: cifras derivadas mensual=anual/12 + narrativa en prosa libre; misma postura que `validate_narrative_grounding`, que deja pasar cifras de negocio).
- **Reprompt-once-then-DROP** (`_apply_m4_chart_grounding` en `graph.py`, espeja `_apply_m5_questions_coherence`): en violación reprompta UNA vez por CONCATENACIÓN (`build_m4_chart_grounding_reprompt`; nunca re-`.format` — llaves de JSON/chart); cualquier gráfico que SIGA violando tras el reprompt se DESCARTA por RE-VALIDACIÓN (los índices pass-1 son inútiles porque el reprompt regenera todo el set); si eso vaciara el set, cae a pass-1 menos sus infractores (set vacío solo si TODO gráfico fabrica dos veces → render vacío grácil, preferible a 2 mentiras). NUNCA falla el job, NUNCA muestra dato falso (filosofía del nodo de charts, espeja `drop_sensitivity_charts`). La variante de notebook RESUELTA (None salvo ml_ds+clf, igual que m5_questions) alimenta (B). Corre ANTES del backstop de sensibilidad (que retira un tornado residual del reprompt); el validador SALTA charts de sensibilidad. Best-effort: cualquier throw degrada a los charts de entrada. Logger-only estructurado (fired/fixed/degraded+`dropped_count`, tipos enumerados, sin texto crudo) → SIN nueva clave canónica/state (sin riesgo de fan-out merge con `m4_questions_generator`, que corre en paralelo y escribe claves disjuntas).
- **Endurecimiento de prompt (root-cause, defensa en profundidad), alcance ESTRICTO a los 2 cohortes:** el genérico compartido `M4_CHART_GENERATOR_PROMPT` queda INTACTO (no-clf + base business byte-idénticos → blast-radius cero). Se editaron SOLO `M4_CHART_PROMPT_CLASSIFICATION` (ml_ds+clf: quita la invitación "estimaciones … benchmarks", añade "usa solo cifras de M4/Exhibit 1 o derivadas; NUNCA inventes; métricas SOLO EXACTAMENTE del bloque verificado") y `M4_CHART_LR_BUSINESS_BLOCK` (business+clf: el bullet de Honestidad OVERRIDE la línea de benchmarks del base, palabra `PROHIBIDAS`). Los `*_LEGACY` intactos. El endurecimiento NO está detrás del switch (mejora de una vía, como las líneas de frontera de M2/M5); el switch gobierna solo el guard runtime. Follow-up documentado (NO en este cambio): el hatch de benchmarks sigue en el genérico para las familias no-clf.
- **Kill-switch `M4_CHART_GROUNDING`** (Settings `m4_chart_grounding`, default `true`; `backend/.env.example`): off → el guard runtime se salta (charts tal cual el LLM); revert instantáneo sin redeploy. **Sin cambio de frontend/schema/sanitization/architect-hash** (renderer `M4Finance.tsx`→`PlotlyChartsRenderer` ya tolera array 2/1/0; `m4Charts` ya allowlisted). Anti-regresión: golden oracle `check_m4_charts_no_fabrication` (charts-only, reusa `detect_benchmark_fabrication`) → `NodeEvalInputs.m4_charts_no_fabrication_ok` → `evaluate_downgrade_gate`; las garantías de métrica/fuga se cubren en unit tests (requieren fixtures de métricas/variante por-job que el golden set no carga). Tests: `backend/tests/test_m4_chart_grounding.py`. Casos ya generados NO se reprocesan.

## M4 Fabrication Guard — matar "benchmarks de {industria}" forzado (Issue #436, Fase 0 ADR 0003)

- Scope: AMBOS perfiles, TODAS las familias. Cierra el follow-up documentado en la sección anterior ("el hatch de benchmarks sigue en el genérico para las familias no-clf") y la grieta de NARRATIVA del ADR 0003: los prompts genéricos de M4 ORDENABAN fabricar cuando faltaban datos — chart genérico `M4_CHART_GENERATOR_PROMPT` §`# Your Boundaries` ("Valores estimados basados en benchmarks de {industria}") y narrativa base `M4_CONTENT_GENERATOR_PROMPT` §`## Error Handling` ("estimaciones de benchmarks de industria"). Como la narrativa clf se concatena DESDE la base en import-time (`M4_NARRATIVE_PROMPT_CLASSIFICATION{,_LR_ONLY,_RF_ONLY}`, `M4_PROMPT_CLASSIFICATION`, `M4_BUSINESS_PROMPT_CLASSIFICATION` = base + bloque), AMBOS cohortes clf heredaban la invitación. NO introduce el Impact Lens (Fase 1+).
- **Fix de prompt = mejora de UNA VÍA (in-place, NO detrás del switch)** — espeja la decisión de `M4_CHART_GROUNDING` ("el endurecimiento NO está detrás del switch; el switch gobierna solo el guard runtime"). Las dos secciones se reescriben para degradar a CUALITATIVO ("omite esa serie o exprésala de forma CUALITATIVA; NUNCA inventes valores ni los justifiques con benchmarks / estimaciones del sector / cifras externas"), espejo de `M4_CHART_PROMPT_CLASSIFICATION` y `M4_CHART_LR_BUSINESS_BLOCK` que ya lo prohibían. El edit a la base propaga a las 5 derivadas (un solo cambio cubre toda la narrativa). NO se crean constantes `*_PREFAB`: el sufijo `_LEGACY` ya lo usa #426 para el path 3-gráficos (`M4_CHART_GENERATOR_PROMPT_LEGACY`), que NO se toca y conserva su invitación VERBATIM (revert de #426). Revert del fix de prompt = git.
- **Guard runtime = LOGGER-ONLY, best-effort** (decisión D2): `m4_grounding.log_narrative_benchmark_fabrication` (en `m4_content_generator`, junto al backstop `m4_deployment_dedup`) y `log_chart_benchmark_fabrication` (en `m4_chart_generator`, DESPUÉS del reprompt-then-DROP clf y de `drop_sensitivity_charts` → para clf ve el set ya limpio, sin doble-conteo). Ambos REUSAN el detector zero-FP `detect_benchmark_fabrication` (disclaimer-shape `estimad…benchmark` / `benchmark…sector/industria/externo`, cuantificador acotado sin ReDoS), corren para TODAS las familias/perfiles (el riesgo es de DOMINIO, no de cohorte) y NUNCA reprompt/mutan/fallan el job (espejan `log_duplicate_deployment_sections`; tragan toda excepción internamente → no pueden tripear el `except` externo del nodo que degradaría M4 al placeholder de error). El reprompt-then-DROP de charts clf (`_apply_m4_chart_grounding`, switch `M4_CHART_GROUNDING`) queda INTACTO.
- **Anti-FP** (el detector corre ahora en TODAS las familias): NO disparan la regla legítima de CAGR-cap del sector ("limitado al CAGR del sector de 8%"), `estimaciones conservadoras` sin la palabra `benchmark`, `benchmark interno (Exhibit 1)` anclado, las cifras de negocio ($/ROI/%) ni `sensibilidad (recall)`.
- **Kill-switch `M4_FABRICATION_GUARD`** (Settings `m4_fabrication_guard`, default `true`; `backend/.env.example`): off → se salta SOLO el guard runtime (el fix de prompt queda; revert del guard sin redeploy). **Sin nueva clave canónica/state, sin entrada en `case_sanitization`, sin migración/frontend/architect-hash; sin estado nuevo → sin riesgo de fan-out merge con `m4_questions_generator`.** Anti-regresión: golden oracle NUEVO `check_m4_narrative_no_fabrication` → `NodeEvalInputs.m4_narrative_no_fabrication_ok` → `evaluate_downgrade_gate` (el de charts `check_m4_charts_no_fabrication` ya existía). El drift-lock `test_generic_chart_prompt_unchanged_zero_nonclf_blast_radius` se VOLTEÓ (ahora asserta invitación ausente + hardening presente). Tests: `backend/tests/test_issue436_m4_fabrication_guard.py`. Casos ya generados NO se reprocesan.

## M4 Impact Lens — marco de valor case-agnostic (Issue #437, Fase 1 ADR 0003)

- Scope: AMBOS perfiles, TODAS las familias. Cierra la grieta `dominio-no-comercial × marco-financiero-forzado` del ADR 0003: M4 ("Arquitecto Financiero") hardcodeaba ROI/Payback/NPV/$ para TODO caso sin ramificar por dominio. Decisión **D-E (β)**: en vez de *añadir* bloques de lente, se **SIMPLIFICA** M4 a un núcleo value-neutral cuyo *marco de valor* (métrica primaria, filas KPI §4.5, métricas del Gráfico 2, framing de preguntas) lo aporta una **lente** ortogonal al dominio y a la familia. El inventario de anclas confirmó que el marco financiero de M4 es SOFT (ningún test duro lo fija). **No toca el architect SHA-locked** (eso es Fase 2).
- **Una lente, resuelta una vez (DD1/F1).** `case_generator.impact_lens` (módulo puro, espejo de `m4_grounding`) define `IMPACT_LENS_CATALOG` (5 lentes: `financial_roi`/`operational_efficiency`/`clinical_outcomes`/`learning_outcomes`/`environmental_outcomes`) + `resolve_impact_lens_from_industry(raw)` (normaliza casefold+acentos; mapea las 8 ETIQUETAS Y valores del dropdown `INDUSTRIAS_OPTIONS`; default `financial_roi`) + `build_impact_lens_hint(lens)` (bloque «MARCO DE VALOR» **brace-free** → seguro antes de `.format`; `financial_roi` reproduce ROI/Payback/NPV). La lente se resuelve en `core/authoring.py` `state_input` desde la etiqueta de intake **ANTES** de que `case_architect` sobrescriba `state["industria"]` con su sustantivo libre, y **sobrevive** el merge de `resume_cached_nodes` (resume-robust por construcción; `case_architect` nunca emite `impact_lens`). `_resolve_impact_lens(state)` (`graph.py`) la lee con default seguro; Fase 2/3 le añadirán `value_model` + override docente (precedencia híbrida D-A).
- **Núcleo neutral + `*_NEUTRAL` twins.** Los 3 nodos M4 (`m4_content_generator`, `m4_chart_generator`, `m4_questions_generator`) seleccionan los prompts `*_NEUTRAL` (identidad "de Impacto", §4.5 difiere las filas KPI al hint, Gráfico 2 usa métricas de la lente, preguntas P2 "¿El valor proyectado justifica?" en vez de "¿El ROI justifica?" — **H1**) y **concatenan** el hint cuando `settings.impact_lens` (default `true`); off → seleccionan los prompts FINANCIEROS originales (intactos) y saltan el hint = **byte-idéntico** a pre-#437 (**DD5**, mecanismo `*_LEGACY` como `M4_CHART_DROP_SENSITIVITY`). Para charts la lente aplica SOLO en el camino de 2 gráficos; el revert de 3-gráficos (tornado, `M4_CHART_DROP_SENSITIVITY=false`) la salta. Anclas load-bearing conservadas VERBATIM: §4.5 `Recomendación de Despliegue`/`Ejecutiva Final`, §4.3 distinta, `EXACTAMENTE 2`/`EXACTAMENTE 3`, `NUNCA inventes`+benchmark (#436), leak-guard #337.
- **DD3:** los costos SIEMPRE en USD; la lente reencuadra solo el lado del VALOR (cost-effectiveness $/outcome). El hint es CONCATENACIÓN, NUNCA un `{placeholder}` (precedente anti-`.format()`). **Sin nueva clave canónica visible al estudiante, sin entrada en `case_sanitization`, sin migración de DB, sin cambio de frontend/schema.** Casos ya generados NO se reprocesan.
- **Kill-switch `IMPACT_LENS`** (Settings `impact_lens`, default `true`; `backend/.env.example`): off = passthrough financiero byte-idéntico (revert instantáneo sin redeploy). Anti-regresión: golden oracle `check_m4_lens_kpi_coherence` (lente no-financiera ⇒ §4.5 sin filas `ROI proyectado`/`NPV estimado`; financial/business/ausente = n/a) → `NodeEvalInputs.m4_lens_kpi_coherence_ok` → `evaluate_downgrade_gate`. Cobertura semántica por-lente completa diferida a Fase 3 (R9). Tests: `backend/tests/test_impact_lens.py` (módulo puro) + `test_issue437_impact_lens_dispatch.py` (dispatch + DD5 + F1). El riesgo #1 (H6: la salida NUEVA del cohorte comercial cambia) se mitiga con el hint `financial_roi` near-equivalente, el eval live Pro-vs-neutral y el revert `*_LEGACY`.

## M1 Architect Impact Lens — value_model + opciones acordes a la lente (Issue #437, Fase 2 ADR 0003)

- Scope: AMBOS perfiles, TODAS las familias (el architect es compartido). Cierra la coherencia M1↔M4 (decisión **D-C**, regen SHA aceptado): el architect emite un `value_model` opcional y reencuadra la **dimensión de superioridad de las opciones A/B/C** según la lente (no siempre "A=mayor ROI"). **Acotado por DD3:** Exhibit 1 sigue siendo un **P&L en USD** (la base de COSTO, legítima para cualquier organización — un hospital/escuela también tiene ingresos/costos); SOLO se reencuadra el lado del VALOR. Se **rechazó** explícitamente la lectura maximalista (reemplazar el P&L por QALYs/unidades no monetarias) = violaría DD3/D-D (eso es un ADR aparte).
- **`value_model` (schema, coerce-never-reject).** `tools_and_schemas.ValueModel = {lens, primary_metric_name, kpi_rows}` opcional en `CaseArchitectOutput`. El validador `_coerce_lens` reusa `impact_lens.normalize_impact_lens` (espejo de `BusinessCostMatrix._normalize_currency`): una lente inválida/ausente COERCE al default, NUNCA rechaza (un raise nulificaría toda la salida del architect). Prompt-side: **NO** clave canónica/student-facing, **NO** entrada en `case_sanitization`, **NO** migración.
- **Bloque de prompt ADITIVO + brace-free (clave del bajo riesgo).** `ARCHITECT_IMPACT_LENS_BLOCK` (`prompts/_architect_base.py`) se **APENDA** en `_assemble_architect_prompt(context, lens_on=...)` SOLO cuando `lens_on`; **no edita NADA del prompt base** (lo que rompería el off-path). Por ser puramente aditivo, **`lens_on=False` es byte-idéntico a pre-#437**: el `_MLDS_ARCHITECT_PROMPT_SHA256` existente **SIGUE MATCHEANDO** (NO se regenera — más seguro que el regen que D-C autorizó). El camino `lens_on=True` tiene su **propio** hash congelado `_MLDS_ARCHITECT_LENS_PROMPT_SHA256` + su diferencial `test_architect_lens_on_unchanged_by_business_gate`. El bloque es brace-free (0 `{}`) → seguro antes de `.format()`.
- **Refinamiento de la lente (D-A híbrido) — RESUME-ROBUST.** `case_architect` llama `_assemble_architect_prompt(lens_on=settings.impact_lens_architect)` y persiste `value_model` (gateado por el switch: `None` cuando off). `_resolve_impact_lens(state)` aplica la precedencia: **`state["value_model"]["lens"]`** (refinamiento del architect, más informado) → `state["impact_lens"]` (intake) → default. **CRÍTICO (defecto P1 atrapado por el review adversarial):** `case_architect` **NO escribe `state["impact_lens"]`** — `state_input` re-inyecta el valor de intake en CADA intento y, como `impact_lens` es un canal last-write-wins, un job RESUMido lo **clobbearía** de vuelta a intake (el architect se short-circuitea en resume y nunca lo re-emite) → M4 leería la lente de intake mientras M1 fue enmarcado para la refinada = caso incoherente. La solución: el refinamiento viaja en `value_model`, que **NO** está en `state_input` → sobrevive el checkpoint durable en resume; `_resolve_impact_lens` lo prefiere. Único valor consumido por todos los nodos M4, también en resume (DD1).
- **Kill-switch SEPARADO `IMPACT_LENS_ARCHITECT`** (Settings `impact_lens_architect`, default `true`; `backend/.env.example`) — distinto de `IMPACT_LENS` (lado M4) para revert GRANULAR del architect sin perder el cambio M4. Off → `lens_on=False` → architect byte-idéntico + sin refinamiento (intake manda). Anti-regresión: golden oracle `check_architect_value_model_lens_valid` → `NodeEvalInputs.architect_value_model_lens_valid_ok` → `evaluate_downgrade_gate` (la coherencia semántica lente↔dominio es eval live, diferida). Tests: `backend/tests/test_issue437_p2_architect_lens.py` + los locks de SHA en `test_issue301_pr2b_architect.py` (off byte-idéntico + lens-on frozen hash + diferencial + brace-free). Casos ya generados NO se reprocesan.

## M4/M5/M6 Impact Lens — override docente + parametrización cross-módulo (Issue #437, Fase 3 ADR 0003)

- Scope: AMBOS perfiles, TODAS las familias. Capa FINAL del ADR 0003: control docente de la lente + completar la coherencia cross-módulo (M1/M4/M5/M6) que DD1 promete. Construye sobre Fase 1 (núcleo M4 + hint) y Fase 2 (architect `value_model`); NO los rehace. **NO toca el architect SHA-locked** (el hint de override vive en el NODO, no en `ARCHITECT_IMPACT_LENS_BLOCK`).
- **Override docente (D-A híbrido, precedencia MÁS ALTA).** Campo OPCIONAL `impact_lens` en `IntakeRequest` (`shared/app.py`, `ImpactLensLiteral | None`, Pydantic Literal → 422 en garbage), persistido a `task_payload.impact_lens_override`, leído en `core/authoring.py` `state_input` (passthrough, puede ser None — espeja `algorithm_mode`), y enrutado como **nivel 0** en `_resolve_impact_lens` (`graph.py`): `state["impact_lens_override"]` (si ∈ `IMPACT_LENS_KEYS`) → `value_model.lens` → `state["impact_lens"]` (intake) → default. **CRÍTICO:** membership-check, NO `normalize_impact_lens` (coercería garbage→`financial_roi` y un override falso ganaría a `value_model`). Intake-only ⇒ `state_input` lo re-inyecta cada intento ⇒ **resume-stable**; ningún nodo lo escribe (sin clobber). `ADAMState` gana `impact_lens_override: NotRequired[str]`.
- **M1 honra el override (Item 1b, hash-safe).** `case_architect` apenda `build_impact_lens_architect_hint(override)` (brace-free, `impact_lens.py`) DESPUÉS de `_assemble_architect_prompt`, SOLO cuando hay override válido Y `impact_lens_architect` on. Constriñe la inferencia-por-dominio del bloque Fase 2 a la lente fijada → M1 enmarca opciones + emite `value_model.lens` por el override = coherente con M4/M5/M6 (que lo toman del resolver). **Sin override = byte-idéntico a Fase 2** (financiero Y no-financiero-por-industria): el hint NO usa el resolved lens — eso perturbaría el camino feliz Fase 2 e introduciría divergencia M1≠M4 bajo refinamiento `value_model` (defecto atrapado en review adversarial) — solo el override explícito. Hash-safe: los SHA-tests hashean `_assemble_architect_prompt()` directo; el nodo apenda al prompt ya formateado (sin 2.º `.format`); `ARCHITECT_IMPACT_LENS_BLOCK`/hashes INTACTOS. `value_model.{lens,kpi_rows,primary_metric_name}` se consume SOLO en `_resolve_impact_lens` → un `value_model.lens` divergente bajo override es inerte.
- **M5 narrativa lens-aware (solo la matriz de decisión).** `m5_content_generator` apenda `build_impact_lens_m5_hint(lens)` (brace-free, SIN §4.5) tras `_maybe_business_classification_prompt`, SOLO cuando `_resolve_impact_lens(state) != DEFAULT_IMPACT_LENS` (financiero = sin hint = byte-idéntico para el cohorte dominante incluso switch on; la matriz ya es lente-neutral). Reencuadra la columna `KPI esperado` a la métrica de la lente. La cabecera `| acción | KPI esperado | riesgo | modelo soporte |` queda byte-idéntica (constante import-time; el hint se APENDA). El memo `m5_questions_generator` (ya neutral/jargon-free) NO se toca (follow-up documentado).
- **M6 synopsis lens-aware.** `module_guide_block.build_module_guide_block(..., lens=None)` gana el param `lens`; el dict CURADO **currency-free** `_M4_VALUE_FRAME_BY_LENS` reencuadra SOLO la línea `aprende` de M4 (`None`/`financial_roi` = prosa de hoy byte-idéntica; NO reusa `IMPACT_LENS_CATALOG.kpi_rows` que llevan `$/outcome` → rompería `test_block_byte_identical_through_usd_enforce`). Las LABELS (drift-lock vs `caseViewerConfig.ts`) y los verbos de veredicto (`M4_VERDICT_*`) NO se tocan. `teaching_note_part1` pasa `lens=_resolve_impact_lens(state) if settings.impact_lens else None` a AMBAS llamadas.
- **Kill-switches REUSADOS (no nuevos).** M5/M6 reusan `IMPACT_LENS`; el hint de override del architect reusa `IMPACT_LENS_ARCHITECT` → revert ATÓMICO del marco de valor M1/M4/M5/M6 (evita el estado incoherente M4=financiero/M5=clínico). El override NO tiene switch propio (inerte cuando los switches están off — sus consumidores están gateados). Caveat documentado: mantener AMBOS switches alineados (default on) — un estado mezclado puede dar M1↔M4/M5/M6 incoherente. **Sin clave canónica/student-facing, sin `case_sanitization`, sin migración, sin schema/canónico nuevo** (override = solo prompt-side). Casos ya generados NO se reprocesan.
- **Frontend.** `authoringFormConfig.IMPACT_LENS_OPTIONS` (+ sentinel `IMPACT_LENS_AUTO` porque Radix Select prohíbe value `""`); `adam-types` `ImpactLens` + `impact_lens`/`impactLens` (+ `EMPTY_FORM`); `useAuthoringJobProgress.buildAuthoringJobCreateRequest` mapea `impact_lens: formData.impactLens ?? null`; `AuthoringForm` selector opcional junto a Industria (default "Automático" = null) + persistencia de form-state (restaura solo null o key válida). El frontend envía la **KEY** canónica (no la etiqueta, distinto de `industry`); la Literal del backend la valida.
- **Anti-regresión.** Gate central DD1 cross-módulo (R2) determinista (happy + resume + override, LLMs mockeados): `test_issue437_p3_impact_lens_fase3.py`. Golden por-lente (5 lentes) reusa `check_m4_lens_kpi_coherence`/`check_architect_value_model_lens_valid` ya cableados a `evaluate_downgrade_gate`; live_llm opt-in diferido (R9, sin materializar fixtures pesadas). Otros: `test_impact_lens.py` (Literal↔KEYS, hints brace-free), `test_m6_module_guide.py` (lens byte-idéntico/reframe/currency-free + drift `_M4_VALUE_FRAME_BY_LENS`↔KEYS), `test_issue301_pr2b_architect.py` (SHA verde). El grounding ya cubre las 5 lentes (Fase 0/#436, `detect_benchmark_fabrication` profile/family-agnóstico); PROHIBIDO anclas numéricas por-unidad (R7).

## Formulario docente — "Marco de valor" oculto + suggester acotado al catálogo soportado

- **Selector "Marco de valor" (Impact Lens, #437 Fase 3) OCULTO del formulario (frontend-only).** `AuthoringForm.tsx` ya no renderiza el `<Select>` de la lente (Industria queda a ancho completo) y se quitó el handler `onImpactLensChange`. **El contrato del backend NO cambia:** `buildAuthoringJobCreateRequest` sigue enviando `impact_lens: formData.impactLens ?? null` → ahora SIEMPRE `null`, así que la lente se resuelve automáticamente desde la industria (`resolve_impact_lens_from_industry` + refinamiento `value_model` del architect). El plumbing (`impactLens` state, restore/persist, `IMPACT_LENS_OPTIONS`/`IMPACT_LENS_AUTO`, tipo `ImpactLens`) se conserva → reactivar = re-renderizar el bloque (revert por git). El override `impact_lens_override` sigue funcional pero queda dormante (el form nunca envía non-null). **Supersede** la línea de #437 Fase 3 "`AuthoringForm` selector opcional junto a Industria". Sin migración/schema/`case_sanitization`; los tests de mapeo (`useAuthoringJobProgress.test.ts`, `api.test.ts`) siguen verdes (prueban `impact_lens`, no la UI).
- **Suggester "Sugerir Caso y Dilema" acotado SOLO a lo que el sistema construye.** La prosa estática del prompt de `suggest_service._build_prompt` (secciones Workflow + Boundaries) anunciaba familias/algoritmos retirados (`serie_temporal`/`nlp`/`recomendacion`, ARIMA/Prophet/XGBoost/TF-IDF/VADER/LDA, métricas R²/MAPE) que CONTRADECÍAN la taxonomía reducida que viene justo después → el LLM podía sugerir un escenario/pregunta que el generador no puede construir. Ahora las familias y los ejemplos de algoritmo se **DERIVAN** del mismo catálogo reducido que el formulario y la taxonomía (`_active_families`/`_active_algorithm_names` → `_technique_items` → `ALGORITHM_CATALOG_REDUCED`), inyectados por f-string; el Workflow refuerza usar el contexto del docente (industria, módulo del syllabus, unidad temática). Las familias permanentemente NO soportadas (series temporales/NLP/recomendación/anomalías/grafos) se nombran como prohibición explícita, mientras `regresion`/`Random Forest` (dependientes del kill-switch) **NO** se prohíben hard (serían incorrectas con el switch OFF). El **anchor block** (cuando el docente ya eligió algoritmo) ya era coherente y NO se tocó — sus strings están bajo test verbatim en `test_suggest_scenario_anchor.py`. Sin kill-switch nuevo (mejora de una vía, como otras líneas de frontera de prompt; revert = git), sin cambio de API/schema/contrato del suggester. Tests: `backend/tests/test_suggest_prompt_supported_scope.py` (drift-locks: prosa sin tokens retirados, enum `problemType` = familias activas, sincronía con `ALGORITHM_CATALOG_REDUCED`).

## M4 Impact Lens — 5ª lente `environmental_outcomes` + industria Medio ambiente (Issue #505)

- Scope: AMBOS perfiles, TODAS las familias. Extiende #437 de forma PURAMENTE ADITIVA con una 5ª lente `environmental_outcomes` (economía ambiental / valoración contingente) + la industria "Medio ambiente / Sector público". Raíz: sin lente ambiental ni industria de medio ambiente, `resolve_impact_lens_from_industry` caía a `financial_roi` y M4 expresaba el valor de un caso ambiental como ROI/NPV/payback (corrida live "AquaVida"). La lente reencuadra el VALOR a servicios ecosistémicos / costo por hectárea / disposición a pagar (WTP) agregada (kpi_rows lenguaje natural, sin `Δ`/`$/`; DD3: costos en USD).
- **Edits (single-source + drift-locks):** `impact_lens.py` (constante `IMPACT_LENS_ENVIRONMENTAL_OUTCOMES`, entrada en `IMPACT_LENS_CATALOG`, miembro en `ImpactLensLiteral`, 2 entradas valor+etiqueta en `_INDUSTRY_LENS_BY_KEY`, `__all__`); `module_guide_block._M4_VALUE_FRAME_BY_LENS` (entrada currency-free, exigida por el drift test `:277`); `_architect_base.ARCHITECT_IMPACT_LENS_BLOCK` (enumera la 5ª lente + mapeo de dominio + ejemplo de opciones → **regenera `_MLDS_ARCHITECT_LENS_PROMPT_SHA256`**, el off-path `_MLDS_ARCHITECT_PROMPT_SHA256` INTACTO); frontend `authoringFormConfig` (`INDUSTRIAS_OPTIONS`+`IMPACT_LENS_OPTIONS`) y `adam-types.ImpactLens`.
- **Por qué el architect (no solo el catálogo):** `_resolve_impact_lens` rankea `value_model.lens` del architect SOBRE la lente de industria; con el architect enseñando solo 4 lentes, seleccionar la industria ambiental sola NO activaba la lente. Enseñar la 5ª lente hace coherentes AMBOS caminos (industria-default Y override docente). Kill-switches REUSADOS (`IMPACT_LENS`/`IMPACT_LENS_ARCHITECT`), sin switch nuevo, sin nueva clave canónica/state/`case_sanitization`/migración. Tests: `test_impact_lens.py`, `test_golden_eval.py`, `test_issue437_p2`, `test_issue301_pr2b_architect.py` (SHA regen). Casos ya generados NO se reprocesan.

## M4 KPI labels legibles + fuga de identificadores sklearn (Issue #437 follow-on)

- Scope: AMBOS perfiles, TODAS las familias. Cierra dos defectos hallados en un caso ml_ds+RandomForest+educación, ambos en la tabla §4.5 de M4.
- **Finding A — etiquetas KPI legibles (catálogo determinista).** Las filas §4.5 de las lentes NO financieras eran strings curados en taquigrafía de hoja de cálculo (`Δ retención/graduación (%)`, `$/estudiante-retenido (USD)`, `Δ tasa de defecto/scrap`, `$/unidad evitada (USD)`, `Costo-efectividad ($/outcome, USD)`, `Outcomes adversos evitados`) que `build_impact_lens_hint` obliga a reproducir VERBATIM ("usar EXACTAMENTE estas filas") → el catálogo ES la etiqueta student-facing. Se reescribieron los ~9 `kpi_rows` + `primary_metric_name` con token-shapes de `IMPACT_LENS_CATALOG` (las 3 lentes no financieras) a español natural (`Cambio en retención/graduación (puntos %)`, `Costo por estudiante retenido (USD)`, `Costo por unidad evitada (USD)`, `Reducción de downtime / mejora de OEE`, `Eventos adversos evitados`, `Costo por evento evitado (USD)`); `financial_roi` queda byte-idéntico (ROI/Payback/NPV, riesgo H6). Se mantiene la instrucción "EXACTAMENTE" → limpio POR CONSTRUCCIÓN, determinista. El golden oracle `check_m4_lens_kpi_coherence` (solo verifica ausencia de `roi proyectado`/`npv estimado` en lentes no financieras) NO se afecta. Drift-lock nuevo en `test_impact_lens.py` (`test_non_financial_kpi_labels_are_natural_language_not_token_shorthand`): falla si cualquier `kpi_rows`/`primary_metric_name` no financiero reintroduce `Δ` o el slug `$/`.
- **Finding B — fuga `num__`/`cat__` a la prosa (root-cause + defensa en profundidad).** Los nombres de feature de sklearn `ColumnTransformer` (`get_feature_names_out()` → `num__col`/`cat__col`/`remainder__col`) viajaban en `top_features` → `build_computed_metrics_block` → `_sanitize_label` (cuyo regex `[^A-Za-z0-9_ .\-/]` CONSERVA `_`) → `computed_metrics_block` → prosa M4/M5 (el LLM los reproducía, p.ej. "Data Drift sobre num__payment_delay_days"). Raíz cerrada con `_strip_transformer_prefix` (puro, lowercase-anclado `^(?:[a-z][a-z0-9]*__)+`, nunca vacía el nombre, maneja prefijos anidados `preprocess__num__col`) aplicado DENTRO de `_sanitize_label` → cubre AMBOS call sites (feature name + string metric value) y por tanto M3-content/M4/M5/charts en un solo choke point DRY. Es **INCONDICIONAL** (espeja el strip de `target_col`; un prefijo de transformador no tiene uso legítimo en prosa). El bloque queda byte-idéntico para nombres sin prefijo (`tenure_months`) → tests #243 verdes.
- **Backstop logger-only + kill-switch.** `detect_raw_identifier_leak(prose)` (puro, espeja `detect_unselected_model_mentions`; flag `\b[a-z][a-z0-9]*__\w+\b` → `RAW_IDENTIFIER:<match>`; snake_case normal de un solo `_` NUNCA matchea) + `log_raw_identifier_leak(...)` (best-effort, NUNCA reprompts/muta/falla el job; sin PII, solo tokens enumerados) cableados en `m4_content_generator` y `m5_content_generator` tras la generación, detrás del kill-switch `CASE_IDENTIFIER_LEAK_GUARD` (Settings `case_identifier_leak_guard`, default `true`; `backend/.env.example`). Off → solo se salta el backstop; el strip determinista queda. Golden oracle `check_no_raw_identifier_leak` → `NodeEvalInputs.narrative_no_raw_identifier_ok` → `evaluate_downgrade_gate`.
- **Sin nueva clave canónica/state, sin entrada en `case_sanitization`, sin migración, sin cambio de frontend/architect-hash** (el renderer ya pinta las celdas tal cual; fixes 100% de generación). El display del notebook M3 (tabla de importancias en Colab) **ya NO muestra `num__`/`cat__`** — el strip se aplica también dentro de las celdas `pipeline_lr`/`pipeline_rf` (ver «M3 ml_ds+LR notebook — coherencia y calidad para producción» abajo), preservando la byte-equivalencia de `build_computed_metrics_block`/`metrics_summary` #353/#427 (el strip downstream es idempotente). Casos ya generados NO se reprocesan. Tests: `test_raw_identifier_leak_guard.py` (matriz FP/FN del strip/detector + integración + gate) + guards de catálogo en `test_impact_lens.py`.

## M3 ml_ds+LR notebook — coherencia y calidad para producción

- Scope: `studentProfile == "ml_ds"` + family `clasificacion` (las 3 variantes `lr_only`/`rf_only`/`lr_rf_contrast`; el notebook M3 es ml_ds-only — `m3_notebook_generator` hace no-op para `business`). TRES fixes student-facing a celdas DETERMINISTAS (el LLM las copia verbatim), **prompt-only, sin kill-switch** (degradan por el `try/except` existente; revert = `git`). Validados ejecutando las celdas reales contra 6 datasets sintéticos + review adversarial multi-agente (0 hallazgos P0-P2).
- **§3.0 EDA Express CONTRACT-FIRST** (`clasificacion/notebook.py`): la celda resolvía el target por alias-heurístico → para un caso no-churn (fraude/mora) mostraba un ID (`transaction_id`, última categórica all-unique) como "Target candidato" mientras §3.0.5+ modelaba el target del contrato → notebook auto-contradictorio. Ahora resuelve `"{contract_target_name}".strip()` primero (espeja `dummy_baseline` #348), con fallback alias-first **byte-idéntico** si no hay contrato. Está FUERA de `_executable_region` (que empieza en `dummy_baseline`) → los locks de orden #348 (`test_dummy_baseline_resolves_contract_target_first`) y #230 intactos. El literal viene de `_safe_contract_target_name` (guard `.isidentifier()`) → cero inyección.
- **`comparison_table` to_string fallback** (`notebooks/_shared.py`, `COMPARISON_SECTIONS` ×3): el recorte #353 perdió el `try: to_markdown except: to_string` del legacy → la tabla Dummy-vs-modelo fallaba con `Import tabulate failed` fuera de Colab (`tabulate` es opcional; presente en Colab, ausente en otros kernels). Restaurado en las 3 variantes. La tabla es el artefacto pedagógico núcleo y no debe fallar en silencio.
- **Strip `num__`/`cat__`** (`clasificacion/notebook.py`, celdas `pipeline_lr` `or_df` + `pipeline_rf` `perm_df`): `re.sub(r"^(?:[a-z][a-z0-9]*__)+", "", str(_n))` (= `_TRANSFORMER_PREFIX_RE` de `narrative_grounding`) sobre los nombres de feature ANTES de imprimirlos → el estudiante ve `transaction_amount`, no `num__transaction_amount`. Cierra el follow-up DEFERIDO de #459 (la fuga al display del notebook). `metrics_summary_json` lee `or_df`/`perm_df` ya limpios → `top_features` persisten stripeados → `build_computed_metrics_block` **byte-idéntico** (el strip downstream `_strip_transformer_prefix` es idempotente) → grounding M4/M5 intacto. `re` NO está en `_DENIED_IMPORT_ROOTS`/`_DENIED_CALL_NAMES` del executor (`m3_notebook_execution.py`) → el scrub AST acepta la celda; regex lineal (anti-ReDoS, anclada `^`).
- **Sin nueva clave canónica/state, sin entrada en `case_sanitization`, sin migración, sin cambio de frontend, sin tocar el architect/SHA.** `business` byte-idéntico (no genera notebook). El blast-radius cubre las 3 variantes (mejora estricta; en `lr_only` se elimina `pipeline_rf` y viceversa, así cada strip vive solo donde su celda existe). Casos ya generados NO se reprocesan. Tests: `backend/tests/test_m3_lr_notebook_production_quality.py` (harness de ejecución espejo de `test_issue230`, RED-control 15/18 sin los fixes). **PENDIENTE verificación LIVE** (merge ≠ deploy): regen real ml_ds+LR.

## M3 ml_ds+Random Forest notebook — coherencia y calidad para producción

- Scope: `studentProfile == "ml_ds"` + family `clasificacion`, variante `rf_only` (Random Forest como algoritmo ÚNICO; el contraste `lr_rf_contrast` comparte el fix de la nota). Hermano de «M3 ml_ds+LR notebook» (arriba), que ya cubrió los 3 fixes COMPARTIDOS (EDA contract-first, `to_string` fallback, strip `num__`/`cat__` — aplican también a `rf_only`). DOS fixes RF-específicos a celdas DETERMINISTAS (el LLM las copia verbatim), **prompt-only, sin kill-switch** (revert = `git`; degradan por el `try/except` existente). Validados ejecutando la cadena `rf_only` real (dummy → pipeline_rf → cv → comparison → confusion → cost → metrics_summary_json) contra dominios sintéticos NO-cliente (fraude/crédito/ambiental) + skip de target degenerado.
- **Celda concepto §3.0.5 DOMAIN-GENERAL** (`notebooks/_shared.py`, `INTRO_BY_VARIANT["rf_only"]`): el intro decía «Cada árbol mira **al cliente** paso a paso» — supuesto cliente-céntrico FALSO para un caso de fraude/ambiental/manufactura (el notebook de-churneado #382/#346 debe leerse coherente para CUALQUIER caso). Ahora «Cada árbol mira **cada caso** paso a paso» (neutro; el intro hermano `lr_only` ya usaba «cada dato», y el de contraste ya era genérico). Conserva la frase pineada `toma una decisión` (`_SHARED_PHRASES`); el veto `test_rf_only_descriptions_never_name_logistic_regression` queda verde (no introduce nombres de LR).
- **Nota `comparison_table` RF auto-contenida** (`notebooks/_shared.py`, `COMPARISON_SECTIONS["rf_only"]` + el `note` de la fila RF del contraste): la columna `interpretability_note` de la fila RandomForest decía «media — **revisar permutation importance**» → apuntaba a un análisis que #353 ELIMINÓ del núcleo (la Regla M veta su emisión) → instrucción colgante en un notebook auto-contenido. Ahora «media — `feature_importances_` del bosque (importancia por impureza)» — nombra lo que la celda `pipeline_rf` SÍ computa. La nota es **student-facing** (se imprime en la tabla `to_markdown`/`to_string`); es el espejo RF de la nota LR auto-contenida «alta — coeficientes interpretables como log-odds».
- **Sin nueva clave canónica/state, sin `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA.** Cero cambio a las métricas emitidas → `build_computed_metrics_block`/grounding M4/M5 y el executor #239 byte-idénticos. `lr_only`/`business`/otras familias byte-idénticas. DIFERIDO (defendible, NO en este cambio): el «un modelo lineal pasaría por alto» del markdown compartido `pipeline_rf` (pedagogía genérica de RF, no nombra LR) y la agregación de niveles one-hot (`region_norte`…) en `top_features` (compartida con LR, edge-case — los drivers numéricos dominan en casos reales —, con implicaciones de grounding byte-idéntico). Casos ya generados NO se reprocesan. Tests: `backend/tests/test_m3_rf_notebook_production_quality.py` (locks de los 2 fixes + cadena `rf_only` E2E en 3 dominios + skip degenerado). **Verificación LIVE PASS** (Gemini real → escala a Pro por #239 + executor de producción): el LLM reproduce ambos fixes VERBATIM (`cada caso` / `feature_importances_ del bosque`, sin `al cliente` / `revisar permutation importance` / fugas de LR), 0 violaciones del validador, notebook ejecuta limpio (`auc_rf≈0.74`, `quality_warning=None`, target contract-first, nombres sin prefijo `num__/cat__`). Confirmación final en deploy (merge ≠ deploy).

## M1 Exhibit Coherence (Issue #360)

- Scope: `studentProfile == "ml_ds"` AND family `clasificacion` ONLY, gated by `_is_ml_ds_classification(state)` inside `case_writer` and `case_questions` (`backend/src/case_generator/graph.py`). For `business` (including `business + clasificacion`, which still receives the classification prompt) and the `regresion`/`clustering`/`serie_temporal` families the path is a byte-identical NO-OP (zero reprompts, zero new state). This is the OPPOSITE of the #243 model-metric grounder, which deliberately lets business numbers pass; here we anchor business numbers cited `(Exhibit N)` to that anexo's table.
- A deterministic, pure validator (`backend/src/case_generator/m1_grounding.py`) checks that the business figures cited in the M1 narrative/questions against an Exhibit actually appear in that anexo's RAW markdown table. It reads `state["doc1_anexo_financiero"|"_operativo"|"_stakeholders"]` directly — NOT the `sanitize_untrusted_payload(..., per_field_limit=2000, total_limit=8000)` prompt copy, whose truncation could drop table rows → false negative. Marker→anexo: `(Exhibit 1)`→financiero, `(Exhibit 2)`→operativo, `(Exhibit 3)`→stakeholders; an anexo with no numeric table (e.g. Exhibit 3) demands no anchoring.
- **EXACT match on the normalized magnitude** (epsilon `1e-9`, float-repr only) — NEVER `narrative_grounding._within_tolerance` (its ±2pp/±2% is for model metrics and would mask `25 vs 26`). `_extract_anchor_numbers` is also unusable here (it needs `':' in line` → zero anchors from a table). The module reuses only the low-level primitives `_split_markdown_table_row` / `_is_markdown_separator_row` / `_is_thousands_formatted`, with its OWN thousands/decimal/suffix number grammar (`_NUMBER_RE` cannot tokenize `4,500,000`).
- **High-precision detection:** only figure-shaped numbers are validated (currency `$ € USD…`, `%`, magnitude suffix `K/M/MM/mil/millones`, or thousands-grouping). Bare structural integers (`3 años`, `2 stakeholders`) are ignored → near-zero false positives → near-zero spurious degradation. Scale-robust EXACT match: a suffixed figure expands to a candidate set (`$1.2M → {1.2, 1_200_000}`) so it matches a cell written `$1,200,000` or bare `1.2`. Narrative attribution is marker-scoped (a number binds only to an `(Exhibit N)` marker in its preceding clause; the `.`/`,` inside a number are not clause boundaries; unbound numbers are never flagged). Questions use the structured `exhibit_ref` field as primary source (`Ninguno`/None/unexpected → skip), scanning `enunciado` + `solucion_esperada`. Violation prefix: `EXHIBIT_MISMATCH:`.
- **Reprompt-once-then-DEGRADE (NOT hard-fail, unlike M4/M5):** on violations the node reprompts once by string CONCATENATION (`# CORRECCIÓN OBLIGATORIA DE COHERENCIA CON EXHIBITS` + the raw violation bullet list — never re-`.format()`, since cifras/markdown may carry `{}`). If violations persist, the best output is kept, a structured `logger.warning(extra={node, violations, case_id})` fires, and the job COMPLETES. The questions reprompt re-invokes structured output and may raise `ValidationError`/`OutputParserException`/`ValueError` → degrade to the pass-1 questions (the helper runs OUTSIDE the node's existing `except RuntimeError: raise`, so it never propagates `RuntimeError`).
- **Best-effort:** the validator is wrapped so any internal exception degrades and continues (mirrors `CostCallbackHandler` / partial-preview) — it never fails a job by its own bug. **No new canonical/teacher-facing key, no `case_sanitization` allowlist entry, no new state field** (logger-only; this also avoids the LangGraph fan-out merge hazard, since `case_writer`/`case_questions` run in parallel). Tests: `backend/tests/test_issue360_m1_grounding.py`.

## M1 P3 Cost Grounding (Issue #361)

- Scope: `studentProfile == "ml_ds"` AND family `clasificacion`, in `case_questions` only (same surface as #360). The M1 discussion question **P3** trade-off (acting unnecessarily vs. omission) now cites the **shared `business_cost_matrix`** instead of fabricating a per-event figure "según Exhibit 1" — Exhibit 1 is an annual aggregate with no per-event cost, so the old anchor was self-contradictory (demanded `[cifra]` "no inventada" from a source that lacks it) and diverged from the matrix M3's cost cell consumes.
- **Single source, business-language presentation.** `build_cost_matrix_block(business_cost_matrix)` (`prompts/clasificacion/M1_clasificacion/cost_block.py`) renders the normalized `state["dataset_schema_required"]["business_cost_matrix"]` dict — the SAME source M3 reads — into a curated, **placeholder-free** block: `fp_cost` → *costo de acción innecesaria*, `fn_cost` → *costo de omisión*, plus `currency`. It NEVER emits the raw DS keys (`fp_cost`/`fn_cost`) nor DS jargon ("falso positivo/negativo", "AUC", "threshold"). Guarantee is "same source", NOT byte-identical to M3 (M3's LLM copies the contract JSON; it does not emit deterministic literals). Do NOT reuse `_format_dataset_contract_block` / dump the contract JSON into P3 — that leaks DS keys to the student.
- **Gate is `business_cost_matrix is None`, not profile.** `case_questions` injects `cost_matrix_block` into context **unconditionally** (always present → the classification prompt's `{cost_matrix_block}` never `KeyError`s; non-clf prompts ignore the unused key). The builder decides quantitative vs. qualitative: `None`/absent/malformed → an explicit **qualitative** trade-off (whom to prioritize under uncertainty), no figure demanded, never fabricated. `None` covers business (never emits a matrix) AND ml_ds+clf with an absent/invalid matrix the validator nulified (`graph.py` `_validate_business_cost_matrix`). Extraction: `_extract_business_cost_matrix(state)`.
- **#360 coordination (load-bearing).** P3's cost figures are case PREMISES, not Exhibit cells, so the anchor pins **`exhibit_ref="Ninguno"`**. That keeps the #360 questions validator (`validate_questions_exhibit_coherence`, same ml_ds+clf gate) from flagging the cost figure against an Exhibit table it can never appear in (would otherwise reprompt-then-degrade every grounded P3). P3 may still reference an Exhibit qualitatively, but never as the source of the FP/FN figures.
- **No new canonical/teacher/student-facing key.** `business_cost_matrix` stays out of `case_sanitization` — it only feeds the prompt (curated). Tests: `backend/tests/test_issue361_p3_cost_grounding.py` (assertions on the assembled prompt + the #360 coupling with a negative control). Out of scope (documented follow-ups, not #361): the architect anchor deriving `fp/fn` "TRAZABLE al caso" against an aggregate Exhibit (`architect.py`; touching it trips `_MLDS_ARCHITECT_PROMPT_SHA256`), and `currency`↔Exhibits coherence (architect soft instruction, no enforcement → now superseded by the USD-only decision below).

## M1 Narrativa concisa (900–1.200 palabras)

- Scope: AMBOS perfiles, TODAS las familias (la base `prompts/_writer_base.py` propaga a las variantes de clasificación y clustering). La narrativa M1 (`case_writer` → `doc1_narrativa`) pasó de un ensayo de 3.000–3.500 palabras a una narrativa CONCISA de **900–1.200 palabras** con presupuestos por sección ALINEADOS (suman 800–1.130): menos carga de lectura para docente/estudiante y ~60% menos tokens de salida del nodo. **Root cause del inflado viejo:** la misión pedía 3.000–3.500 mientras los objetivos por sección sumaban ~1.350–1.750 — el LLM resolvía la contradicción inflando; además una narrativa de 3.000+ palabras (~19k chars) llegaba TRUNCADA al downstream (`contexto_m1 = doc1_narrativa[:8000]`), cortando las Opciones A/B/C y el Dilema Final que M4/M5 leen. Con el recorte la narrativa completa cabe en esa ventana → bonus de coherencia downstream.
- **Prompt-only, SIN kill-switch ni validador de longitud** (la longitud es ESTILO, no coherencia; un reprompt-por-longitud quemaría los tokens que se ahorran — precedente del memo M5 100-160 palabras; revert = git). La instrucción de inflado («Mínimo 12 párrafos… amplía las secciones») se reemplazó por auto-verificación de concisión («si superas ~1200, RECORTA», sin tocar cifras de Exhibits ni opciones) + regla pedagógica (párrafos de máximo 3-4 oraciones, una idea por párrafo, cero relleno retórico, cero repetición). NO re-expandir sin decisión de producto.
- **Estructura load-bearing INTACTA:** los 7 headers H3 exactos (los anchors por familia los referencian por nombre), los mínimos de citación de Exhibits (3 cifras / 2 métricas / 2 actores — grounding #360), el bloque «3 opciones (A, B, C)» con señal a 90 días (#412/#467), el split «sabe vs no sabe» (#362) y el set de placeholders (sin claves nuevas). `M1_NARRATIVE_WORDS` (teaching note M6, `module_guide_block.py`) actualizado a `"900–1.200"` para que la guía del docente describa M1 verazmente.
- Sin cambio de frontend/schema/`case_sanitization`/migración; el architect (SHA-locked) NO se toca — `company_profile`/`dilema_brief` son inputs del writer, no superficie estudiante. Casos ya generados NO se reprocesan. Drift-locks: `backend/tests/test_m1_narrative_concision.py` (techo de concisión + coherencia aritmética misión↔secciones — la raíz del bug no puede volver en silencio — + estructura + constante M6 == rango del prompt).

## M2 Narrativa concisa (450–600 palabras)

- Scope: AMBOS perfiles, TODAS las familias — las TRES superficies de narrativa M2 (`eda_text_analyst` → `doc2_eda`): el genérico `EDA_TEXT_ANALYST_PROMPT` (`prompts/__init__.py`; business + regresion/serie_temporal + business+clustering), el de clasificación `EDA_TEXT_ANALYST_PROMPT_CLASSIFICATION` (`M2_clasificacion/eda_text.py`, ambos perfiles vía bloques inyectados) y el de clustering ml_ds `EDA_TEXT_ANALYST_PROMPT_CLUSTERING` (`M2_clustering/eda_text.py`, #456). El reporte EDA pasó de un nominal 700–900 a **450–600 palabras** con presupuestos por sección ALINEADOS (§1 120–160, §2 230–300, §3 90–130; suman 440–590). **Root cause del inflado (inverso al de M1):** el presupuesto declarado era razonable pero las EXIGENCIAS de contenido lo desbordaban (intro Sherlock + tabla analógica genérica repetida en cada caso, diccionario «mín 8 variables» que además FORZABA columnas inventadas en datasets de-templateados post-#507, 3-4 subsecciones de predictores, 4-5 features derivadas + umbral AUC duplicado del bloque de distribución) — el LLM obedecía el contenido, no el presupuesto. Se recortó el CONTENIDO para que quepa en el presupuesto: metáfora detective en 2-3 frases SIN tabla analógica, diccionario con TODAS las variables reales (anti-invención, espejo del fix F1 de clustering), 2-3 predictores, 3 features derivadas (3-4 señales en business), fuera el «Umbral pedagógico AUC-ROC ≥ 0.70» del bloque FE (duplicaba el guidance de threshold del bloque de distribución; el token `AUC-ROC` sobrevive en el class-balance). Bonus: `contexto_m2` alimenta M3-content/M3-questions/EDA-questions SIN truncar → el recorte también ahorra tokens de ENTRADA downstream.
- **Prompt-only, SIN kill-switch ni validador de longitud** (longitud = ESTILO, precedente M1/PR#566 y memo M5/PR#420; revert = git). Añadidos: auto-verificación de concisión («si superas ~600, RECORTA», NUNCA números extraídos del dataset / balance de clases / tablas obligatorias / bullet de brechas) + regla «Concisión pedagógica» (párrafos máx 3-4 oraciones, una idea por párrafo; «la densidad la ponen el dataset y los gráficos; la narrativa los conecta, no los parafrasea»). El DATASET y los GRÁFICOS no se tocan: cero cambios en `eda_chart_generator`/builders/schema/generador — el recorte es SOLO de la prosa. NO re-expandir sin decisión de producto.
- **Estructura load-bearing INTACTA:** los 3 headers H2 exactos por prompt, el diccionario de datos (tabla), la regla de surfacing **"Brechas de datos detectadas"** (M3 depende de ella), la regla de números EXACTOS + CERO ALUCINACIÓN (grounding del dataset), los tokens de rigor ml_ds del contrato de dispatch (`imbalance_ratio` con `max(count_cat0, count_cat1)`, `Matriz de Confusión`, `F1-Score`, `Accuracy Paradox`, `AUC-ROC`, `Leakage Guard`), los contratos de placeholders (19 clf / 14 clustering / genérico sin cambios) y los guards data-only/target-free de clustering. De paso se corrigió la referencia stale «sección 4» del workflow genérico (→ «sección 2») y se neutralizó «encoding de categóricas para LR/RF» (→ «para el algoritmo seleccionado», coherente con el M2 model-neutral de #536). `M2_EDA_WORDS` (teaching note M6, `module_guide_block.py`) actualizado a `"450–600"`.
- Sin cambio de frontend/schema/`case_sanitization`/migración; el architect (SHA-locked) NO se toca. Casos ya generados NO se reprocesan. Drift-locks: `backend/tests/test_m2_narrative_concision.py` (techo de concisión + coherencia aritmética misión↔secciones en las 4 superficies renderizadas + tabla analógica y «mín 8» no pueden volver + estructura/tokens + constante M6 == rango del prompt). **PENDIENTE verificación LIVE** (merge ≠ deploy): regen 1 caso ml_ds+clf y 1 business → M2 en 450–600 palabras, diccionario con columnas reales, charts intactos.

## M3 Narrativa concisa (ml_ds 350–650 · business 400–550 palabras)

- Scope: AMBOS perfiles, TODAS las familias — SOLO la narrativa M3-content (`m3_content_generator` → `m3_content`); el NOTEBOOK M3 y sus prompts quedan **INTACTOS** (la pedagogía técnica por algoritmo vive ahí — decisión de producto). Superficies: la base ml_ds `M3_EXPERIMENT_PROMPT` (`prompts/_shared.py`; regresion/serie_temporal la usan directa; las variantes de clasificación y el bloque de clustering la heredan por CONCATENACIÓN → un solo edit propaga), los bloques por variante de `M3_clasificacion/content.py`, el bloque de segmentación de `M3_clustering/content.py`, y el business `M3_AUDIT_PROMPT` (`prompts/__init__.py`). ml_ds pasó de 800–1.100 nominal (9 elementos casi todos SIN tope + secciones de familia SIN tope → en la práctica 1.100–1.400) a **350–650 palabras con UN algoritmo** (hasta 1.000 con dos), con tope explícito por elemento — los **9 elementos se CONSERVAN** (las preguntas M3 anclan en hipótesis/sesgo/descarte, y `_extract_main_risk` del memo M5 lee el riesgo vía keyword/headers que sobreviven) — y topes por sección de familia que cuentan DENTRO del total (clasificación ≤80 c/u; clustering ≤55 c/u, bloque sigue brace-free). business pasó de 650–850 a **400–550** con presupuestos por sección realineados (90+150+70+70+80 = 460).
- **Prompt-only, SIN kill-switch ni validador de longitud** (longitud = ESTILO, no coherencia; precedente M1 PR#566 / M2 PR#567 / memo M5 PR#420; revert = git). Añadidos: auto-verificación de concisión («si superas el máximo, RECORTA» — NUNCA la hipótesis experimental, la métrica de éxito, los criterios de validación/descarte ni las columnas reales citadas del EDA M2; en business NUNCA los supuestos 3.2 ni el veredicto 3.5) + regla «Concisión pedagógica» (párrafos máx 3 oraciones, una idea por párrafo; «la profundidad técnica la aporta el notebook; esta narrativa DISEÑA el análisis, no lo parafrasea»). NO re-expandir sin decisión de producto.
- **De paso (coherencia con el catálogo reducido):** la lista de visualizaciones del elemento 8 PODA las familias retiradas (NLP/Grafos/Recomendación/Anomalías — degradan a clasificación desde #233; puro peso muerto de prompt) dejando Clasificación/Regresión, Clustering y Serie temporal; la política de priorización pasa de «más de 4» a «más de 2» algoritmos (el intake emite máx 2 picks); y la sección estudiante-facing «Cómo leer la matriz de costos» de los 3 bloques clf de-jergoniza las claves crudas `fp_cost`/`fn_cost` → «costo de negocio de una falsa alarma (falso positivo) / de una omisión (falso negativo)» y threshold → umbral de decisión (doctrina #536).
- **Estructura load-bearing INTACTA:** los H2/H3 exactos de la base, los títulos de sección de familia («Por qué LR/RF para esta decisión», «Por qué LR baseline»/«RF challenger», «Cómo leer la matriz de costos», las 4 secciones + markers del bloque clustering #457), los GUARDRAILS anti-alucinación con la cita de columnas reales entre comillas inversas, la línea de matemática en TEXTO PLANO (#480), los contratos de placeholders (base = 5 claves; clf conserva `{pregunta_eje}` + `{{computed_metrics_block}}`; clustering brace-free), `{lr_business_block}` y la escala semáforo 🟢🟡🔴 del audit. `M3_AUDIT_WORDS`/`M3_EXPERIMENT_WORDS` (teaching note M6, `module_guide_block.py`) actualizadas a `"400–550"`/`"350–650"`. Bonus downstream: `m3_content` fluye SIN truncar a M4/M5 (`contexto_m3`) y al prompt del notebook generator → el recorte ahorra también tokens de ENTRADA.
- Sin cambio de frontend/schema/`case_sanitization`/migración; el architect (SHA-locked) NO se toca; el notebook M3 (generador/executor/validadores/celdas) byte-idéntico. Casos ya generados NO se reprocesan. Drift-locks: `backend/tests/test_m3_narrative_concision.py` (techo de concisión + coherencia aritmética misión↔topes en las 6 superficies — la raíz del inflado no puede volver en silencio — + estructura + poda de familias retiradas + de-jerga `fp_cost` + constantes M6 == rangos de los prompts). **PENDIENTE verificación LIVE** (merge ≠ deploy): regen 1 caso ml_ds+clf y 1 business → M3 en rango, notebook intacto y coherente con su algoritmo.

## M2 EDA Question Coherence

- Scope: classification family for BOTH profiles (`ml_ds + clasificacion` AND `business + clasificacion`), gated by `_is_classification_family(state)` inside `eda_questions_generator` (`graph.py`). Non-classification families and the kill-switch-off path are a byte-identical NO-OP. M2 sibling of the M1 option-coherence guard (#412): M2 questions are open Socratic (`EDASocraticQuestion`, no A/B/C options), so the M1 defect can't recur identically; the M2 analog is a `solucion_esperada` citing a **chart that does not exist** or an **event rate that contradicts its own `enunciado` / the real prevalence**.
- A deterministic, pure validator (`backend/src/case_generator/m2_grounding.py`, `validate_eda_questions_coherence`) runs four checks per question: **CHART_REF_NONEXISTENT** (non-sentinel `chart_ref` not in the chart manifest ids); **EVENT_RATE_INCOHERENT** (`solucion_esperada` rate vs its own `enunciado`, > ±0.5pp); **EVENT_RATE_VS_CONTRACT** (ml_ds only: rate vs `dataset_schema_required.target_event_rate*100`, the #372 anchor; `None`/bool/out-of-range → skipped, covering business); **MODELO_NO_SELECCIONADO** (ml_ds single-model `lr_only`/`rf_only` only — reuses `narrative_grounding.detect_unselected_model_mentions` #337 over `titulo`+`enunciado`+`solucion_esperada`; `lr_only` must not name Random Forest, `rf_only` must not name Logistic Regression; caller passes `variant`, `None`/`lr_rf_contrast` → no-op for business and contrast; bare `RF`/`LR` acronyms NOT matched → zero-FP). Tolerance ±0.5pp (mirrors `m1_grounding._RATE_PCT_TOLERANCE`). Event-rate extraction (`extract_first_event_rate`) is keyword-anchored + `%`-required + metric-suffix-excluded, so accuracy/recall/threshold/lift are never mistaken for a rate; bounded quantifiers (no ReDoS).
- **Wiring** (`_apply_eda_questions_coherence`): reprompt-once-then-DEGRADE, best-effort, behind the `m2_question_coherence` kill-switch. The corrected set is accepted ONLY if it preserves the **question count AND `numero` sequence** (the `M2-Q{numero}` answer/grading key — `EDAQuestionsOutput` does NOT enforce `len==2`) AND is coherent; otherwise degrade to pass-1. The OUTER `except Exception` encloses the reprompt `.invoke` so a reprompt `RuntimeError` degrades instead of escaping to the node's `except RuntimeError: raise` (which would fail the job). `chart_ids` uses the SAME `c.get("id", f"chart_{i}")` fallback as the manifest. Structured `logger` events (fired/fixed/degraded) with enumerated `violation_types` (no raw text/PII).
- **Prompt hardening:** `eda_questions.py` genericizes the leak-prone hardcoded example numbers of the example `solucion_esperada` to symbolic placeholders + one boundary bullet (no new placeholder; 7-placeholder contract + no-churn + pedagogy guards stay green; no SHA256 on M2 prompts). **No new canonical/teacher/student-facing key, no `case_sanitization` entry, no new state field** (logger-only; M2 is a strict sequential chain, not a fan-out). Kill-switch `M2_QUESTION_COHERENCE` (Settings `m2_question_coherence`, default `true`; `backend/.env.example`). Golden oracle `eda_questions_coherence_ok` in `tests/golden_eval.py`. Tests: `backend/tests/test_m2_question_coherence.py`.
- **M2↔M3 model coherence (de-specify + variant hint, root-cause for MODELO_NO_SELECCIONADO):** the M2 EDA questions prompt was the LONE question surface that hardcoded `LR/RF` / `max_features en RF`, so an `lr_only` case named a model it never builds. The base `EDA_QUESTIONS_GENERATOR_PROMPT_CLASSIFICATION` is now **model-neutral** (one-way prompt fix, no switch; business byte-identical), and `eda_questions_generator` resolves the variant ONLY for ml_ds+clf (mirror `m3_questions_generator`) and concatenates a brace-free `_build_eda_model_focus_hint(variant)` re-anchoring the SELECTED model (lr_only→Regresión Logística, rf_only→Random Forest, `None`/contrast→`""`). Prompt = defense, the `MODELO_NO_SELECCIONADO` check = guarantee. Drift-locks `TestPromptModelNeutral`/`TestModelFocusHint`/`TestNodeHintWiring`; golden `check_eda_questions_coherence(+variant)`.
- **M3 conceptual LR question quality (prompt-only, one-way):** the `lr_only` M3 conceptual questions (`M3_clasificacion/questions.py`) cited **VIF** (P1/P3) — removed from the executed notebook by #353, so the student never sees it; re-anchored to the predictor correlations the M2 EDA actually produces. Raw `fp_cost ≠ fn_cost` keys leaked into the student-facing `enunciado` (P2 of `lr_only`/`lr_rf_contrast`) → now readable language (falso positivo/negativo), threshold hint asks for the DIRECTION (number computed in the notebook, #361 doctrine). No switch. Drift-locks in `test_m3_clasificacion_structure.py`.

## M4 Question Coherence — single-model leak + metric parity

- Scope: M4 (Impacto) questions. Option/MCQ checks for ALL families (unchanged); the TWO NEW checks gated to ml_ds + `clasificacion` single-model (`lr_only`/`rf_only`). Closes the last asymmetry: M4 questions were the ONLY classification question surface missing the single-model leak guard (`detect_unselected_model_mentions`, #337) and the model-metric anchoring guard (`detect_unanchored_adjacent_metrics`, #243) that M2/M3/M5 questions + M4 content/charts already carry — so an ml_ds+LR deep dive (`lr_only`) could name Random Forest or fabricate an AUC with no deterministic guarantee. M4 sibling of the M5 memo guard.
- Pure validator `validate_m4_questions_coherence(preguntas, *, variant, metrics_block)` (`m4_grounding.py`) aggregates FOUR checks (zero NEW regex, all reuse hardened primitives): **OPTION_NONEXISTENT/OPTION_NOT_PRESENTED** (ALL families, `m1_grounding.validate_question_option_coherence` with FLOOR `""`; BOTH #416 rules kept, unlike M5) + **MCQ_OPCIONES_EMBEBIDAS** (ALL families, #481) + **MODELO_NO_SELECCIONADO** (ml_ds single-model, over `titulo+enunciado+solucion_esperada`; no-op via `variant`) + **METRICA_NO_ANCLADA** (ml_ds executed M3, gated on `has_metric_anchors`; scans BOTH `enunciado` AND `solucion` — M4 cites metrics in the enunciado, unlike M5). Replaces the old inline `_m4_question_violations` + `_M4_QUESTIONS_OPTION_REPROMPT_HEADER`.
- Wiring (`_apply_m4_questions_coherence` in `graph.py`, renamed from `_apply_m4_questions_option_coherence`): reprompt-once-then-DEGRADE, best-effort, SAME `m4_question_coherence` kill-switch (no new switch). Identity-guarded on count + `numero` (the `M4-Q{numero}` key); a reprompt `RuntimeError` degrades (never fails the job). `m4_questions_generator` resolves the RESOLVED variant ONLY for ml_ds+clf (mirror `m5_questions_generator`), NEVER `algorithm_mode`, and passes the `computed_metrics_block` it already built. Prompt defense-in-depth: a single-model boundary in `M4_QUESTIONS_PROMPT_CLASSIFICATION` + NEUTRAL twin (reuses existing placeholders; `opción↔solución` sentinel kept; business/generic UNCHANGED). No new canonical/state key, no `case_sanitization`, logger-only. Golden oracle `check_m4_questions_model_coherence` → `NodeEvalInputs.m4_questions_model_coherence_ok` → `evaluate_downgrade_gate`. Tests: `backend/tests/test_m4_question_coherence.py`.

## M5 Memorándum Coherence

- Scope: classification family for BOTH profiles, gated by `_is_classification_family(state)` inside `m5_questions_generator` (`graph.py`); non-classification + kill-switch-off = byte-identical NO-OP. Sibling of #412/#414/#415. **The M5 NARRATIVE (`m5_content`) is already guarded (`_invoke_m5_content_with_contract`); the gap closed here is the QUESTIONS/memo** (`GeneradorPreguntasM5Output`: 1×`PreguntaM5`, `numero==1`, no `opciones`/no `*_ref`), which had ZERO coherence validation.
- Pure validator `m5_grounding.validate_m5_questions_coherence` runs THREE gated checks over `titulo + enunciado + solucion_esperada` (zero NEW regex; all reuse hardened primitives): **MODELO_NO_SELECCIONADO** (ml_ds single-model — `narrative_grounding.detect_unselected_model_mentions`, #337; no-op for contrast and business `variant=None`); **METRICA_NO_ANCLADA** (ml_ds w/ executed metrics, gated on `has_metric_anchors` — the NEW ADJACENCY-ONLY `narrative_grounding.detect_unanchored_adjacent_metrics`); **OPTION_NONEXISTENT** (BOTH profiles — `m1_grounding.validate_question_option_coherence`, #412, PRIMARY rule only — the real coherence for `business + clf`).
- ADJACENCY-ONLY metric guard because the M5 prompt MANDATES co-locating business figures and model metrics in the same paragraphs → the clause-level generic pass would FP on business numbers; the adjacency pass fires only on `AUC 0.78`/`recall=0.71` and drops the `CITA:` arm (the prompt permits `Porter (1985)`). Documented limitations (near-zero-FP, degrade-safe): FNs — keyword-detached metric value, bare-acronym leak, Check C not scanning the ≤8-word `titulo`, and the contrast narrative-winner↔memo-winner (the M5 matrix reveals no single winner → no deterministic anchor); residual FP — a business quantity with a model-metric keyword in direct adjacency and no connector (`"exactitud 92% operativa"`), avoided by the natural `del`/`de` phrasing, only reprompt-then-degrade.
- **Wiring** (`_apply_m5_questions_coherence`): reprompt-once-then-DEGRADE, best-effort, behind `m5_question_coherence`. Corrected memo accepted ONLY if it preserves count + `numero` (`M5-Q{numero}` key) AND is coherent; else degrade to pass-1. OUTER `except Exception` encloses the reprompt `.invoke` so a reprompt `RuntimeError` degrades instead of escaping to the node's `except RuntimeError: raise`. Node runs SEQUENTIAL post-`synthesis_phase1_sync` (no fan-out hazard). Caller passes the RESOLVED notebook variant, NEVER `algorithm_mode`. Structured `logger` (fired/fixed/degraded) + `degraded` boolean. Defense-in-depth: one boundary line in `M5_QUESTIONS_PROMPT_CLASSIFICATION` (ml_ds clf, no new placeholder). **No new canonical/teacher/student-facing key, no `case_sanitization` entry, no new state field.** Kill-switch `M5_QUESTION_COHERENCE` (Settings `m5_question_coherence`, default `true`; `backend/.env.example`). Golden oracle `check_m5_questions_coherence` + `NodeEvalInputs.m5_questions_coherence_ok` in `tests/golden_eval.py`. Tests: `backend/tests/test_m5_question_coherence.py`.
- **Longitud de la `solucion_esperada` (concisión por diseño):** el memorándum M5 es CONCISO — **100-160 palabras, 5 párrafos de UNA frase** (Decisión/Evidencia/Riesgo/Implementación/Marco), para acelerar la calificación docente y ahorrar tokens (M5 questions corre en Pro). **Prompt-only** en `M5_QUESTIONS_PROMPT_CLASSIFICATION` y `M5_QUESTIONS_GENERATOR_PROMPT` (genérico → cubre `business+clf` vía `M5_QUESTIONS_LR_BUSINESS_BLOCK`), **SIN validador de longitud** (estilo, no coherencia; acortar es riesgo no-creciente para los 3 checks). El cerco conserva `EXACTAMENTE 1 consigna`/`memorándum ejecutivo`/`decisión final` (`test_issue242_pedagogical_coherence.py`), la regla anti-bullets y el genérico jargon-free. NO re-expandir a 350-500. Candados: `test_m5_clasificacion_dispatch.py`.

## M1→M5 Verdict Coherence (clasificación)

- Scope: `ml_ds + clasificacion` (M1 es algorithm-agnostic → cubre lr_only/rf_only/contrast), gateado por `_is_ml_ds_classification(state)` + kill-switch. Análogo de clasificación del #467 de clustering ("M1→A/B mientras M4/M5→C"): la P3 de M1 recomienda una opción A/B/C (mandada por el prompt M1; validez por #412) y el memo M5 nombra INDEPENDIENTEMENTE su opción A/B/C — sin ancla podían diverger y el caso se contradecía. **M4 FUERA de alcance** (su §4.5 es veredicto de DESPLIEGUE del modelo "Desplegar/No desplegar", no una opción A/B/C → nada que anclar; forzarla pelearía con el dedup #423). `m5_content` fuera (matriz de acciones, sin único ganador). Único par con opción A/B/C independiente: **M1 P3 ↔ memo M5**.
- **"M1 manda" (propagación), la opción NO se randomiza.** Módulo PURO `case_generator/classification_verdict.py` (nunca lanza): `resolve_m1_recommended_option(preguntas)` extrae la letra de M1 P3 PRECISION-FIRST (ancla SOLO con UNA opción inequívoca; dos-bound/ninguna → `""` → guard no-op → byte-idéntico, NUNCA un ancla equivocada). Negación-aware (`_NEG_BEFORE_RE`): "No se recomienda la Opción A" marca A RECHAZADA y nunca la ancla (atrapa el asimétrico "No se recomienda A … B es superior"→`""`); el límite `([A-F])(?![\w/-])` espeja `m1_grounding._OPTION_REF_RE` (no ata "opción A/B testing"/"C-suite"). `build_m1_decision_phrasing_hint()` (brace-free) pide que P3 enuncie su opción parseable («Se recomienda la Opción X») SIN dictar CUÁL; `build_m5_classification_verdict_hint(option)` (brace-free) pide que el memo honre la opción de M1.
- **GARANTÍA:** `_apply_classification_m5_verdict_coherence` (en `m5_questions_generator`, DESPUÉS de `_apply_m5_questions_coherence` #417 y del guard de clustering) reusa `clustering_decision.validate_verdict_option` sobre `titulo`+`solucion_esperada` del memo → reprompt-once-then-DEGRADE, best-effort. Acepta SOLO si conserva conteo + `numero` (clave `M5-Q{numero}`) Y pasa el veredicto Y NO reintroduce ninguna violación #417/#337/#243/#412 (`validate_m5_questions_coherence` re-corrido ⊆ pass-1 → nunca re-filtra modelo/métrica); si no, degrada a pass-1. `except Exception` EXTERNO envuelve el `.invoke` → un `RuntimeError` degrada (nunca falla el job). **STANDALONE** — no toca los guards de clustering (combinado veredicto+silhouette #467+#469) → cero blast-radius en clustering. Gate `_is_ml_ds_classification(default_unresolved_ml_ds_to_classification=True)` espeja la resolución del nodo M5.
- **Kill-switch `CLASSIFICATION_VERDICT_COHERENCE`** (Settings `classification_verdict_coherence`, default `true`; `backend/.env.example`): off → hint M1 + hint M5 + guard no-op → byte-idéntico (revert env-only). `business+clf` no-op (follow-up); `contrast`/`rf_only` cubiertos (opción independiente de la variante, ortogonal al modelo → sin interacción con #337). **Sin nueva clave canónica/teacher/student-facing, sin `case_sanitization`, sin migración, sin frontend, SIN editar constantes de prompt → SHA del architect intacto, sin clave de state nueva** (lee `doc1_preguntas` durable → resume-trivial); logger-only. Golden oracle `check_classification_verdict_coherence` + `NodeEvalInputs.classification_verdict_coherence_ok` → `evaluate_downgrade_gate`. **PENDIENTE LIVE** (merge ≠ deploy). Tests: `backend/tests/test_classification_verdict_coherence.py`.

## M6 Teaching Note — Guía del Docente por módulo

- Scope: ALL cases (ambos perfiles, todas las familias). El Módulo 6 ("Solución Maestra", teacher-only, `content.teachingNote`) pasó de un ensayo de ~1.500+ palabras (dominado por un §4 "Análisis del Caso" de 1.000 palabras) a una **guía concisa de 3 secciones** (~450-550 palabras): §1 `## Resumen para el Docente`, §2 `## Recorrido por Módulo`, §3 `## Plan de Clase y Dónde se Traban`. El §4 (Factores Críticos de Éxito + Benchmarks del Sector) se ELIMINÓ.
- **Invariante de coherencia — Python posee el roster de §2.** `build_module_guide_block(...)` (`prompts/teaching_note/module_guide_block.py`, función pura placeholder-free como `build_cost_matrix_block`) ensambla "Recorrido por Módulo" en Python → conjunto/numeración/etiquetas correctos POR CONSTRUCCIÓN (módulo fantasma/omitido/mal numerado imposible; **sin reprompt-loop**). El LLM SOLO aporta sinopsis + público + 3 objetivos + una frase de "Anclaje del caso" (≤22 palabras) por módulo. Espeja `getModuleConfig` (`frontend/src/shared/case-viewer/caseViewerConfig.ts`): siempre M1; M2+M3 solo si `caseType=="harvard_with_eda"`; M4 número `4 if EDA else 2`; M5 `5 if EDA else 3`; nunca M6.
- **Wiring desde estado:** `is_business = state.get("studentProfile") == "business"` (igualdad ESTRICTA; NUNCA el `context["student_profile"]` resuelto). `case_type = state.get("caseType","harvard_only")` (señal de `route_master`). `family = context.get("primary_family")` RESUELTO (reusa el `_build_base_context` del nodo; `state.get("primary_family")` NO existe → siempre `None`). Cláusula de notebook M3 **family-AGNÓSTICA** gateada por estado realizado `bool(state.get("m3_notebook_code")) and not state.get("m3_notebook_degraded")`. Línea socrática M2 ramificada por `family=="clasificacion"`. Veredicto M4 por perfil (business Aprobar/Rechazar vs. ml_ds Desplegar/No desplegar).
- **Render (markdown-only, CERO cambios React/CSS):** §2 es LISTA DE VIÑETAS con encabezados de módulo en `**negrita**` separados por línea en blanco (el frontend usa `marked({breaks:false})` + `prose-case p{text-align:justify}`, que fusiona líneas con salto simple). Secciones en `##` (h2); **prohibido `###`/`####`** (h3 es píldora azul; h4 sin estilo). `sanitize_markdown`/`enforce_usd_currency` (#377) no-op byte-idéntico (copia sin tokens de moneda).
- **Nodos (`graph.py`):** `teaching_note_part1` (fan-out, sin M5 en estado) → §1 + §2 vía UNA llamada structured-output (`TeachingNoteIntroOutput`); el bloque §2 se construye SIEMPRE PRIMERO (fuera del `try` del LLM) → fallo del LLM degrada a esqueleto + §1 de respaldo, nunca falla el job. `teaching_note_part2` (post-sync1) → §3; elimina el split muerto `part1.split("#### 2.")` y `teaching_note_part1_synopsis`. El placeholder de fallo catastrófico lleva centinela `[TEACHING_NOTE_PART1_ERROR]` (resume recalcula). Topología INTACTA.
- **Coherencia + tests:** `m6_grounding.py` (best-effort, **logger-only**, nunca lanza) marca menciones de módulos fuera del roster en la prosa §1/§3 (palabra-frontera → sin FP por `queda`/`moneda`). Drift test liga etiquetas/numeración a `caseViewerConfig.ts`. Kill-switch `TEACHING_NOTE_MODULE_GUIDE` (Settings `teaching_note_module_guide`, default `true`; `backend/.env.example`) → OFF ejecuta `_legacy_teaching_note_part1/part2` byte-idéntico. **Sin nueva clave canónica/state, sin entrada en `case_sanitization`.** Golden `check_m6_module_coherence` + `NodeEvalInputs.m6_module_coherence_ok`. Tests: `backend/tests/test_m6_module_guide.py`.
- **Limitaciones documentadas:** (1) el centinela `[TEACHING_NOTE_PART*_ERROR]` se conserva en las claves por-parte (resume recalcula) pero `synthesis_phase2_sync` lo elimina de la nota compuesta → el docente nunca ve el token (legacy + camino feliz byte-idénticos). (2) `regenerate-notebook` re-corre solo M3, no la teaching note → cláusula de notebook §2 puede quedar desactualizada (sub-descripción menor, aceptada). (3) §2 determinista en español; producto español-only.

## M5 `main_risk` corte limpio (Issue #470)

- Scope: AMBOS perfiles, TODAS las familias. El "Riesgo principal M3/M4" que el memo M5 cita (`main_risk_from_m3_m4`) se truncaba con `text[:200]` DURO en `_extract_main_risk` (closure de `_build_base_context`, `graph.py`), dejando fragmentos crudos a media palabra/cláusula visibles al estudiante (caso real: «…(cuyo rango va de 5»). Fix: helper PURO a nivel de módulo `_truncate_risk_text` (`_MAX_RISK_CHARS=320`) que colapsa whitespace y corta en frontera de **oración** (preferida, `rfind(". "/"! "/"? ")`, floor ≥40) o de **palabra** (fallback `textwrap.shorten` + «…»). El whitespace-collapse es **load-bearing** (sin él, `".\n"` no matchea `". "` y el corte por oración falla en silencio).
- **Fix GENERAL, NO gateado.** Corre para todas las familias (genérico `prompts/_shared.py` + `M5_clasificacion/questions.py`). **El invariante byte-idéntico-clf NO aplica** (bug fix intencional general); garantía = golden-eval sin regresión + lectura del M5 clf. **Sin kill-switch** (revert objetivo = el `[:200]` roto → anti-patrón; revert = `git revert`), **sin golden oracle** (ensamblaje determinista → el unit test es la herramienta), **sin tocar el template M5** (placeholder intacto → `test_issue242`/longitud #420/5 párrafos verdes por construcción). **Sin nueva clave canónica/state, sin `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA.** Casos ya generados NO se reprocesan. Tests: `backend/tests/test_issue470_m5_risk_truncation.py`.

## business+clf Event Rate Calibration (Issue #518)

- Scope: `studentProfile == "business"` + family `clasificacion`. Cierra el residual auditado del epic #511: el dataset sintético business+clf generaba el target binario por split mediano (`int(round(values))` → prevalencia ≈ 50 %) mientras Exhibit 2 imprime una tasa de evento declarada (la Regla 4 del ancla de clasificación que business hereda, ej. "morosidad 8.3 %"), sin guard que lo detecte (#360/#372/m2-CheckC son ml_ds-only no-ops para business). Un profesor que abriera `doc7_dataset` veía ~50 % de positivos contra el 8 % anunciado. El fix **extiende la calibración top-k (antes ml_ds-only) a business**: la prevalencia del dataset IGUALA la tasa declarada en `target_event_rate` POR CONSTRUCCIÓN (idéntico a cómo ml_ds ya funciona vía #372/F1).
- **SHA-safe (Approach A, decisión de producto):** la sección `target_event_rate` del ancla compartida SHA-locked (`prompts/clasificacion/M1_clasificacion/architect.py`) dice "solo ml_ds"; en vez de editarla (regen del hash), una **regla #5 en `M1_CLASSIFICATION_BUSINESS_TARGET_BLOCK`** (business-only, NO hasheado, se apenda solo para business+clf en `_assemble_architect_prompt`) la ANULA explícitamente para business, ordenando emitir `target_event_rate` = la cifra de Exhibit 2. **`_MLDS_ARCHITECT_PROMPT_SHA256` queda intacto** (mismo patrón de override que el bloque ya usa para forzar `classification_target` sobre la regla base, #301).
- **Cadena (toda determinista salvo la emisión del LLM):** `_validate_target_event_rate` (`graph.py`) gana un kwarg `business_calibration_enabled` (= `settings.business_event_rate_calibration`, ÚNICO punto que gatea el switch) → con on, business+clf entra al gate y conserva la tasa (antes la nulificaba). El caller (`case_architect`) corre DESPUÉS de `_normalize_business_classification_target` → el target ya es `classification_target`/`int`, el gate pasa. La tasa sobrevive `schema_designer` (`_contract_with_enforced_target` copia el dict completo). El brazo de calibración top-k (`_generate_dataset_from_schema`) se amplía a `profile in ("business","ml_ds")` y el helper `_is_mlds_event_target` se renombra a `_is_event_target` (profile-agnóstico); NO se gatea con el switch (auto-gatea por el null-guard `isinstance(target_event_rate, ...)` → con el switch off la tasa llega `None` y cae al `else` byte-idéntico). El guard #372 (`_invoke_m1_exhibit2_coherence`) se amplía a business+clf cuando hay tasa + switch on (reusa `validate_exhibit2_event_rate`); el "Check C" de m2 (`validate_eda_questions_coherence`) se auto-extiende a business sin cambio de código (self-gatea por `rate_pct is not None`).
- **Failure mode grácil:** si el LLM omite la tasa para business, `_validate_target_event_rate` avisa (`target_event_rate_missing`) y el dataset cae a ~0.50 (= comportamiento de hoy); el job COMPLETA. top-k preserva el ORDEN driver→target → señal/AUC intactas. Business NO corre el executor M3/AUC gate (es ml_ds-only).
- **Kill-switch `BUSINESS_EVENT_RATE_CALIBRATION`** (Settings `business_event_rate_calibration`, default `true`; `backend/.env.example`): off → la tasa business se nulifica de nuevo y el target vuelve a ~0.50 → **byte-idéntico al previo** (revert env-only, sin redeploy; forma de `MLDS_DECHURN_SIGNAL`). **Sin nueva clave canónica/student-facing, sin entrada en `case_sanitization`, sin migración Alembic, sin cambio de frontend** (el renderer ya pinta `doc7_dataset`/Exhibits genéricamente; `target_event_rate` ya existe en el contrato). ml_ds+clf y las demás familias byte-idénticas. Golden oracle `golden_eval.check_dataset_prevalence_matches_contract` → `NodeEvalInputs.dataset_prevalence_matches_contract_ok` → `evaluate_downgrade_gate` (simbólico en el set congelado; dientes en los unit RED/GREEN). Casos ya generados NO se reprocesan. **merge ≠ deploy** (verificación LIVE de un caso business+clf obligatoria antes del deploy). Tests: `backend/tests/test_issue518_business_event_rate.py` (+ actualizados `test_issue363`/`test_issue372`).

## Moneda USD-only (Issue #370)

- Decisión de producto: **todo valor monetario del producto es USD.** No reintroducir multi-moneda sin ADR.
- `business_cost_matrix.currency` se **COERCE** a `"USD"` en `tools_and_schemas._normalize_currency` (cualquier valor no-USD → `"USD"`), NUNCA se rechaza. Coerce (no reject) es deliberado: un `raise` se propaga a `_validate_business_cost_matrix` (`graph.py`) y nulifica la matriz COMPLETA, perdiendo `fp_cost`/`fn_cost` (M3 cae a fallback fp=1/fn=5 y se borra la asimetría). `_SUPPORTED_COST_CURRENCIES` queda como único allowlist con un solo código (`{"USD"}`).
- Los Exhibits (la fuente real de la moneda) se fuerzan a USD vía la **regla global de moneda** en el prompt base del architect (`prompts/_architect_base.py`, sección Boundaries) — aplica a `business` Y `ml_ds`, todas las familias. El anchor de clasificación (`M1_clasificacion/architect.py`) fija `currency` SIEMPRE `"USD"`. Editar cualquiera de los dos regenera `_MLDS_ARCHITECT_PROMPT_SHA256` (`test_issue301_pr2b_architect.py`).
- Drift-guard: `test_issue340_architect_cost_matrix_emission.py::test_prompt_currency_is_usd_only` mantiene prompt↔validador en USD-only. El backstop determinista de moneda en los **numerales** (no la etiqueta) = `TODO-USD-EXHIBIT-GUARD-370A`, **RESUELTO en #377** (ver sección dedicada). El eval live (370B) sigue diferido (`TODO-USD-LIVE-EVAL-370B`).

## Backstop determinista de moneda USD (Issue #377)

- Cierra `TODO-USD-EXHIBIT-GUARD-370A`. #370 hizo determinista la **etiqueta** (`currency` coerce); #377 hace determinista el **numeral**: un relabel **profile-agnostic** que reescribe cualquier token de moneda no-USD pegado a una cifra (`€/£/EUR/COP/MXN/R$/…`) a `USD`. Misma filosofía de #370: **forzar** USD, no detectar-y-degradar. Decisión de diseño: el reprompt-que-degrada estilo #360/#372 que proponía el issue **NO** se implementó — degrada (puede shipear `€500`) y solo cubre ml_ds+clf; el relabel da garantía **dura** ("ninguna moneda extranjera llega al estudiante") en **cualquier** caso.
- Detector puro `enforce_usd_currency(text)` en `m1_grounding.py` (puro, profile-agnostic, no importa graph): matcher **propio** no-USD (NO reusa `_CURRENCY`, que mezcla `$`/USD), **allowlist `$`/`US$`/`USD` = no-op** (caso conforme byte-idéntico, business incluido), reemplaza SOLO el token de moneda por `USD` con **magnitud preservada** (no rompe el match exacto de #360), best-effort (error interno → identidad). **Reglas de precisión zero-FP (cada una cierra una clase de falso positivo hallada en la review adversarial de #377), cuatro pasadas `re.sub`:**
  - **Símbolos** (`€ £ ¥ R$ ₡ ₲ …`, glifos solo-moneda): decoran CUALQUIER cifra (incluida bare). `R$` antes que `$`; bare `$` NO está en el set.
  - **Códigos ISO** (`EUR/COP/MXN/…`): SOLO sobre una cifra con **agrupación de miles** (`1.200`/`5.000.000`/`1.200,50`) Y **doble `\b`**. Una cifra bare / año / ratio junto a un token ISO-shaped **NO** es dinero → se respeta la regla "enteros estructurales bare se ignoran" del módulo. Esto mata `INR 3` (ratio clínico), `NIO 500` (empresa), `COP 2024` (año), `PEN 5`, `CAD 4`, y el `\b` final mata `EUR500X → USD 500X` (asimetría prefijo/sufijo).
  - **Palabras**: solo `euros` (cualquier cifra, **sufijo only** — el español escribe "500 euros") y la frase explícita `libras esterlinas`. Bare `libras` (= unidad de peso) NO se reescribe; `pesos`/`reales` quedan fuera (ambigüedad + la partícula `de`). El brazo de palabra se quitó del prefijo → `norma euro 6` intacto.
  - **Espacios NO-newline** (`[^\S\n]`): nunca une una cifra de una línea con un identificador ISO-shaped de la siguiente → el código Python del notebook M3 (`x = 42\nTRY = …`) no se corrompe (verificado con `ast.parse`).
  - **Cuantificadores ACOTADOS** (sin `+` ilimitado en una corrida de dígitos) → matching **lineal**, sin backtracking catastrófico (ReDoS): un blob numérico contiguo de 80KB corre en ~0.17s (antes O(n²), 18-24s).
- Limitación honesta: un relabel no revierte una conversión FX (`5.000 COP → 5.000 USD` queda con magnitud equivocada si el LLM convirtió); el fallo común es desliz de símbolo (se corrige perfecto). FN aceptados de bajo valor: ISO bare sin agrupar (`EUR 500`), `5 millones EUR`, `1.2M EUR` (los catch dominantes — símbolo `€`, montos formateados, `euros` — sí cubren). Número correcto sigue prompt-only + el eval live diferido (370B).
- **Dos puntos de integración** (`graph.py`): (1) **fuente** — el architect emite **structured output** (`CaseArchitectOutput`) que **NO pasa por `sanitize_markdown`**, así que `_enforce_usd_currency_field` se aplica a los 7 campos del architect en el `return` de `case_architect`, sobre `anexo_operativo_final` (DESPUÉS de #372, para no tocar la fila de tasa F1); (2) **downstream** — `enforce_usd_currency` se compone como último paso de `sanitize_markdown`, cubriendo narrativa M1 del writer, EDA M2 y narrativa M4/M5 en un solo punto DRY (el notebook M3 también pasa por ahí: no-op virtual en código Python real).
- **NO toca prompt-constants** → `_MLDS_ARCHITECT_PROMPT_SHA256` congelado (la regla USD del prompt ya vive en `_architect_base.py`/`architect.py` desde #370). Sin nueva clave canónica/state, sin entrada en `case_sanitization`, logger-only. Kill-switch `CASE_USD_CURRENCY_ENFORCE` (Settings `case_usd_currency_enforce`, default `true`; `backend/.env.example`) → off = passthrough exacto (revert instantáneo sin redeploy, forma de `MLDS_DECHURN_SIGNAL`). Tests: `test_issue377_usd_currency_enforce.py` (matriz FP/FN del detector + composición `sanitize_markdown` + nodo `case_architect` fuente/business-byte-idéntico/kill-switch + orden vs #372).

## Strip determinista de LaTeX matemático (Issue #480)

- Scope: AMBOS perfiles, TODAS las familias y módulos. El LLM emite notación matemática LaTeX inline (`$k$`, `$k=2$`, `$5000.0 - 50.0$`) en la narrativa técnica; el case-viewer renderiza con `marked()` y **NO tiene KaTeX/MathJax** (`CaseContentRenderer.tsx`/`safeMarkdown.ts`), así que el estudiante ve el delimitador literal (hallado en un caso LIVE ml_ds+K-Means). El fix es BACKEND (el contenido del producto debe ser texto plano). Espeja el patrón de dos superficies del backstop USD #377.
- **Detector puro `strip_latex_math(text)` (`m1_grounding.py`, co-ubicado con `enforce_usd_currency`):** quita los delimitadores `$…$` de matemática INEQUÍVOCA conservando el contenido. **Currency-safe por construcción** — toda la moneda del producto es `$`-PREFIJO emitido como token separado (`$8,000,000 USD`/`$64.8M`), nunca pareado, y `enforce_usd_currency` **nunca toca un `$` pelado** (espacios de caracteres DISJUNTOS → el orden entre ambos es irrelevante). El discriminador se codifica DENTRO de la regex (dos tiers) para que el motor SALTE un `$` de moneda y encuentre el par LaTeX real (mata el FN de interleave `$8M y $k$`): **Tier 1** = contenido que empieza con LETRA (variable `k`/`k=2`/`F_1`/`x^2`); **Tier 2** = numérico/aritmético SIN coma de miles, SIN letra de magnitud (K/M/MM), SIN `USD` (`5000.0 - 50.0`/`4950.0`). El `$` de cierre debe pegar al token (sin `\s` final → `$HOME $PATH` shell-safe) y NO ir seguido de dígito (`(?!\d)` → adyacencia `$50 $100`). Cuantificadores ACOTADOS → lineal (anti-ReDoS, como #377); loop ≤2 pasadas colapsa `$$…$$`; best-effort (error → identidad); idempotente. FN aceptados (nunca corrompen moneda): comandos `$\beta$`/`$\frac{a}{b}$` (los cubre el prompt), decimal con coma locale-ES.
- **Dos superficies (`graph.py`):** (1) **downstream** — `strip_latex_math` se compone en `sanitize_markdown(text, *, strip_latex_math=True)` ANTES de `enforce_usd_currency`, cubriendo M1 writer / EDA M2 / M3-content / M4·M5 / teaching notes en un punto DRY. **El M3 NOTEBOOK se EXCLUYE** (`strip_latex_math=False` en el único call site `_invoke_m3_notebook_algo_section`): se consume en **Colab/Jupyter, que SÍ renderiza math**, y su código puede llevar `$` legítimo (shell `$HOME`, regex `$`). (2) **fuente** — `_strip_latex_math_field` (espejo de `_enforce_usd_currency_field`) se aplica SOLO a los **4 campos de PROSA** del architect (`company_profile`, `dilema_brief`, `pregunta_eje`, `doc1_instrucciones`), NO a las 3 tablas Exhibit (densas en `$`-moneda, cero math → mínima superficie de FP).
- **Defensa en profundidad (prompt-level, GENERAL):** una línea brace-free en `M3_EXPERIMENT_PROMPT` + `M4_CONTENT_GENERATOR_PROMPT` + `M4_CONTENT_GENERATOR_PROMPT_NEUTRAL` (`prompts/_shared.py`) instruye escribir la matemática en texto plano (las variantes clf/clustering/business derivan de estas bases). M2 EDA queda cubierto por el backstop determinista (no se tocan los prompts EDA per-familia). El backstop es la GARANTÍA; el prompt es defensa.
- **NO toca prompt-constants** → `_MLDS_ARCHITECT_PROMPT_SHA256` congelado. Sin nueva clave canónica/state, sin entrada en `case_sanitization`, sin migración, sin frontend. Kill-switch `CASE_STRIP_LATEX_MATH` (Settings `case_strip_latex_math`, default `true`; `backend/.env.example`) → off = passthrough byte-idéntico (revert instantáneo sin redeploy, forma de `CASE_USD_CURRENCY_ENFORCE`). Golden oracle `check_no_latex_math` (reusa `strip_latex_math`: un texto es limpio iff stripear es no-op) → `NodeEvalInputs.narrative_no_latex_math_ok` → `evaluate_downgrade_gate`. Casos ya generados NO se reprocesan. Tests: `test_issue480_strip_latex_math.py` (matriz FP/FN exhaustiva del detector + composición/exclusión-notebook en `sanitize_markdown` + nodo `case_architect` prosa/Exhibit/kill-switch + ReDoS + golden RED).

## ml_ds Classification Target & Churn-Coupling (Issue #347 auditado → de-churn del DATO DONE en #382)

- Scope: `studentProfile == "ml_ds"` + family `clasificacion`. Para un dilema **NO-retención** (fraude/mora/aprobación) la SEÑAL del target binario ahora deriva de un **driver de DOMINIO**, NO de `churn_rate`, y el template churn/SaaS se elimina del dataset (Issue #382). El camino **churn/retención queda byte-idéntico**. (#347 se cerró como auditoría+guardarraíl; #382 es su sucesor real que tocó el generador determinista.)
- Cadena determinista en `schema_designer` (`graph.py` happy + fallback): `_align_ml_ds_classification_target` (renombra la binaria `categoria` al nombre del contrato CONSERVANDO su driver; corre ANTES del augment) → `_augment_schema_with_contract` (inyecta las features del contrato con `dependency=None`) → `_enforce_business_classification_schema` (**NOOP para ml_ds**) → **`_enforce_mlds_classification_schema` (SIBLING #382)**.
- **SIBLING `_enforce_mlds_classification_schema`** — NO generalizar la función business (`test_enforce_noop_for_ml_ds` exige que sea identidad para ml_ds). PURO copy-on-write (no muta el dict de entrada → determinismo del seed + thread-safety). Gate: `enabled` (kill-switch) AND `profile=="ml_ds"` AND `(primary_family or "clasificacion")=="clasificacion"` AND target binario (`role==classification_target`, `dtype=="int"`) AND `NOT _is_retention_target_name(target)`. Fuera del gate → schema byte-idéntico (mismo objeto). Acción: (a) resuelve la binaria objetivo orden-robusta + **guard de colisión** (si `_align` saltó el rename porque el nombre del contrato colisiona con una columna no-objetivo → no-op + warning; defecto pre-existente de `_align`, no se amplía); (b) `_select_driver_feature` (1ª `feature_columns` numérica no-leakage, o sintetiza `domain_driver_score` y lo **APENDE** al schema); (c) re-apunta `target.dependency.depends_on` al driver de dominio (noise_factor con la fórmula de `_binary_target_column`); (d) **strip** de `_CHURN_TEMPLATE_COLUMNS ∪ _MLDS_SAAS_TEMPLATE_COLUMNS` EXCEPTO las `feature_columns` del contrato / el target / el driver (conserva la base financiera `period/revenue/costs/margin_pct`); (e) **repara dependencias colgantes** (un `depends_on` a un padre eliminado → `None`, evita huérfanos→ruido); (f) marca `is_domain_target=True`; (g) `logger.warning` estructurado **LOG-ONLY** (no teacher-facing; precedente #336).
- **Kill-switch `MLDS_DECHURN_SIGNAL`** (Settings `mlds_dechurn_signal`, default `true`; `backend/.env.example`): off → schema byte-idéntico al churn-coupled previo (revert instantáneo sin redeploy, forma de `AUTHORING_LIVE_PREVIEW`). Los call sites pasan `enabled=settings.mlds_dechurn_signal`.
- **Anti-degeneración:** `_ensure_both_classes` se amplió a `profile in {"business","ml_ds"}` para el target `is_domain_target` — cubre el path SIN `target_event_rate` (el architect puede omitirlo, `graph.py` `_validate_target_event_rate`). El path CON rate (top-k F1, `_generate_dataset_from_schema`) queda **intacto** y ya garantiza ambas clases. Byte-idéntico para churn: `categoria` no lleva `is_domain_target`.
- **Prompt acoplado (mismo PR):** `M2_clasificacion/dataset.py` reframeado dilema-aware (template churn/SaaS de 18 cols solo para RETENCIÓN o contrato null; CONTRACT-FIRST domain para no-retención) + `reasoning_summary`/docstring; ejemplos SaaS de `eda_text.py` (`_EDA_*_ML_DS`) domain-neutralizados (business byte-idéntico, rigor DS intacto). Contrato business 10-col + los 7 placeholders intactos. El SIBLING es la GARANTÍA; el prompt mejora el camino feliz.
- **NO toca** `_build_fallback_schema`, `_align`/`_augment`, ni el M1 architect (hash `_MLDS_ARCHITECT_PROMPT_SHA256` congelado — el de-churn es 100% capa de DATO post-LLM). El gate AUC del executor sigue **NO-bloqueante** (`is_m3_quality_warning_blocking`, `m3_notebook_execution.py`) → un de-churn fallido DEGRADA, no falla el job.
- Guardarraíles: `test_issue347_mlds_target_signal_guard.py` (su helper corre el sibling; añade aserción **domain-only AUC ∈ [0.55,0.99]** + churn/SaaS ausentes + driver de dominio + `is_domain_target`; conserva el invariante + control de ruido no-tautológico) y `test_issue382_mlds_dechurn_signal.py` (mecánica del sibling: strip, re-apunte, purity, colisión, kill-switch, synth-driver, feature-protegida, no-rate→ambas clases, churn byte-idéntico). Medido E2E: caso fraude → `churn_rate` ausente, target ← `transaction_amount`, domain-only AUC ≈ 0.91; caso churn → byte-idéntico.
- Follow-up **#383 (RESUELTO)** — el literal `categoria` → `{target_column_name}` en M2 EDA ya está parametrizado (ver sección dedicada abajo). Edge **Caso A** (ml_ds+clf SIN `dataset_schema_required`): mantiene el template churn (sin dominio que inferir; sibling no-op) y el resolver de #383 cae a `categoria` → coherente con el dato.

## M1 ml_ds Classification Target Binario-Only (Issue #350)

- El target ml_ds + `clasificacion` es **BINARIO-ONLY** (`int` 0/1). Todo el downstream lo asume (schema `CLASIFICACIÓN BINARIA`, notebook `is_binary=(y.nunique()==2)` → `skipped_non_binary_target`, métricas/cost 2×2). El ancla M1 (`architect.py` regla 1) lo fija explícitamente (`dtype` `int` 0/1, prohíbe multiclase) y la regla 2 (`pregunta_eje`) plantea una decisión binaria intervenir/no-intervenir. NO reintroducir la invitación "binario o multiclase" sin ADR (multiclase real = path B: schema multiclase, notebook OvR, métricas macro, cost matrix K×K). Las referencias legítimas a "multiclase" en las puertas de `business_cost_matrix` / `target_event_rate` (`architect.py` ~:85/:130, "NO la/lo emitas") NO son el target framing y NO se tocan.
- Editar el ancla regenera `_MLDS_ARCHITECT_PROMPT_SHA256` (`test_issue301_pr2b_architect.py`) — DELIBERADO; el diferencial (`test_mlds_architect_prompt_unchanged_by_business_gate`) debe quedar verde PRIMERO (prueba que el bloque business no se filtró). Solo ESE hash rompe; #340/#363/#370(USD, dentro de #340) asertan otras sub-secciones. El ancla es brace-free salvo los ejemplos JSON ya `{{}}`-escapados.
- **Backstop determinista** (el prompt es probabilístico): `_normalize_mlds_classification_target` (`graph.py`, SIBLING de `_normalize_business_classification_target` — NO generalizar; `test_normalize_noop_for_ml_ds` exige que el business siga no-op para ml_ds). Corre en el call site de `case_architect` (junto al normalizador business, ANTES de persistir `dataset_schema_required`). Coacciona AMBOS `role` Y `dtype` de cualquier target que no sea ya `classification_target` binario (un `anomaly_target` `dtype=int` pasaría un chequeo dtype-only y degradaría igual vía `_augment._default_column` → `int[0,100]`). Preserva el nombre de dominio para `_CLASSIFICATION_ADJACENT_ROLES`, si no cae a `target_event_flag`. Copy-on-write (determinismo del seed + thread-safety), LOG-ONLY warning (precedente #336), passthrough byte-idéntico (mismo objeto) para churn / binario ya válido. Gate `ml_ds` + `(family or "clasificacion")=="clasificacion"` (cubre el cohorte ml_ds-sin-algoritmos; `family_resolved` ya llega `"clasificacion"` por `_resolve_generation_focus`). Cierra en el ORIGEN la degradación SILENCIOSA post-#348: un target `dtype=str`/multiclase llegaba al notebook contract-first → `is_binary=False` → `skipped_non_binary_target` NO-bloqueante (`is_m3_quality_warning_blocking`) → el job COMPLETA sin modelo y SIN flag `m3NotebookDegraded` (solo un `logger.warning`).
- **Kill-switch** `MLDS_BINARY_TARGET_COERCE` (Settings `mlds_binary_target_coerce`, default `true`; `backend/.env.example`): off → passthrough exacto, comportamiento previo restaurado sin redeploy (forma de `MLDS_DECHURN_SIGNAL`). Guardarraíles: `test_issue350_m1_binary_only.py` (guards del ancla; unidades del normalizador str/continuo/anomaly/passthrough/no-op/kill-switch/purity/cohorte; cadena `_build_fallback_schema → normalizador → _align → _augment → _enforce_mlds` con **CONTROL NEGATIVO ROJO-sin-fix** que deja la columna `str` colgante + aserción de 2 clases del dataset generado). Cero cambio para `business`/`regresion`/`clustering`/`serie_temporal` ni para el de-churn (#346/#382/#383).

## ml_ds Classification Narrative De-churn (Issue #346 — narrativa M2/M4)

- La capa de PROMPTS de M2 (narrativa EDA `eda_text.py` + 2 preguntas Socráticas `eda_questions.py`) y el ejemplo P1 de M4 (`M4_clasificacion/questions.py`) ya NO cablean churn para `studentProfile == "ml_ds"` + family `clasificacion`. Antes, un caso no-churn (fraude/mora) le decía al estudiante que el target era "el churn", nombraba las clases "no-churn"/"churners" y proponía `churn_risk_score`; ahora la narrativa es **domain-general** (el evento objetivo), coherente con M1/M3/M4/M5 que ya lo eran.
- #346 es SOLO vocabulario de prompts (M2/M4): CERO cambio al generador determinista, schemas, o M1 architect (hash congelado). Conserva TODO el rigor DS (AUC-ROC, Accuracy Paradox, Matriz de Confusión, fórmula `max(count_cat0, count_cat1)`, F1, Leakage Guard, Precision/Recall, F-beta) y el literal `categoria` (igual que el render business).
- `eda_text.py` gatea por perfil con bloques inyectados (`select_eda_text_blocks`): el de-churn editó SOLO los símbolos `_EDA_*_ML_DS` + el `event_label` ml_ds ("el churn" → "el evento objetivo"), así que el render **business queda byte-idéntico** por construcción. `eda_questions.py` es una plantilla ÚNICA (sin bloques por perfil): el churn vivía en el cuerpo P1/P2 compartido → el de-churn limpia AMBOS perfiles (no hay contrato byte-idéntico que preservar ahí; "mantener business intacto" se refería a `eda_text`, no a este).
- Guards en `test_m2_clasificacion_dispatch.py`: `test_eda_text_ml_ds_not_churn_hardcoded`, `test_eda_questions_classification_not_churn` (ambos perfiles), `test_eda_questions_classification_preserves_pedagogy`; el contrato de placeholders de EDA-preguntas (7) NO cambia (el de EDA-texto pasó de 18 a 19 en #383 al añadir `{target_column_name}`).
- Follow-up (RESUELTO en #383): el prompt EDA usaba el literal `categoria` mientras `_align_ml_ds_classification_target` renombra la columna del dataset al nombre del contrato (`fraud_flag`, etc.). El de-churn del DATO (#382) NO tocó ese literal (mantuvo `categoria` en `eda_text.py`). #346 cerró la incoherencia de NARRATIVA, #382 la del DATO (señal), **#383 cerró la del nombre de columna** (ver sección dedicada).

## ml_ds Classification M2 EDA Target Name (Issue #383)

- Cierra el follow-up de #346/#382: el prompt EDA de clasificación (`M2_clasificacion/eda_text.py`) ya NO hardcodea el literal `categoria`. La capa de DATO (`_align_ml_ds_classification_target` en `schema_designer`) renombra la binaria `categoria` al nombre del contrato (p.ej. `fraud_flag`) ANTES de `eda_text_analyst`; ahora la narrativa M2 cita ese nombre real. Solo-prompt + 1 placeholder + 1 helper puro; CERO cambio al generador, schemas, `_align`, o M1 architect (hash congelado — solo se CONSUME el nombre downstream).
- Resolver: `graph.py` `_resolve_eda_target_name(state)` (puro, junto a `_safe_contract_target_name`). Gatea con `_is_ml_ds_classification(state, default_unresolved_ml_ds_to_classification=True)` para ESPEJAR la selección de prompt (`_build_base_context`) y `_align` (ambos tratan ml_ds-sin-algoritmos como clasificación); de lo contrario ese cohorte renderizaría el prompt clf + columna renombrada pero narraría "categoria". Deriva del CONTRATO (`dataset_schema_required.target_column.name`, guard `.isidentifier()`) y lo VERIFICA contra las columnas reales de `doc7_dataset` (cubre el edge de colisión de `_align` que salta el rename → dataset queda `categoria`). `business` / no-clf / regresion → literal `"categoria"`.
- `eda_text_analyst` inyecta `target_column_name` al contexto (cuerpo, 5 sitios vía `.format`) Y lo pasa a `select_eda_text_blocks(profile, target_column_name)`, que lo **pre-sustituye** en los bloques `_EDA_*_ML_DS` vía `str.replace("categoria", name)` — los bloques son placeholder-free (un `{..}` dentro no sobrevive el único `.format`). `count_cat0`/`count_cat1` (fórmula de imbalance) NO contienen "categoria" → preservados; `max(count_cat0, count_cat1)` queda intacto.
- **`business` BYTE-IDÉNTICO** por construcción (gate → "categoria"; bloques business sin `categoria`; el cuerpo reproduce el literal) — verificado con diff main↔rama. `regresion`/`clustering`/`serie_temporal` usan el prompt genérico que ignora la clave extra. Default `"categoria"` (param + caso churn-con-categoria) = no-op reversible. `eda_questions.py` queda sin cambio (ya genérico, 0 `categoria`).
- Contrato de placeholders de `EDA_TEXT_ANALYST_PROMPT_CLASSIFICATION`: **18 → 19** (`+{target_column_name}`). Guardarraíles en `test_m2_clasificacion_dispatch.py`: contrato 19, render ml_ds renombra / byte-idéntico-default, business invariante al param, y la matriz del resolver (happy, colisión, contrato inválido, ml_ds-sin-algoritmos, regresion, sin-muestra).

## ml_ds Classification De-template Cross-Section (Issue #507)

- Scope: `studentProfile == "ml_ds"` + family `clasificacion`, targets NO-retención de ENTIDADES cross-section (encuestas de hogares/individuos: valoración ambiental, scoring, aprobación). Cierra el follow-up de #382/#383: el de-churn (#382) elimina el bloque churn/SaaS pero **CONSERVABA** la "base financiera" de panel mensual de empresa (`period/revenue/costs/margin_pct`). Para un target cross-section (`accepts_fee_flag` de una encuesta de hogares) esas columnas son RESIDUO de plantilla — un hogar no tiene revenue/costs/margin mensual. Hallado en una corrida LIVE real (caso "AquaVida — humedal", ml_ds+LR, AUC 0.856; el M4 reportó "`margin_pct` con outliers de -85%"). Ahora se eliminan TAMBIÉN, EXCEPTO las columnas que el contrato declara como feature real.
- **Allowlist = detección (NO grano).** El issue insinuaba "detectar cross-section vs panel", pero NO existe campo de grano en el contrato y añadirlo tocaría el architect/SHA + colisionaría con #506. La decisión es más robusta: el `protected` set de #382 (features del contrato ∪ {target, driver}) YA es la detección — una columna financiera que NO es feature del contrato es residuo; si un panel-empresa legítimo la lista como predictor (`revenue`), queda protegida. Determinista, sin LLM, sin tocar prompt/architect/SHA.
- **Extiende el sibling #382 (DRY), no duplica.** `_FINANCIAL_PANEL_TEMPLATE_COLUMNS = frozenset({period, revenue, costs, margin_pct})` (junto a `_CHURN_TEMPLATE_COLUMNS`/`_MLDS_SAAS_TEMPLATE_COLUMNS` en `graph.py`). `_enforce_mlds_classification_schema` gana un param `detemplate_cross_section: bool = True` (default fail-safe, consistente con `enabled=True`) que UNE el set financiero al `strip_set` existente — misma operación, mismo gate, misma reparación de deps colgantes, mismo log estructurado. Los 2 call sites de `schema_designer` (happy + fallback) pasan `detemplate_cross_section=settings.mlds_detemplate_cross_section`.
- **Cobertura IDÉNTICA a #382** (mismo gate + resolución de `binary_targets`): dispara EXACTAMENTE cuando #382 dispara, en casos NO-retención (gate `NOT _is_retention_target_name`). El strip opera sobre la lista de columnas del schema en AMBOS call sites → robusto a schema-del-LLM o fallback. Sin deps colgantes (ninguna feature del contrato depende de la base financiera; `churn_rate→revenue` se elimina junto). El generador tolera la ausencia: las filas se proyectan sobre `col_order` (columnas del schema; el `df_data["period"]=…` incondicional se descarta al ensamblar) y revenue/costs/margin recompute guardan con `if col in row`. Downstream MEJORA: el chart MI de `eda_charts_classification.py` deja de graficar la base financiera como falso predictor (origen del "margin_pct -85%").
- **Kill-switch `MLDS_DETEMPLATE_CROSS_SECTION`** (Settings `mlds_detemplate_cross_section`, default `true`; `backend/.env.example`): off → comportamiento #382 byte-idéntico (base financiera conservada; el de-churn churn/SaaS sigue). Byte-idéntico para churn/retención (gate retorna temprano), business (gate `profile=="ml_ds"`), y otras familias. **Sin nueva clave canónica/state, sin entrada en `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA.** Exhibit 1 (P&L del caso, `anexo_financiero`) es un artefacto SEPARADO de `doc7_dataset` → intacto. Golden oracle `check_no_financial_panel_residue` (reusa la constante) → `NodeEvalInputs.financial_panel_stripped_ok` → `evaluate_downgrade_gate`; fixture `g13` regenerada (sin base financiera). El blast radius de tests es EXACTAMENTE 1 aserción (`test_issue382:95`, actualizada); 347/351/350/466/glm/301 verdes (piso/gap, churn-only, driver-only, business no-op). Casos ya generados NO se reprocesan. Tests: `backend/tests/test_issue507_detemplate_cross_section.py`.

## ml_ds Classification De-template Workforce/HR Attrition (sibling de #382/#507)

- Scope: `studentProfile == "ml_ds"` + family `clasificacion`, targets de ATRICIÓN DE EMPLEADOS (`attrition_flag`, `employee_attrition`, `staff_turnover`). Cierra el falso-equivalente *churn de clientes ≡ atrición de empleados*: `attrition` es un token DURO de retención (`retention_tokens._HARD_TOKENS`), así que `_is_retention_target_name` los marca retención y el de-churn #382 los SALTABA por su LÍNEA ROJA (early-return), conservando el template SaaS de CLIENTES (`customer_ltv`/`plan_tier`/`payment_failures`/`churn_rate`/`retention_m*`…) + el panel financiero. Para un caso de RRHH (entidad = EMPLEADO) esas columnas son residuo de plantilla → el chart M2 `mutual_info_top8` mostraba `revenue`/`churn_rate`/`customer_ltv` como predictores de la atrición de empleados. Hallado en una corrida real (HR attrition: target tipo `attrition_flag`, features antigüedad/satisfacción/departamento/nivel del puesto/horas extra).
- **Decisión (least-risk para el test con usuarios de universidad): carve-out de ALTA PRECISIÓN solo-RRHH**, NO se generaliza a todo retención no-SaaS (student dropout / patient readmission / donor churn quedan como follow-up documentado). El churn de clientes genuino queda **byte-idéntico** (constraint duro: `test_issue382:152` / `test_issue507:162` fijan `churn_flag`+`transaction_amount` = no-op). NO se toca el vocab compartido `is_retention_match` (blast radius amplio — leakage inference + `_TITLE_TO_TARGET_TOKENS`); el carve-out vive SOLO en el gate del de-churn.
- **Mecanismo (enforcer-only, espeja #507; sin tocar el prompt).** Predicado puro `_is_workforce_attrition_case(contract)` (`graph.py`) con DOS niveles de tokens RRHH normalizados sin separadores (robusto a `job_level`/`JobLevel`/`joblevel`): **ENTIDAD** (`employee`/`empleado`/`staff`/`workforce`/`headcount`/`plantilla`/`personnel`/`worker`/`trabajador`) cuenta SOLO en el NOMBRE DEL TARGET (en una feature es ambiguo — un churn B2B legítimo lleva `num_employees` = tamaño de la empresa-cliente) y **ATRIBUTO de empleado individual** (`overtime`/`seniority`/`absentee`/`worklife`/`years_at_company`/`years_in_role`/`job_*`) cuenta en el target O en cualquier feature (no describe a un cliente). EXCLUYE a propósito tokens ambiguos cliente-también (`tenure`=antigüedad de suscripción, `attrition`/`turnover`=atrición/rotación de CLIENTES, `department`/`salary`/`manager`) → cero FP sobre la línea roja. La LÍNEA ROJA del gate (`_enforce_mlds_classification_schema`) se ACOTA: `if _is_retention_target_name(t) and not (detemplate_workforce and _is_workforce_attrition_case(contract)): return schema`. Un target RRHH SALE del early-return y sigue la MISMA ruta de-template (re-apunta a un driver de DOMINIO RRHH + strip del template SaaS/churn/financiero, conservando las features del contrato).
- **Kill-switch `MLDS_DETEMPLATE_WORKFORCE`** (Settings `mlds_detemplate_workforce`, default `true`; `backend/.env.example`): off → todo target retención conserva el template byte-idéntico (= #382/#507; revert env-only, sin redeploy). Pasado en los 2 call sites de `schema_designer` (happy+fallback) como kwarg `detemplate_workforce`. El strip del panel financiero sigue gateado por `MLDS_DETEMPLATE_CROSS_SECTION` (default on → RRHH también pierde period/revenue/costs/margin). **Sin nueva clave canónica/state, sin entrada en `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA, sin cambio de prompt** (el enforcer es la GARANTÍA; precedente #507). Golden oracle = el ya existente `check_domain_coherence` (un schema RRHH de-templateado pasa: sin residuo SaaS/churn + `is_domain_target` con driver≠`churn_rate`; con el switch off el template sobrevive → RED control) → `NodeEvalInputs.domain_coherence_ok` → `evaluate_downgrade_gate`. business / churn de clientes (incl. B2B con `num_employees`) / otras familias byte-idénticas. Casos ya generados NO se reprocesan. **PENDIENTE verificación LIVE** (merge ≠ deploy): regen ml_ds+clf de atrición de empleados → CSV sin columnas SaaS, M2 `mutual_info_top8` con features de RRHH. Tests: `backend/tests/test_mlds_workforce_detemplate.py`.

## Dataset outliers respetan el rango declarado (ml_ds, todas las familias)

- Scope: `studentProfile == "ml_ds"`, TODAS las familias. La inyección de outliers para EDA (Fix B-05, `_generate_dataset_from_schema` en `graph.py`) capeaba el atípico de `numeric_non_revenue[0]` a `range_max × 2.0` para ml_ds (vs `× 1.0` en business). El generador YA clipa todo valor generado a `[low, high]`, así que el atípico era la **única** violación de los rangos declarados por el propio schema → producía valores **semánticamente imposibles** para una feature acotada (un `credit_score` declarado `[300,850]` llegaba a **1700**; un porcentaje pasaba de 100; una edad de 150). Hallado en una corrida LIVE de ml_ds + Random Forest. Es un defecto de **realismo/confianza** (un profesor abre `doc7_dataset` o el boxplot M2 y ve un puntaje crediticio de 1700) que conecta con la promesa narrativa rf_only ("robustez ante outliers" — el atípico debe ser extremo PERO posible, no un error de datos).
- **Fix = el atípico ml_ds capa en `range_max` (×1.0), igual que el path business ya-correcto.** El punto inyectado sigue siendo un atípico estadístico **~5σ** (el grueso de la columna está concentrado en el centro con `std ≈ 0.10·rango`, así que un valor en `range_max` queda a varias σ de la media → detectable en EDA) pero ahora es un valor **VÁLIDO**. Solo se capa el `range_max` (la única violación observada); el `range_min` lo garantiza el clip de generación para las features **no-negativas** que usa ml_ds (el atípico es `original×3.5` ≥ `original` ≥ `range_min` cuando `range_min ≥ 0` — score/monto/conteo/ratio/edad). NO se añade un floor explícito de `range_min` (mantener el OFF byte-idéntico es la prioridad; un `range_min` negativo no ocurre en este dominio y su underflow sería el comportamiento legacy pre-existente, sin cambio). El generador queda **puro** (no lee `settings`): el flag se pasa como kwarg `outlier_respect_range: bool = True` y `data_generator` lo cablea con `settings.mlds_outlier_respect_range`.
- **Kill-switch `MLDS_OUTLIER_RESPECT_RANGE`** (Settings `mlds_outlier_respect_range`, default `true`; `backend/.env.example`): off → restaura el cap ml_ds `× 2.0` legacy **byte-idéntico** (revert env-only instantáneo, sin redeploy). **business es byte-idéntico en ambos modos** (siempre cayó en la rama `× 1.0`). Solo difiere la columna del atípico (`numeric_non_revenue[0]`); el resto del dataset es idéntico entre modos. Determinista (el cap es puro). **Sin nueva clave canónica/student-facing, sin entrada en `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA.** Golden oracle `golden_eval.check_outliers_within_declared_bounds` (puro, profile/family-agnóstico; salta las columnas re-escaladas/re-computadas/enforced `_BOUNDS_SKIP_TOKENS` = revenue/cost/ebitda/margin/retention_/period → zero-FP) → `NodeEvalInputs.outliers_within_declared_bounds_ok` → `evaluate_downgrade_gate`. Control RED: un dataset generado con el kill-switch off (×2) embarca un valor > `range_max` → falla. Casos ya generados NO se reprocesan. Tests: `backend/tests/test_mlds_outlier_respect_range.py`.

## Dataset — descartar columnas 100% nulas del dataset emitido (M2)

- Scope: AMBOS perfiles, TODAS las familias. El "Mapa de valores faltantes" de M2 mostró una columna (`cancellation_request_date`) 100% nula. Raíz: `_generate_dataset_from_schema` (`graph.py`) no tiene rama para una columna `type:"date"` que no se llame literalmente `period` (~línea 6660 la mete en `period_col_names`, la excluye de los loops, y el ensamblado devuelve `None` para toda columna no escrita) → sale 100% nula. DOS vías: (1) el LLM `schema_designer` emite `type:"date"`; (2) el architect declara un feature de contrato `dtype:"date"` que `_augment`/`_default_column` agrega **sin re-validar**. Los tipos exóticos NO son vector (Pydantic `Literal["int","float","str","date"]`); el único huérfano es `date`.
- **DESCARTAR, NO poblar (decisión clave).** Poblar sería una REGRESIÓN: `cancellation_request_date` ya está marcada `is_leakage_risk=True` (campo post-resultado); el filtro determinista del notebook NO lee `is_leakage_risk` y hoy la salva de la fuga SOLO porque está 100% nula (`isna>0.5` la descarta). Poblarla la metería al modelo → **fuga de target que infla el AUC**. Descartar es **byte-idéntico en modelado/grounding** (el notebook ya la excluye) — solo limpia la tabla del dataset + el heatmap M2. Una columna 100% nula es SIEMPRE un hueco de generación (las columnas legítimas nunca llegan a 100% null: nullable = 5% máx) → descartar nunca bota datos reales.
- **Fix = capa de datos (garantía) + prompt (defensa).** Helper puro `_drop_all_null_columns(rows, schema, *, target_col_name, enabled)` (`graph.py`, junto a `_validate_and_correct_dataset`; copy-on-write, total, idempotente) descarta toda columna 100% nula EXCEPTO el target, podando filas + `schema["columns"]`. Cableado en el nodo `data_validator` (best-effort `try/except` → nunca falla el job; corre ANTES de M2 EDA/M3 por topología `eda_flow → m3_flow`) tras `_validate_and_correct_dataset` (hermano del row-drop all-null existente). El schema podado se re-emite SOLO si se descartó algo (no-op → retorno byte-idéntico; espeja el precedente de re-emisión de clustering). Umbral 100% null ESTRICTO → una columna parcialmente nula (missingness real) NUNCA se descarta. Defensa: regla que veta columnas `type:"date"` standalone (la única temporal es `period` str) — `schema_designer` despacha por familia (`SCHEMA_DESIGNER_PROMPT_BY_FAMILY`), así que la regla va en el genérico `SCHEMA_DESIGNER_PROMPT` Y en `SCHEMA_DESIGNER_PROMPT_CLASSIFICATION` (el cohorte ml_ds+clf del bug usa el segundo; clustering ya lo prohibía) — todos NO SHA-locked; NO toca el architect (SHA-locked) → la vía 2 la cubre la capa de datos.
- **Kill-switch `DATASET_DROP_EMPTY_COLUMNS`** (Settings `dataset_drop_empty_columns`, default `true`; `backend/.env.example`): off → el nodo no descarta → **byte-idéntico** (la columna vacía sobrevive; revert env-only sin redeploy). Golden oracle `golden_eval.check_no_all_null_columns(rows)` → `NodeEvalInputs.no_all_null_columns_ok` → `evaluate_downgrade_gate` (RED control: dataset con columna all-null falla). **Sin nueva clave canónica/student-facing, sin `case_sanitization`, sin migración, sin frontend** (headers dinámicos `Object.keys(data[0])`), **sin tocar el architect/SHA**. `dataset_schema` no se persiste al output canónico y sus lecturas downstream están guardadas (`in df.columns`)/advisory → sin divergencia. Follow-ups documentados (NO en este cambio): prevención de la vía 2 por prompt del architect (regenera SHA); podar el feature descartado del contrato `dataset_schema_required`. Casos ya generados NO se reprocesan. **PENDIENTE verificación LIVE** (merge ≠ deploy). Tests: `backend/tests/test_dataset_drop_empty_columns.py`.

## ml_ds Classification De-churn Test Guardrails (Issue #351)

- **TEST/EVAL ONLY** — regression-lock que blinda el de-churn YA MERGED (#346/#382/#383); CERO código de producción. El valor neto sobre el coverage single-domain de `test_issue382`/`test_issue347` es **MULTIDOMINIO** (≥3 dominios no-churn) + un **oráculo de coherencia de dominio** en el golden set.
- `backend/tests/test_issue351_mlds_multidomain_evals.py` corre la CADENA REAL ml_ds (`_build_fallback_schema → _align_ml_ds_classification_target → _augment_schema_with_contract → _enforce_business_classification_schema (NOOP ml_ds) → _enforce_mlds_classification_schema(enabled=...)`) para 3 dominios no-churn (`fraud_flag`/`default_60d`/`late_delivery_flag`; nombres asertados NO-retención vía `is_retention_match`). Reusa el patrón de cadena de #347; no lo reimplementa.
- **No-tautología = ESTRUCTURAL.** `_assert_dechurn_structural` (sin columnas churn/SaaS, `is_domain_target`, `depends_on != churn_rate`) PASA con el de-churn ON, y `test_structural_oracle_is_red_when_kill_switch_off` prueba que LANZA `AssertionError` sobre el schema `enabled=False` (churn-coupled) — el eje RED→GREEN del kill-switch, determinista y domain-independiente. El oráculo corr/AUC es solo calidad-ON-path, **NO** el discriminador del kill-switch (la baseline churn-coupled domain-only AUC ≈0.57 está por ENCIMA del piso 0.55, F2; la prueba de GANANCIA de AUC para `fraud_flag` queda en `test_issue347`). Las verificaciones de narrativa (`_resolve_eda_target_name`, render EDA-texto sin churn) montan sobre #346/#383 y NO son kill-switch-proven (F1: `_align` y los prompts no están gateados por `MLDS_DECHURN_SIGNAL`).
- **Golden set:** `golden_eval.check_domain_coherence(schema)` (puro; import perezoso de los frozensets de template desde `graph`) verifica la SEÑAL de-churned (sin cols churn/SaaS + `is_domain_target` con driver de dominio), NO el texto de descripción. `NodeEvalInputs.domain_coherence_ok` (default `True` → business/no-clf n/a) alimenta `evaluate_downgrade_gate`. `GoldenJobSpec("g13", ...)` es la PRIMERA fixture materializada — un schema de-churned real post-cadena en `tests/fixtures/golden/g13_mlds_clf_single.json`, cargado por `test_golden_eval.py` (+ caso de discriminación churn-coupled + caso anti-staleness reconstruido por la cadena).
- **Follow-up FUERA DE ALCANCE (NO se arregla en #351):** la descripción del template `categoria` del fallback ("…en riesgo de churn", `graph.py` `_build_fallback_schema` + `M2_clasificacion/dataset.py`) sobrevive al rename en un target no-churn. #382 de-churna la SEÑAL (driver + columnas), no la prosa de descripción; el oráculo golden es deliberadamente signal-scoped. Verificar surfacing student-facing antes de tratarlo como gap.
- **Live opt-in:** `RUN_LIVE_LLM_TESTS=1 … -k mlds_multidomain` aserta que el architect emite un `classification_target` binario de dominio para un dilema ml_ds no-churn (auto-skip si no).

## ml_ds Clustering Decision Coherence (Issue #467, EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Cierra la incoherencia data↔narrativa (k de datos ≠ k de narrativa; M4=Opción C vs M5=Opción B; umbral fabricado «silhouette > 0.55» vs real 0.498). Detalle completo en `CLAUDE.md` (sección homónima). Resumen operativo:
- **Fuente de verdad determinista** `clustering_decision = {target_k∈{3,4}, recommended_option∈{A,B,C}, silhouette_floor}` (módulo puro `clustering_decision.py`, espejo de `impact_lens.py`): resuelta una vez en `core/authoring.py` `state_input` desde un seed de intake ESTABLE (idempotente → resume-stable, lifecycle `impact_lens`), **ningún nodo la escribe** (sin clobber / sin fan-out merge hazard). `ADAMState.clustering_decision: NotRequired[dict]`.
- **Propagación gateada** (`_is_ml_ds_clustering` + kill-switch + decisión; hints brace-free a nivel de nodo → SHA del architect intacto): #452 `_enforce_mlds_clustering_structure(..., target_k=)` honra el COUNT (off/`target_k=None` = byte-idéntico a #452); architect/**M1-questions**/M3-questions/M4/M5 reciben hints (`build_clustering_*_hint`); el silhouette REAL (`m3_metrics_summary`) se inyecta a M4/M5 contra la fabricación de «0.55». M3-content NO se toca (ya cualitativo, #457).
- **Guard determinista de veredicto** (la garantía): `validate_verdict_option` / `validate_m4_verdict_option` (reusan `m1_grounding._extract_option_labels`, zero-FP) + TRES guards `_apply_clustering_m1_verdict_coherence` / `_apply_clustering_m4_verdict_coherence` / `_apply_clustering_m5_verdict_coherence` (reprompt-once-then-DEGRADE, best-effort, logger-only) → **M1=M4=M5=`recommended_option`**. (El guard M1 lo añadió la verificación LIVE: P3 lo escribe `case_questions`, nodo separado del architect, así que necesita su propio hint+guard.)
- **Kill-switch `MLDS_CLUSTERING_DECISION_COHERENCE`** (Settings `mlds_clustering_decision_coherence`, default `true`; `backend/.env.example`), INDEPENDIENTE de `MLDS_CLUSTERING_STRUCTURE`. Off → byte-idéntico a pre-#467. Golden oracle `check_clustering_decision_coherence` → `NodeEvalInputs.clustering_decision_coherence_ok` → `evaluate_downgrade_gate`. Sin clave canónica/state nueva, sin `case_sanitization`, sin migración, sin frontend. **Gate LIVE obligatorio** (que la narrativa nombre `target_k` es prompt-reforzado); `target_k` acotado {3,4} hasta #468; el validador de grounding de silhouette M4 es #469. Tests: `backend/tests/test_issue467_clustering_decision.py`.

## ml_ds Clustering M4 Value Frame + Silhouette Grounding (Issue #469, EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Split de #467. Detalle completo en `CLAUDE.md` (sección homónima). Auditoría: el rol "Arquitecto Financiero" NO vive con `impact_lens` ON (corre el NEUTRAL "de Impacto", #437); el veredicto M4 vs M5 lo cerró #467. #469 arregla en M4: Gráfico 1 = payback/breakeven fabricado, §4.2 inventa uplifts, y la GARANTÍA determinista del silhouette real (el hint de #467 ya mitiga la fabricación «0.55»).
- **Prompts dedicados (NO hints), patrón #457:** subárbol `prompts/clustering/M4_clustering/` — `content.py` (`M4_CONTENT_PROMPT_CLUSTERING` = NEUTRAL base + bloque brace-free que redefine §4.1 sin AUC / §4.2 sin uplift; §4.3-4.5 reusadas → sin drift) y `charts.py` (`M4_CHART_PROMPT_CLUSTERING` standalone, Gráfico 1 = valor-por-segmento, **cero** payback salvo la línea PROHIBIDO). Dispatch por helper puro `_effective_m4_clustering_prompts` (override solo de la clave `"clustering"` cuando `lens_on ∧ ml_ds ∧ clustering ∧ switch`; identidad de objeto si no → byte-idéntico). `m4_questions` recibe un hint brace-free (reusa `build_clustering_verdict_hint`, prompt-only).
- **Validador determinista de silhouette (garantía):** `detect_unanchored_silhouette(prose, real)` (`clustering_decision.py`) marca un silhouette citado que difiera del REAL (`m3_metrics_summary["silhouette"]`); zero-FP-leaning (forma de aserción + banda [0.30,0.95] + exención floor 0.45 + guarda de negación), ReDoS-safe. **Guard COMBINADO:** se EXTIENDE `_apply_clustering_m4_verdict_coherence` (veredicto #467 + silhouette #469 en UN reprompt → evita el clobber); reprompt-once-then-DEGRADE, best-effort, logger-only. Byte-idéntico para los 5 tests #467 (sin `m3_metrics_summary` → check de silhouette inerte).
- **Kill-switch `MLDS_CLUSTERING_M4_VALUE_FRAME`** (Settings `mlds_clustering_m4_value_frame`, default `true`; `backend/.env.example`), INDEPENDIENTE de `MLDS_CLUSTERING_DECISION_COHERENCE`. Off → byte-idéntico a post-#467. Golden oracles `check_m4_clustering_no_payback_chart` / `check_m4_clustering_silhouette_grounded` → `NodeEvalInputs.m4_clustering_no_payback_ok` / `m4_clustering_silhouette_ok` → `evaluate_downgrade_gate`. Sin clave canónica/state nueva, sin `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA. **Gate LIVE obligatorio** (que la prosa enmarque valor-por-segmento es prompt-reforzado). Tests: `backend/tests/test_issue469_m4_clustering_value_frame.py`.

## ml_ds Clustering M4 chart — valores REALES por segmento + guard estructural (Issue #498, EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Detalle completo en `CLAUDE.md` (sección homónima). Cierra el Gráfico 1 de M4 (#469) que FABRICABA la distribución por segmento (`[135000,0,0,0]` o el mismo número en todos, nombres "Segmento N"): recibía solo el `{m4_content}` cualitativo y el nodo no tenía grounding clustering. **Auditoría:** anclar a `cluster_profiles` (lo que pedía la issue) es INCORRECTO — el valor por segmento es una PREMISA derivada (orden de magnitud > media RFM), anclar haría false-DROP; por eso el guard es SOURCE-AGNOSTIC y ESTRUCTURAL.
- **Prevención + garantía:** `M4_CHART_PROMPT_CLUSTERING_PROFILES` (charts.py, +`{segment_data_table}`) recibe la tabla real (`_render_segment_data_table`: `cluster_sizes` SIEMPRE + `cluster_profiles` #494) seleccionada en `m4_chart_generator` cuando hay datos (si no, base #469 byte-idéntico; sin perfiles igual alimenta sizes → nunca conserva el bug). `detect_fabricated_segment_distribution(charts, n_clusters)` (`clustering_decision.py`, puro): un trace de valor sobre ≥3 segmentos con ≤1 valor distinto no-cero = fabricación (zero-FP para el dato #452); acotado al chart de valor-por-segmento (excluye el A/B/C por traces "Opción" + ≤2 traces; identificación ESTRUCTURAL por eje de ≥3 categorías, NO conteo exacto == n_clusters — un exacto se saltaba la fabricación si el LLM dibuja otro nº de barras; n_clusters solo gatea ≥3 → el set nunca queda vacío). `_apply_clustering_m4_chart_grounding` (espeja `_apply_m4_chart_grounding`): reprompt-once-then-DROP, best-effort, logger-only + backstop de nombres/identificadores crudos.
- **Kill-switch REUSADO `MLDS_CLUSTERING_M4_VALUE_FRAME`** (prompt + guard atómicos). Off → byte-idéntico #469. Byte-idéntico para clasificación/business/regresión/serie_temporal y business+clustering. Golden oracle `check_m4_clustering_segment_differentiated` → `NodeEvalInputs.m4_clustering_segment_differentiated_ok` → `evaluate_downgrade_gate`. Sin clave canónica/state nueva, sin `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA. **Gate LIVE obligatorio** (que la prosa enmarque valores reales es prompt-reforzado). Tests: `backend/tests/test_issue498_m4_clustering_segment_grounding.py`.

## ml_ds Clustering M3 Preguntas Conceptuales (EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering` (K-Means). Detalle completo en `CLAUDE.md` (sección homónima). Las **3 preguntas CONCEPTUALES de M3** (`m3_questions_generator`, PRE-executor, numero 1/2/3; distintas de las 2 output-grounded del notebook #489/#494) eran el único rezagado de clustering sin prompt dedicado: montaban sobre el genérico SUPERVISADO `M3_EXPERIMENT_QUESTIONS_PROMPT` (hipótesis/sesgo/descarte, refs `exp.*`) solo "reformulado" por el hint #467 sobre ejemplos contradictorios (con #467 off → prompt experimental crudo para K-Means).
- **Prompt dedicado `M3_QUESTIONS_PROMPT_CLUSTERING`** (`prompts/clustering/M3_clustering/questions.py`, espeja #456/#489): segmentation-native, NO supervisado, 3 preguntas con arco no-redundante vs el notebook P4/P5 — **P1** `seg.seleccion_k` (elección de k: codo+silhouette, óptimo vs accionable), **P2** `seg.validez` (qué prep vuelve ARTEFACTO a los segmentos + silhouette BAJO CUALITATIVO, pre-exec sin número), **P3** `seg.estrategia` (estructura → decisión A/B/C del M1 + condición de descarte). Contrato de placeholders IDÉNTICO al experimento (7 claves) → contexto del nodo sin cambios; anti-fabricación en el límite del prompt (#457); el `target_k` lo aporta el hint #467 (sigue componiendo).
- **Dispatch** por helper puro `_select_m3_ml_ds_nonclf_questions_prompt(family)` (`graph.py`, espeja `_select_eda_prompt`): ml_ds+clustering gateado por `MLDS_CLUSTERING_M3_QUESTIONS` → dedicado; regresión/serie_temporal/off/unresolved → experimento genérico byte-idéntico. `m3_section_ref = seg.*` es metadata interna (allowlisted, no renderizada, no es grading key) → sin schema/frontend; el validador runtime clasificación-gated no tokeniza `seg.*` (sin falso-flag, sin tocar `m3_grounding`).
- **Kill-switch `MLDS_CLUSTERING_M3_QUESTIONS`** (Settings `mlds_clustering_m3_questions`, default `true`; `backend/.env.example`). Off → genérico + hint #467 byte-idéntico. Golden oracle `check_clustering_m3_questions_segmentation` (sin fuga supervisada + sin silhouette fabricado, reusa `check_clustering_m3_content_no_metric_numbers`) → `NodeEvalInputs.clustering_m3_questions_segmentation_ok` → `evaluate_downgrade_gate`. Sin clave canónica/state nueva, sin `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA. business/clasificación/otras byte-idénticas. M2 EDA-questions (#456) ya coherente, NO cambia. **PENDIENTE verificación LIVE** (merge ≠ deploy). Tests: `backend/tests/test_clustering_m3_conceptual_questions.py`.

## ml_ds Clustering M4/M5 Preguntas (EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering` (K-Means). Detalle completo en `CLAUDE.md` (sección homónima). Cierra el último rezagado: las **preguntas + soluciones de M4 (3, `m4_questions_generator`) y M5 (1 memo, `m5_questions_generator`)** eran las únicas superficies de pregunta de clustering sin prompt dedicado — usaban el genérico SUPERVISADO/financiero (M4 P2 "¿el valor del **modelo** justifica la inversión dado el **veredicto de M3**?"; M5 "límites del **modelo** … rol del CTO") + el hint de veredicto #467/#469. K-Means no tiene modelo predictivo/uplift/veredicto.
- **Prompts dedicados self-contained** (espejan #458/#469): `M4_QUESTIONS_PROMPT_CLUSTERING` (`prompts/clustering/M4_clustering/questions.py`) — P1 valor de un segmento, P2 priorización vs costo USD (sin uplift), P3 riesgo de segmentación batch (StandardScaler/drift), conserva `m4_section_ref ∈ {4.1..4.5}`. `M5_QUESTIONS_PROMPT_CLUSTERING` (`prompts/clustering/M5_clustering/questions.py`) — memo decide QUÉ estrategia de segmentación (Opción A/B/C), conserva VERBATIM el contrato M5 (`EXACTAMENTE 1 consigna`/`memorándum ejecutivo`/`decisión final`/100-160 palabras). Ambos: NO supervisado + silhouette SOLO el real. Placeholder contract ⊆ el del genérico (KeyError-proof).
- **Dispatch a nivel de nodo** (helpers puros `_select_m4_questions_clustering_prompt` / `_select_m5_questions_clustering_prompt`, `graph.py`, espejan `_select_m3_ml_ds_nonclf_questions_prompt`): ml_ds+clustering (STRICT) → dedicado; resto devuelve el base SIN cambios. Los dicts `M4/M5_QUESTIONS_PROMPT_BY_FAMILY` NO se mutan (siguen `clustering → genérico`). El override va antes de los hints lens/veredicto (que se apenden). El guard `_apply_clustering_m5_verdict_coherence` (#467) sigue corriendo.
- **Kill-switches `MLDS_CLUSTERING_M4_QUESTIONS` / `MLDS_CLUSTERING_M5_QUESTIONS`** (Settings homónimos, default `true`; `backend/.env.example`) → off byte-idéntico (ortogonales al hint/guard de veredicto). Sin nueva clave canónica/state, sin `case_sanitization`, sin migración, sin frontend, sin tocar el architect/SHA. Sin nuevo reprompt-guard runtime (garantía = golden oracle + hint/guard #467/#469). Golden oracles `check_clustering_m4_questions_segmentation` / `check_clustering_m5_questions_segmentation` (fuga supervisada zero-FP; NO marcan silhouette numérico — M4/M5 son POST-executor) → `NodeEvalInputs.*_ok` → `evaluate_downgrade_gate`. business/clasificación/otras byte-idénticas. **PENDIENTE verificación LIVE** (merge ≠ deploy). Tests: `backend/tests/test_clustering_m4_m5_questions.py`.

## ml_ds Clustering M1 Currency Honesty (Issue #514, EPIC #511)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Detalle completo en `CLAUDE.md` (sección homónima). P0 del EPIC #511 (coherencia M1↔dataset): la narrativa M1 pintaba dinero ABSOLUTO por-entidad/por-segmento ("$135.000/mes", "$15M") que el dataset por-entidad (`monetary_value ∈ [50,5000]`) no respalda. PREVENCIÓN vía prompt; la garantía determinista es el hermano #515. **Auditado:** "CSV normalizado" del issue es IMPRECISO (el CSV es ABSOLUTO; el defecto es de ESCALA); la regla feature-AGNÓSTICA (I3) se mantiene.
- **Hint brace-free, feature-agnóstico (I3), best-effort:** `build_clustering_currency_honesty_hint()` (`clustering_decision.py`) apendado por `_maybe_append_clustering_currency_hint(prompt, state)` (`graph.py`) en `case_architect` Y `case_writer` (las dos superficies de prosa M1). MANTIENE Exhibit 1 (P&L empresa USD); PROHÍBE $ absoluto por-entidad; pide valor por-segmento cualitativo/relativo. Append a nivel de nodo → `_MLDS_ARCHITECT_PROMPT_SHA256` intacto. NO dice "normalizado".
- **Kill-switch `MLDS_CLUSTERING_CURRENCY_HONESTY`** (Settings `mlds_clustering_currency_honesty`, default `true`; `backend/.env.example`) → off = byte-idéntico (revert env-only). Gate STRICT `profile==ml_ds AND family==clustering`, independiente de `MLDS_CLUSTERING_STRUCTURE`/`_M1_ANCHOR`. Sin nueva clave canónica/state, sin `case_sanitization`, sin migración, sin frontend. Golden oracle diferido a #515. Tests: `backend/tests/test_issue514_clustering_currency_honesty.py`. PENDIENTE verificación LIVE (merge ≠ deploy).

## ml_ds Clustering M1↔dataset Coherence Validator (Issue #515, EPIC #511)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Detalle completo en `CLAUDE.md` (sección homónima). P1 del EPIC #511 — la GARANTÍA determinista hermana de la PREVENCIÓN prompt-only #513/#514 (doctrina: prompt = defensa, validador = garantía). Módulo puro `case_generator/m1_dataset_coherence.py` (espeja `m1_grounding.py`, no importa graph; reusa `_normalize_core`/`_mag_scale`), best-effort, zero-FP-leaning, anti-ReDoS. Tres checks param-driven (I1/I2/I3 — entidad/conteo/techo por parámetro).
- **`CURRENCY_ON_FEATURE` (reprompt-grade, la garantía):** $ USD por-ENTIDAD (anclado en la raíz de entidad REAL del caso — "por centro"; NO grupo/unidad/cluster/segmento) que supera el techo monetario del dataset × `_CURRENCY_FABRICATION_FACTOR=3`, en la misma cláusula (`_clause_window`). Atrapa el defecto LIVE de #514 (`"$135.000 por centro"` vs tope 5000); solo cifras con marcador de moneda. Per-segmento NO se ancla (agregado legítimo; cubierto por el hint #514). **`ENTITY_MISMATCH`/`POPULATION_MISMATCH` (logger-only, E1):** el hint #513 ya los previene; un reprompt sería FP-prone (entidad default `cliente`) → solo se registran, nunca reescriben.
- **D3 (estructural):** `doc1_flow` (M1) corre ANTES de `eda_flow` → `doc7_dataset`/`dataset_schema` AUSENTES en tiempo-M1; el runtime ancla a deterministas (`_MLDS_CLUSTERING_MAX_ROWS` + `monetary_ceiling_from_columns` sobre `_CLUSTERING_SEGMENTATION_COLUMNS`), el golden oracle alimenta el dataset real al MISMO validador. Wiring `graph.py`: `_apply_m1_dataset_coherence_writer` (case_writer, reprompt-once-then-DEGRADE solo CURRENCY) + `_log_m1_dataset_coherence_architect` (case_architect, logger-only, `entity_descriptor` EXPLÍCITO — H3). Gate incl. `mlds_clustering_structure` (H2). SHA del architect intacto.
- **Kill-switch `MLDS_CLUSTERING_M1_COHERENCE_GUARD`** (Settings `mlds_clustering_m1_coherence_guard`, default `true`; `backend/.env.example`) → off = byte-idéntico. Sin nueva clave canónica/state/`case_sanitization`/migración/frontend (logger-only → sin fan-out merge hazard). Golden oracle `check_m1_dataset_coherence` → `NodeEvalInputs.m1_dataset_coherence_ok` → `evaluate_downgrade_gate`. Tests: `backend/tests/test_issue515_m1_dataset_coherence.py`. PENDIENTE verificación LIVE (merge ≠ deploy).

## ml_ds Clustering Domain-Adaptive Segmentation Columns (Issue #531, EPIC #511/#458)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Detalle completo en `CLAUDE.md` (sección homónima). Hace el dataset coherente con CUALQUIER dominio (educación/salud/ambiental/manufactura), no siempre la RFM genérica. Raíz: #455 dejó clustering sin contrato → dataset SIEMPRE RFM-6 fija; y si el architect emitía `feature_columns`, `_augment_schema_with_contract` las apendaba SOBRE la RFM con rangos rotos `_default_column` ([0,1]/[0,100]) → duplicados por concepto + valores absurdos (`willingness_to_pay_usd = 0.65`). Decisión de producto (usuario): columnas adaptadas al dominio.
- **Fuente única `_resolve_clustering_segmentation_columns(contract, *, domain_enabled)` (`graph.py`):** ≥3 features numéricas usables del contrato → ESAS (rango del architect o heurística `_clustering_feature_range_heuristic`) SON las columnas; si no, RFM fija. Salta str/date/`period`/`*_id`/`cluster*`/`kmeans*`, de-dup, cap 8. La consume el builder `_build_clustering_fallback_schema` Y el techo #515 (`_resolve_m1_dataset_monetary_ceiling` ahora schema-driven → iguala la feature monetaria real, ej. 800 no 5000). El rango solo afecta display (silhouette StandardScaler-invariante → estructura/silueta #452 intacta).
- **Schema:** `DatasetFeatureSpec` gana `range_min`/`range_max` OPCIONALES (aditivos, default None → cero impacto en otras familias; `model_validator` coerce-never-reject nulifica par inválido). **Fix de duplicados INCONDICIONAL:** `schema_designer` salta `_augment_schema_with_contract` en el path determinista de clustering (restaurarlo re-expondría el bug, cf. #470) → `MLDS_CLUSTERING_DOMAIN_COLUMNS=false` revierte a RFM-6 LIMPIA (g07), no al path duplicado. **Architect (sibling, SHA-safe):** `_M1_CLUSTERING_ANCHOR_ARCHITECT` emite `target_column` marcador `role: "clustering_target"` (satisface el campo requerido; nunca se materializa) + 4-8 `feature_columns` de dominio CON rangos; oráculo `check_clustering_m1_no_target_anchors` permite `clustering_target`, sigue marcando target supervisado/cost-matrix/event-rate.
- **Kill-switch `MLDS_CLUSTERING_DOMAIN_COLUMNS`** (Settings `mlds_clustering_domain_columns`, default `true`; `backend/.env.example`). Downstream column-agnostic (notebook/EDA/cluster_profiles) → seguro. Sin nueva clave canónica/state/`case_sanitization`/migración/frontend. Otras familias/business/kill-switch-off byte-idénticas. Casos ya generados NO se reprocesan. Tests: `backend/tests/test_issue531_clustering_domain_columns.py`. PENDIENTE verificación LIVE (merge ≠ deploy).

## M2 ml_ds + K-Means EDA Chart Feature Coherence (follow-on #466/#531, EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Detalle completo en `CLAUDE.md` (sección homónima). Builder data-only de #466 (`datagen/eda_charts_clustering.py`); el panel M2 ahora muestra las MISMAS features que el K-Means ajusta para CUALQUIER dominio. DETERMINISTA, solo-builder, sin LLM. Cierra el follow-up de #531 (el selector descartaba un eje de dominio con substring tipo-target en el nombre aunque el fit lo usara).
- **Fix 1 — selector espeja el strip:** `_select_clustering_feature_columns` replica `_enforce_mlds_clustering_no_target` (Reglas B/C, helpers `_is_classification_target_named`/`_is_cluster_label_named`/`_is_binary_series`): etiqueta de cluster (`cluster*`/`kmeans*`) fuera por NOMBRE a cualquier forma (Regla C, antes ausente en el selector → un `cluster_id` filtrado se graficaba con el strip off); target de clasificación fuera SOLO si binario (valores ⊆ {0,1}, Regla B) → una feature CONTINUA tipo-target (`target_segment_value`) se CONSERVA. "Binario" se infiere del DATO (sin schema). Exclusiones previas (period/`id`/no-numérica/bool/constante/>50%% nulos) intactas.
- **Fix 2 — panel coherente:** el cap `_MAX_DISTRIBUTION_FEATURES`=8 se aplica UNA vez en `generate_clustering_eda_charts` → box/heatmap/scatter muestran las MISMAS variables (antes el box capaba a 8 y el heatmap/scatter no). Byte-idéntico ≤8 (#531 ya capa a 8); coherente para #317 (>8, sin cap).
- **Sin kill-switch nuevo** (reusa `MLDS_CLUSTERING_CHARTS`, precedente #487). Sin nueva clave canónica/state/`case_sanitization`/migración/frontend/architect-SHA (mismo shape de trace → renderer/`sanitizeTraces` intactos). El strip de schema #466 sigue siendo la garantía aguas arriba; esto es defensa-en-profundidad + cierre del under-coverage de #531. Otras familias/business/kill-switch-off byte-idénticas. Casos ya generados NO se reprocesan. **LIVE node-level PASS** (Gemini Flash, dominio ambiental con `target_outreach_score`): el nodo real `eda_chart_generator` emite 4 charts coherentes, grafica la feature tipo-target, anotación data-only; regen end-to-end completo = gate final estándar (ejerce upstream sin cambios). Tests: `backend/tests/test_clustering_m2_chart_feature_coherence.py`.

## M3 Notebook Reliability & Graceful Degradation

- The M3 notebook generator (`_invoke_m3_notebook_algo_section` in `graph.py`) runs an N-attempt loop (`M3_NOTEBOOK_MAX_ATTEMPTS`, default 3 = 1 initial + 2 reprompts). Attempt 1 uses Flash (`_get_m3_notebook_llm`); reprompts ESCALATE to the Pro tier (`_get_m3_notebook_escalation_llm`, resolved via `NODE_M3_NOTEBOOK_ESCALATION`, reversible per-node). Never collapse the `.with_fallbacks` chains; the escalation factory must NOT attach `code_execution` tools (wrong for emitting long Jupytext).
- For `clasificacion`, a deterministic, fail-safe repair (`m3_notebook_repair.repair_locals_existence_guards`) rewrites the prompt-invited `if 'X' in locals()/globals()/vars()` self-bootstrap idiom into `try/except NameError` BEFORE validation, on every attempt. It is pure, total, AST-located (never `ast.unparse` of a whole cell — that would erase `# === SECTION:` sentinels and manufacture FALTANTE) and discards on any doubt (still-parses + sentinels-preserved + scrub-clean). The ban is also co-located at the Rule M self-bootstrap instruction in `prompts/clasificacion/notebooks/_shared.py` (preamble only — the executable region must stay free of the `globals()` literal so `test_issue230` holds).
- On exhaustion the loop raises the typed `M3NotebookValidationError` (a `RuntimeError` subclass) carrying `violations` + a bounded, secret-redacted `last_output`. `authoring.py` persists `debug_m3_notebook_*` into `task_payload` only when `ADAM_DEBUG_NOTEBOOK_DUMP` is set; never teacher-facing.
- Graceful degradation: a notebook that cannot be generated/executed after all retries does NOT fail the case. `m3_notebook_generator` and `m3_notebook_executor` set `m3_notebook_degraded` + a markdown placeholder (never a runtime-broken notebook), the job still COMPLETES, and the flag flows to canonical output as `m3NotebookDegraded` (allowlisted in `case_sanitization.py` + `teacher_reads.py`). The executor noops when already degraded and degrades on its own failures (missing dataset, crash after correction, blocking quality gate).
- Feature hygiene for ml_ds+clf (the executed `dummy_baseline` cell in `prompts/clasificacion/notebook.py` — the single base source for all 3 variants via `build_classification_notebook_prompt`, passed through untouched): the all-unique ID drop (`nunique == n_filas`) is scoped to NON-numeric columns. Candidate categoricals are pre-capped at `nunique <= 20`, so an unscoped `nunique < n` clause could ONLY ever fire on a NUMERIC column — i.e. a high-cardinality continuous DRIVER (`transaction_amount`/`income`/`fee` over a realistic range is all-unique under the neutral-trend deterministic generator), the column that carries the target's signal in the de-churned (#382) / directional (#506) schema. Dropping it left only noise features → AUC ≈ 0.5–0.6 shipped SILENTLY (it still clears the non-blocking 0.55 executor gate). Numeric columns are now kept regardless of cardinality (`c in _num_set_boot or nunique < n`); identifiers are still removed by NAME (`_id` token) or by being high-cardinality categorical. The K-bis markdown recipe twin matches. **Do NOT re-add the numeric all-unique drop** — it discards the only signal-bearing driver and silently ships a near-random Logistic Regression. Tests: `test_dummy_baseline_keeps_highcardinality_numeric_driver` (execs the real cell, 3 variants).
- Issue #349 — defense-in-depth target-identity guard (ml_ds+clf only, the executor's existing gate). The executed `metrics_summary_json` cell now also emits the modeled `target_col` (the contract-first value `dummy_baseline` resolved, #348) into `ADAM_M3_METRICS_SUMMARY_JSON` — a key added INSIDE the cell, so the `# === SECTION:metrics_summary_json ===` sentinel/parser contract is intact (parser accepts any keys). The pure helper `build_target_identity_warning(metrics_summary, expected_target)` (in `m3_notebook_execution.py`) returns `m3_quality_target_mismatch:` when the modeled target differs from the declared contract target (`expected_target = _safe_contract_target_name(state.get("dataset_schema_required"))`, resolved in `_run_m3_notebook_execution`). `is_m3_quality_warning_blocking` classifies that kind as BLOCKING, so it rides the SAME reprompt-once-then-RuntimeError-then-degrade loop (no new loop). The AUC gate stays NON-blocking. The bug is already PREVENTED in production by #348/#382 (modeled == declared in the happy path), so this is a pure anti-regression guard for a future double-fault; the failing test is SYNTHETIC. Degrades clean (returns `None`, never blocks) when `expected_target` is empty (no contract / non-identifier name → #348 alias path is sanctioned), metrics are missing, the run declares an intentional modeling skip (mirrors the `auc_missing` skip rule — no model shipped on any column), or modeled == declared. `target_col` is an executor-internal signal: it is STRIPPED (non-mutating copy) from the persisted `m3_metrics_summary` so the M4/M5 grounding block (`build_computed_metrics_block`) stays byte-identical for all ml_ds+clf (churn included). Tests: `backend/tests/test_issue349_target_identity_guard.py`.
- Regenerate: `POST /api/authoring/jobs/{job_id}/regenerate-notebook` (owner-checked, 400 if not degraded) re-runs ONLY the notebook via the production generator+executor nodes and patches `assignment.canonical_output` on success; a second failure leaves it degraded for retry. Minimal regen inputs are snapshotted into `task_payload["m3_notebook_regen_inputs"]` at completion. Supabase-native — no new bus/table. Frontend wires the "Regenerar notebook" button via an optional `onRegenerateNotebook` callback (teacher generation preview only).

## Cost Controls & Per-Node Model Routing

- Model tier is selected **per node**, not globally. `case_generator.configuration.resolve_node_model(cfg, node_name, default)` returns `cfg.node_model_overrides.get(node_name, default)`; the `default` passed at each call site is the node's committed baseline tier (`architect_model` Pro or `writer_model` Flash). Override a single node via the `NODE_MODEL_OVERRIDES` env var (JSON object) or `run_config["configurable"]["node_model_overrides"]` (canary by % of jobs without redeploy). Node-name keys are the `NODE_*` constants in `configuration.py`.
- The seven Pro-capable nodes are `case_architect`, `schema_designer`, `m3_content_generator` (ml_ds), `m3_notebook_generator`, `m4_content_generator`, `m5_content_generator`, `m5_questions_generator`. Their model is resolved via `resolve_node_model`. Do not re-hardcode model strings inside these nodes (the previous hardcodes in `schema_designer` and `_M5_MODEL` were removed for reversibility).
- All LLM clients are built by `_build_gemini(...)` in `graph.py` (the single base that owns `api_key`, the shared `_rate_limiter`, and `max_retries`); the six tier factories (`_get_writer_llm`, `_get_architect_llm`, `_get_m4_llm`, `_get_chart_llm`, `_get_m5_llm`, `_get_m3_notebook_llm`) are thin wrappers. **Never collapse the `.with_fallbacks` chains** — a downgrade changes only the primary, never the resilience net.
- Committed Fase 1 cost changes: `case_architect` and `m4_content_generator` run `thinking_level="medium"` (was `high`); `m3_notebook_generator` defaults to **Flash** (`writer_model`) because `m3_notebook_executor` already gates it on real execution + AUC ∈ [0.55, 0.99] plus the family-consistency reprompt. `schema_designer`, `m3_content_generator`, and `m5_questions_generator` stay on Pro until they pass the golden-set eval gate (Fase 2).
- Cost instrumentation: `case_generator.cost_metrics.CostCallbackHandler` is attached once per job at the top-level graph config in `core/authoring.py`; callbacks propagate to every child LLM call and attribution is by LangGraph's injected `metadata["langgraph_node"]`. It is **best-effort** — it swallows every exception and must never fail a job. The per-node `{input, output, thinking, cached_input, usd}` breakdown is flushed into `authoring_jobs.task_payload["cost_breakdown"]` at job completion (Supabase-native write path — do not add a new bus/SSE/table). Prices live in `cost_metrics.PRICE_MAP` (placeholder rates — confirm against live Gemini pricing; token counts are exact regardless).
- The Fase 2 downgrade gate is `tests/golden_eval.py::evaluate_downgrade_gate`: a node may move to Flash only if deterministic oracles pass on 100% of the frozen golden set, the LLM-as-judge mean drop ≤ 0.30 (5-pt), pairwise Pro-win ≤ 0.70, and (for `schema_designer`) the AUC distribution does not degrade toward the floor. The live Pro-vs-Flash run is a `live_llm` harness (`RUN_LIVE_LLM_TESTS=1`).

## OpenRouter Provider Routing — M5 minimax-m3 (trial)

- A model-id dispatcher `_build_llm(model, *, gemini_fallback_model=...)` in `graph.py` routes any id containing `/` (e.g. `minimax/minimax-m3`, `z-ai/glm-5.2`) to OpenRouter via `_build_openrouter` (`langchain_openai.ChatOpenAI`, `base_url=https://openrouter.ai/api/v1`), and every other id (real `gemini-*` ids and synthetic test ids like `pro-x`) to `_build_gemini`. It is wired into `_get_m5_llm`, the now provider-aware `_get_writer_llm`, the new `_get_m3_content_llm`, and the `schema_designer` OpenRouter branch (see the GLM-extension section below); the remaining factories stay on `_build_gemini`. Activation is per-node via `NODE_MODEL_OVERRIDES` (e.g. `{"m5_content_generator":"minimax/minimax-m3","m5_questions_generator":"minimax/minimax-m3"}`); removing it reverts to 100% Gemini, byte-identical (the new code is inert without a `/` id).
- **Resilience invariant (load-bearing):** the tier factories build all tiers EAGERLY before `.with_fallbacks`, so a construction-time raise from `_build_openrouter` would destroy the fallback chain. Therefore `_build_llm` wraps the OpenRouter build in try/except and DEGRADES to `_build_gemini(gemini_fallback_model)` on ANY failure (missing `OPENROUTER_API_KEY`, `langchain_openai` not installed — the import is lazy, construction error). A provider misconfig falls cleanly to Gemini (the intended fallback); a node never hard-fails for it. Do NOT let a `_build_openrouter` raise reach `_get_m5_llm` directly.
- Cross-provider chain for a minimax M5: `minimax → minimax → gemini-3-flash-preview → gemini-2.5-flash`. `.with_structured_output(GeneradorPreguntasM5Output)` is called on the `RunnableWithFallbacks` (graph.py call site unchanged); langchain-core's `RunnableWithFallbacks.__getattr__` maps it per-model so minimax and Gemini each bind their NATIVE structured-output method (verified against installed langchain-core 1.2.23 + langchain-openai 1.1.12). minimax structured output is provider-gated → `extra_body.provider.require_parameters=true` routes only to a capable upstream (e.g. Together/Parasail/Morph); the default `function_calling` method is kept.
- **minimax is a reasoning model**, so reasoning tokens count against `max_tokens` and are billed at the output rate. `_build_openrouter` sets `extra_body.reasoning.max_tokens` (`OPENROUTER_REASONING_MAX_TOKENS`, default 4000) and `max_tokens = answer_budget + reasoning_cap` so reasoning cannot truncate the visible answer — critical because a truncated `m5_content` decision matrix fails the 2nd validation → `RuntimeError` → job fail (the Gemini fallback cannot rescue a valid-but-truncated 200). It also sets a per-request `timeout` (`OPENROUTER_TIMEOUT_SECONDS`, default 150) so a stalled call falls to Gemini well before the ~1900s job deadline, a DEDICATED `_openrouter_rate_limiter` (NOT the Gemini `_rate_limiter`, sized independently), and `extra_body.usage.include=true`.
- `m5_questions_generator`'s transient re-raise now uses `_is_transient_llm_error(e)` (recognizes openai `.status_code` 429/5xx in addition to the Gemini string markers; **402 = no credits is NON-transient → degrades, with a distinct credit-exhaustion warning**). `m5_content_generator`'s error handling is unchanged (stays Gemini-byte-identical).
- Cost: `cost_metrics.PRICE_MAP["minimax/minimax-m3"]` = `$0.30/$1.20` (`_MINIMAX_M3_RATES`, `cached==input`), plus a `"minimax"` substring arm in `_rates_for_model` so a provider-suffixed slug (`…:together`) still prices. Operators detect fallback via the cost-breakdown `models` set (>1 model id = a fallback fired).
- **Provider routing (the real 404) + data governance:** the M5 prompt carries teacher-derived content to THIRD-PARTY OpenRouter upstreams. **Real 404 root cause (verified against the live API, NOT data_collection):** langchain `ChatOpenAI` serializes its `max_tokens=` field as `max_completion_tokens`, a param minimax-m3's providers do not list; under `provider.require_parameters=true` OpenRouter returns `404 "No endpoints found that can handle the requested parameters"` and the chain silently falls back to Gemini. `_build_openrouter` therefore sends `max_tokens` via `extra_body` (the param every provider supports), NEVER the `ChatOpenAI(max_tokens=...)` field — `test_build_openrouter_does_not_serialize_max_completion_tokens` guards this. `require_parameters` (default `true`, `OPENROUTER_REQUIRE_PARAMETERS`) routes structured output (m5_questions) to a capable upstream (Together/Parasail/Morph). `provider.data_collection` is OPT-IN via `OPENROUTER_DATA_COLLECTION` and omitted by default — a routing constraint must not be a hardcoded default (when set, `"deny"` restricts to no-collection/no-training upstreams — NOT zero-retention; account-level ZDR is separate; for minimax-m3 `"deny"` actually returns 200, so it was never the 404 cause). Broad rollout needs stakeholder sign-off. Roll out via canary (`NODE_MODEL_OVERRIDES` on a small % of jobs) + a `live_openrouter` A/B, not a fleet-wide flip.
- **Dependency:** `langchain-openai` is pinned with `langchain-core` kept on the audited 1.2.x line (`langchain-openai>=1.3` would pull `langchain-core>=1.4.7`, bumping core app-wide — out of scope for this M5-only trial). Both `OPENROUTER_API_KEY` and `GEMINI_API_KEY` are required when minimax is active (Gemini is the fallback, and graph.py raises at import without `GEMINI_API_KEY`). Env knobs live in `backend/.env.example`.

## OpenRouter Provider Routing — GLM extension (schema_designer + M3 content/questions)

- Extends the M5 OpenRouter backbone (above) to three more nodes with the active model `z-ai/glm-5.2` (already in `cost_metrics.PRICE_MAP` + the `"glm"` substring arm of `_rates_for_model`). Activation is per-node via `NODE_MODEL_OVERRIDES` (e.g. `{"schema_designer":"z-ai/glm-5.2","m3_content_generator":"z-ai/glm-5.2","m3_questions_generator":"z-ai/glm-5.2"}`); with no override every node is byte-identical to Gemini (the new code is inert without a `/` id). The minimax/`max_tokens`-via-`extra_body` 404 fix, `_is_transient_llm_error`, the dedicated `_openrouter_rate_limiter`, and the reasoning-token handling are REUSED unchanged.
- **`m3_questions_generator`** gained the missing per-node hook: a new `NODE_M3_QUESTIONS` constant (`configuration.py`) + `m3q_model = resolve_node_model(cfg, NODE_M3_QUESTIONS, cfg.writer_model)`; it calls the now **provider-aware `_get_writer_llm`** (its primary routes through `_build_llm`, gemini_fallback `gemini-2.5-flash`). `_get_writer_llm` stays byte-identical for every current caller (none pass a `/` id — `test_writer_factory_parity` holds). The `.with_structured_output(GeneradorPreguntasOutput)` call site + the `_apply_m3_questions_coherence` reprompt are cross-provider-safe (unchanged).
- **`m3_content_generator`** routes ONLY the ml_ds (Pro narrative) branch via the new `_get_m3_content_llm(resolve_node_model(cfg, NODE_M3_CONTENT, ...), cfg.writer_model)` (GLM/medium → GLM/low → Gemini flash/low). The business audit branch stays Gemini Flash (`_get_writer_llm(cfg.writer_model)`, no `/`). Free-text node; grounding is structurally disabled at m3_content (#336, `variant=None`) so the `RuntimeError` reprompt path cannot fire → no GLM job-fail vector.
- **`schema_designer`** uses TWO explicit branches gated by `"/" in resolved_model`: the OpenRouter branch builds an **rmt-free** chain via `_build_llm` and calls `.with_structured_output(DatasetSchema)` (a bad/truncated output raises → `.with_fallbacks` advances to Gemini → only then the loop `except` → deterministic `_build_fallback_schema`); the Gemini branch keeps the EXISTING inline build (`response_mime_type="application/json"` + manual `_extract_json_from_llm_response`) **byte-identical** (golden AUC gate untouched). The rmt-free OpenRouter chain is load-bearing: passing `response_mime_type` there would conflict with the structured-output tool when `_build_llm` degrades an in-chain tier to Gemini. The #477 clustering short-circuit (LLM skipped) is unchanged → GLM never reaches clustering.
- **Per-node error policy (graceful degrade preserved — NO new failure modes):** all three nodes keep their PRE-GLM error handling byte-identical, because GLM resilience rides entirely on the `.with_fallbacks` cross-provider chain (a transient GLM error falls to Gemini WITHIN the node). `schema_designer` degrades to the deterministic fallback schema; `m3_content_generator` degrades to `[M3_NOT_EXECUTED]` (honoring the documented M3 graceful-degradation contract — M3 degrades, never fails the case); `m3_questions_generator` degrades to `[]`. A transient re-raise was deliberately NOT added (it would deviate from `m5_content`'s byte-identical degrade and add a job-fail vector the routing does not need). Each node additionally logs a distinct **402 (no-credits)** warning for ops observability.
- **Production risks (documented; gate before fleet rollout):** R1 silent 404→Gemini if no GLM upstream supports tools/JSON under `require_parameters=true` (detect via `cost_breakdown.models` containing `z-ai/glm-5.2`; pin `OPENROUTER_PROVIDER_ORDER` if needed); R2 structured-output truncation under `OPENROUTER_REASONING_EFFORT=high` (watch `schema_designer`; bump its `max_output_tokens` if truncating); R3 reasoning cost/latency ~2.5× (2→5 GLM nodes, m3_questions overpays under global high reasoning — per-node reasoning is a documented follow-up); R4 quality (run `tests/golden_eval.py` oracles + manual E2E on GLM, mirroring the Fase 2 gate, before fleet). Tests: `backend/tests/test_glm_node_routing.py` (+ the M5 `test_openrouter_routing.py` / `test_cost_model_routing.py`).

## OpenRouter Provider Routing — m4_content_generator Gemini 3.1 Pro (Issue #492)

- Extends the OpenRouter backbone to `m4_content_generator` with the active model `google/gemini-3.1-pro-preview` (Gemini 3.1 Pro **via OpenRouter** instead of the direct Gemini API). Activation is per-node via `NODE_MODEL_OVERRIDES` (add `"m4_content_generator":"google/gemini-3.1-pro-preview"`); with no override every node is byte-identical to Gemini (inert without a `/` id). The `max_tokens`-via-`extra_body`, `_is_transient_llm_error`, the dedicated `_openrouter_rate_limiter`, and the global reasoning config are REUSED unchanged — no global config change (`reasoning.effort`→`thinkingLevel` for Gemini 3, so `OPENROUTER_REASONING_EFFORT=high` is correct).
- **`_get_m4_llm`** is provider-aware (mirror of `_get_m5_llm`): the two Pro tiers carrying the resolved `model` (`primary` medium, `pro_fallback_low` low) route through `_build_llm`. m4 is free-text; `_invoke_narrative_with_grounding` (reprompt-once-then-`RuntimeError`, ml_ds+clf only) and the `[M4_GENERATION_ERROR]` degrade are UNCHANGED — no new failure mode.
- **CRITICAL NODE — ALWAYS Pro, NEVER Flash (follow-up fix).** M4 is the terminal executive narrative, so the WHOLE fallback chain stays Pro: net = direct Gemini Pro (`fallback_model`) → GA-stable `gemini-2.5-pro` (was `gemini-3-flash-preview` → `gemini-2.5-flash`); the `_build_llm` build-failure degrade uses `gemini_fallback_model` = direct Gemini Pro. Call site passes `cfg.architect_model` (Pro) as `fallback_model` (was `cfg.writer_model`, Flash). Cost trade-off accepted (a fallback bills at Pro rates) so m4 never ships a Flash narrative. Pro guarantee assumes resolved `model`/`architect_model` are Pro tiers.
- **Cost:** `cost_metrics.PRICE_MAP["google/gemini-3.1-pro-preview"]` = `$2/$12` (`_OPENROUTER_GEMINI_PRO_RATES`, `cached_input==input`) + a `"google/" + "pro"` arm in `_rates_for_model` (BEFORE the `"pro"` arm) for versioned/suffixed slugs; the direct `gemini-3.1-pro-preview` id keeps `$1.25/$10`. This is a cost **increase** vs direct Gemini, and both tiers reason at the GLOBAL `effort=high` (the Fase-1 medium cut applies only on the direct-Gemini path).
- **Out of scope (deliberate):** `case_architect` (needs Code Execution → ADR) and `m3_notebook_generator_escalation` stay Gemini-only — the escalation emits a long Jupytext notebook and under `effort=high` reasoning consumes ~80% of the combined `max_tokens`, cutting its output budget ~3× and truncating the notebook it exists to fix.
- **Production risks (gate before rollout):** R1 m4 truncation under `effort=high` (for ml_ds+clf a truncation can fail grounding → `RuntimeError` → job fail — LIVE-gate `finish_reason=stop`; lever = raise global `OPENROUTER_REASONING_MAX_TOKENS`); R2 on config error / missing key / timeout m4 degrades to **direct Gemini Pro** (same quality, NOT Flash after the critical-node fix; observe via `cost_breakdown.models`); R3 shared `_openrouter_rate_limiter` now carries up to 6 nodes/job (lever `OPENROUTER_REQUESTS_PER_SECOND`); R4 reporting-only mispricing if OpenRouter returns a bare `gemini-3.1-pro-preview`. **merge ≠ deploy** — set the override only post-deploy. Tests: `backend/tests/test_issue492_m4_gemini_openrouter_routing.py` (+ parity locks in `test_cost_model_routing.py` / `test_issue243_narrative_grounding.py`).

## Forbidden Patterns

- Secrets, tokens, keys, DSNs, or credentials committed to the repo
- New business logic hidden inside migrations, routers, or prompt strings
- Cross-domain imports that bypass the current ownership boundaries
- Reopening generic frontend folders such as `components`, `pages`, `hooks`, `helpers`, `common`, or `misc`
- Cosmetic-only edits in sensitive authoring files without functional justification

## Nested Guidance

More specific rules live in:

- `backend/AGENTS.md`
- `backend/src/case_generator/AGENTS.md`
- `frontend/AGENTS.md`

Files closer to the working directory are intended to add or override guidance for that area.

## Local Dev Auth

Cuando el cambio toque setup local o auth:

- usa `docs/runbooks/local-dev-auth.md` como fuente canonica
- distingue siempre los dos planos locales:
  - app DB del repo por `docker compose` en `5434`
  - auth/session local por `supabase start` en `54321`
- no documentes `5432` como puerto host local por defecto del repo
- no permitas ejemplos donde `DATABASE_URL` apunte al Postgres interno de Supabase local en `54322`

## Coding Guidelines (Karpathy Skills)

These cross-cutting working principles mirror the `andrej-karpathy-skills` plugin (`karpathy-guidelines`) enabled for this repo via `.claude/settings.json`. They are restated here so they stay **always-active** for every session and agent, even when the skill is not explicitly invoked. They bias toward caution over speed; for trivial tasks, use judgment. Merge with the project-specific rules above — where a repo rule and a guideline conflict, the repo rule wins. (Kept in sync with `CLAUDE.md`.)

### Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them — don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

### Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

### Surgical Changes

**Touch only what you must. Clean up only your own mess.**

- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it — don't delete it.
- Remove imports/variables/functions that YOUR changes made unused; don't remove pre-existing dead code unless asked.

The test: every changed line should trace directly to the user's request.

### Goal-Driven Execution

**Define success criteria. Loop until verified.**

- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan with a verify check per step. Strong success criteria let you loop independently; weak criteria ("make it work") require constant clarification.

**These guidelines are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.
