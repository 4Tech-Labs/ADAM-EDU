# AGENTS.md

## Critical Scope

This directory contains the core teacher authoring logic: prompts, graph execution, schemas, and orchestration used to generate the final case output.

## Working Rules

- Only change files here when the work is directly justified by authoring behavior.
- Prefer minimal, behavior-driven edits over broad refactors.
- Do not perform cosmetic cleanup in sensitive files unless it is required by the functional change.
- Keep existing contracts stable unless the change explicitly includes the required frontend and documentation updates.

## Prompt and Graph Safety

- Never embed secrets, credentials, DSNs, or raw environment values in prompts.
- Treat user-provided text as untrusted input before it crosses an LLM boundary.
- Preserve or strengthen sanitization when changing prompt assembly or payload injection.
- Prefer structured, explicit state transitions over hidden side effects in graph execution.

## High-Sensitivity Files

- `graph.py`
- `prompts.py`
- `core/authoring/**`

These files require extra caution because small edits can change generated output quality, safety posture, and job execution behavior.

## Algorithm Catalog (Issue #233)

- `suggest_service.ALGORITHM_CATALOG` is the single source of truth for the 4×2 algorithm taxonomy: `clasificacion`, `regresion`, `clustering`, `serie_temporal` (8 entries total: 1 baseline + 1 challenger per family for `ml_ds`; baselines only for `business`).
- Public helpers exposed for downstream consumers: `family_of(name)`, `resolve_legacy_family(name)`, `get_dispatch_meta(family)`, `FAMILY_LABELS`, `FAMILY_META`.
- The legacy `ALGORITHM_REGISTRY` dict in `graph.py` has been removed. Do not reintroduce a parallel registry — extend the catalog instead.
- Adding a new family or breaking the 4×2 invariant requires an ADR.

## M3 Notebook Per-Family Dispatch (Issue #233)

- `m3_notebook_generator` resolves a single family from the algorithm picks and dispatches to `prompts.PROMPT_BY_FAMILY[family]`, except `clasificacion`, where Issue #230 selects `lr_only`, `rf_only`, or `lr_rf_contrast` from `CLASSIFICATION_NOTEBOOK_PROMPT_BY_VARIANT` using `algorithm_mode` plus `algoritmos`. The public classification prompt alias remains the contrast/default prompt for backwards compatibility.
- Post-LLM, `_validate_notebook_family_consistency(family, code, notebook_variant=None)` enforces the per-family forbidden-token list (`_FAMILY_PROHIBITED_PATTERNS`). For classification variants it also enforces variant-specific required sentinels/APIs and rejects executable references to the unselected LR/RF model. On violation: reprompt ONCE with the explicit list; on second violation: raise `RuntimeError` to fail the job. Never ship a notebook that mixes families or selected-model scope.
- Legacy algorithm names (XGBoost, Ridge, NLP, etc.) in historical `task_payload` rows are mapped via `resolve_legacy_family`. Unknown names fall back to `clasificacion` and emit a warning into the data-gap block.
- Issue #240 amplía `_FAMILY_REQUIRED_SENTINELS["clasificacion"]` con `tuning_lr/tuning_rf/interp_lr/interp_rf` y `_FAMILY_REQUIRED_APIS["clasificacion"]` con `GridSearchCV/RandomizedSearchCV/permutation_importance/PartialDependenceDisplay`. Las celdas declaran modo rápido por tamaño en cascada de mayor a menor (orden importa para alcanzabilidad: >5000 ⊂ >2000): `>5000 → SKIP tuning`, `>2000 → cv/n_iter reducidos`, `≤ 2000 → grilla completa`., guard `is_binary` y self-bootstrap. VIF se calcula sin `statsmodels` (fallback `1/(1-R²)` con `LinearRegression`). SHAP NO se duplica en `interp_rf`: vive en la Regla J global. Cero cambios a otras familias ni a `_FAMILY_PROHIBITED_PATTERNS`.
- Issue #239 adds `m3_notebook_executor` after `m3_notebook_generator` and before `m3_sync`. It runs only for `studentProfile == "ml_ds"`, `output_depth == "visual_plus_notebook"`, and family `clasificacion`; other families/business noop and must not receive classification metrics or warnings. The executor uses `nbclient` in a subprocess with a minimal env, `TemporaryDirectory` HOME/cwd, no `shell=True`, hard timeout, AST scrub, and one execution-crash correction pass. `_FAMILY_REQUIRED_SENTINELS["clasificacion"]` includes `# === SECTION:metrics_summary_json ===`, which must remain atomic with the executor/parser that consumes `ADAM_M3_METRICS_SUMMARY_JSON=`.

## ml_ds Clustering Structure (Issue #452)

- For `studentProfile == "ml_ds"` + family `clustering` (K-Means), the synthetic dataset is given REAL latent structure so K-Means discovers genuine, interpretable segments instead of partitioning a unimodal cloud. Two gated pieces (kill-switch `MLDS_CLUSTERING_STRUCTURE`, default true):
  - `SCHEMA_DESIGNER_PROMPT_CLUSTERING` + `_build_clustering_fallback_schema` give an entity-level SEGMENTATION schema (interpretable features, no churn/retention/financial time-series panel).
  - `_enforce_mlds_clustering_structure` (pure copy-on-write, deterministic local RNG) runs in `data_generator` AFTER `_generate_dataset_from_schema` and rewrites the scalable numeric features into K∈{3,4} separable Gaussian blobs (permuted evenly-spaced centers, `_CLUSTERING_BLOB_SPREAD_FRAC`).
- Gate is `profile=="ml_ds" AND family=="clustering"` → clasificación/regresión/serie_temporal/business are byte-identical (same object). The latent blob label is generation-only (NOT persisted to `doc7_dataset`). Does NOT touch the M1 architect (hash frozen). No new canonical key, no `case_sanitization` entry, no migration, no frontend change.
- Deterministic golden oracle `golden_eval.check_clustering_structure` (StandardScaler+KMeans+silhouette+ARI, no LLM/key — scikit-learn is a backend dep) → `NodeEvalInputs.clustering_structure_ok` → `evaluate_downgrade_gate`; RED control = kill-switch off → unimodal → fails. Do NOT change `_CLUSTERING_BLOB_SPREAD_FRAC` without re-running `tests/test_issue452_clustering_structure.py`. Follow-ups (execution + metrics + K-Means-only notebook + M1/M2/M3c specialization) are #453–#457.

## ml_ds Clustering Executor + Quality Gate (Issue #453, extends #239)

- The ml_ds + clustering M3 notebook is now EXECUTED + quality-gated (was clf-only). `m3_notebook_executor` gates on `profile=="ml_ds" AND family in _m3_executor_families()` = `{clasificacion}` ∪ (`{clustering}` if `settings.mlds_clustering_executor`) — an ALLOWLIST, never `if profile!="ml_ds"` (that would wrongly execute regresion/serie_temporal). Kill-switch `MLDS_CLUSTERING_EXECUTOR` (default true) → off = clustering generates but is not executed (byte-identical).
- `execute_m3_notebook(..., family=...)` routes the quality warning: clustering → `build_clustering_quality_warning` (silhouette floor 0.25, `n_clusters≥2`, both via `_finite_metric` since `_clean_json_value` floats ints); classification → `build_m3_quality_warning` (AUC). `build_target_identity_warning` (#349) is classification-only (clustering has no target). Blocking semantics (`is_m3_quality_warning_blocking`): silhouette_missing (unless intentional skip) + clusters_degenerate BLOCK (reprompt-once-then-degrade); silhouette_low is NON-blocking (degrade). The subprocess/AST-scrub/marker-parser is family-agnostic (sklearn is allowed).
- Generation: `_FAMILY_REQUIRED_SENTINELS["clustering"]` = the `metrics_summary_json` sentinel; `_FAMILY_REQUIRED_APIS["clustering"]` = `StandardScaler`/`silhouette_score` (KM/DBSCAN-universal; `KMeans` requirement is #454). The clustering prompt gains a verbatim `# === SECTION:metrics_summary_json ===` cell emitting silhouette/n_clusters/cluster_sizes (always prints the marker → never marker_missing; on a degenerate `<2`-cluster result or an exception it emits `modeling_status="execution_error"` — NOT an intentional skip — so the gate BLOCKS → reprompt-then-degrade rather than shipping a silent non-degraded success). No new canonical key, no `case_sanitization`/migration/frontend change; `m3_metrics_summary` (internal) feeds #457 grounding. Tests: `tests/test_issue453_clustering_executor.py`.

## ml_ds Clustering M1 Segmentation Anchor (Issue #455)

- For `studentProfile == "ml_ds"` + family `clustering`, the M1 nodes (`case_architect`, `case_writer`, `case_questions`) now select dedicated SEGMENTATION prompts instead of the generic ones. The dilemma is framed as a segmentation decision and the A/B/C options as segmentation strategies (not classifier thresholds), coherent with the RFM/behavioural dataset of #452. Prompts live in `prompts/clustering/M1_clustering/{architect,writer,questions}.py` as `GENERIC_BASE + _M1_CLUSTERING_ANCHOR_*` (DRY single-source; anchors are brace-free + use only confirmed context keys), exported as `CASE_*_PROMPT_CLUSTERING` and added under the `"clustering"` key of the three `CASE_*_PROMPT_BY_FAMILY` dicts.
- Dispatch is gated by the DRY helper `_resolve_m1_prompt_family(context)` (`graph.py`): returns `primary_family` except `""` (→ generic) when `family=="clustering"` and (`profile!="ml_ds"` OR kill-switch off). Strict `== "clustering"` (the ml_ds-no-algoritmos cohort resolves to `clasificacion` and is untouched). Used at the architect/writer/questions selection sites; the architect business-surgery gate keeps using the raw `family`.
- **No supervised anchors:** clustering is unsupervised, so the architect prompt FORBIDS `target_column`, `business_cost_matrix`, `target_event_rate` (the clf-gated validators already no-op for clustering; the gateless `_augment_schema_with_contract` makes the prompt-level prohibition load-bearing). `_sanitize_pregunta_eje` is extended (kill-switch-gated) so an ml_ds+clustering segmentation `pregunta_eje` survives into the questions prompt; `_issue242_contract_required` stays clf-only (clustering never hard-fails on a missing eje). M1 does NOT touch the data layer (#452/#453 own it).
- Kill-switch `MLDS_CLUSTERING_M1_ANCHOR` (Settings `mlds_clustering_m1_anchor`, default `true`; `backend/.env.example`) → off reverts to the generic M1 prompts + nulled pregunta_eje byte-identically (instant env-only revert). Byte-identical for clasificación/regresión/serie_temporal/business (incl. business+clustering); the architect SHA `_MLDS_ARCHITECT_PROMPT_SHA256` is untouched (the clustering prompt is a sibling constant + dict key, never edited into the shared base/anchor; `_sanitize_pregunta_eje` is post-processing, not part of `_assemble_architect_prompt`). Deterministic golden oracle `golden_eval.check_clustering_m1_no_target_anchors` → `NodeEvalInputs.clustering_m1_no_target_ok` → `evaluate_downgrade_gate` (RED control: a contract with any supervised anchor fails). Tests: `tests/test_issue455_clustering_m1_anchor.py`. Follow-ups: #456 (M2 EDA), #457 (M3-content + grounding), M4/M5/M6 (Fase 3).

## ml_ds Clustering M2 EDA (Issue #456, EPIC #458 P1)

- For `studentProfile == "ml_ds"` + family `clustering`, the M2 EDA narrative (`eda_text_analyst`) + the 2 Socratic questions (`eda_questions_generator`) now select dedicated DATA-ONLY, PRE-MODEL, TARGET-FREE clustering prompts instead of the generic (target-oriented) ones: why standardize before K-Means (scale/distance → `StandardScaler`), spread of the #452 RFM features, correlation/redundancy → PCA motivation, cluster tendency (no asserted k). **CORE = prompts only**; EDA charts are unchanged (still the LLM-JSON path; a deterministic `_eda_clustering_python_path` builder is a follow-up coordinated with #317). Prompts in `prompts/clustering/M2_clustering/{eda_text,eda_questions}.py` (`EDA_TEXT_ANALYST_PROMPT_CLUSTERING`, `EDA_QUESTIONS_GENERATOR_PROMPT_CLUSTERING`), imported directly by `prompts/__init__.py` (the #455 `prompts/clustering/__init__.py` is untouched). The text contract is a 14-placeholder subset of the generic (no `{target_column_name}`/class blocks → KeyError-proof; data dictionary lists ALL present variables, not a fixed 8); the questions JSON block byte-mirrors the classification one (`solucion_esperada` is `str`) so structured output never crashes.
- Dispatch via the DRY helper `_select_eda_prompt(state, primary_family, *, by_family, generic, clustering_override)` (`graph.py`): `settings.mlds_clustering_m2_eda AND _is_ml_ds_clustering(state)` → clustering prompt; otherwise the existing family dispatch byte-identically. No `clustering` key is added to `EDA_*_PROMPT_BY_FAMILY` (that dispatch is profile-agnostic → would break business). `_is_ml_ds_clustering` is STRICT (`default_unresolved=False`, mirrors #452's gate). **business+clustering stays on the generic prompt** (byte-identical; #317's lane); the M2 #414 questions-coherence guard is a no-op for clustering (clf-gated).
- Kill-switch `MLDS_CLUSTERING_M2_EDA` (Settings `mlds_clustering_m2_eda`, default `true`; `backend/.env.example`) → off selects the generic EDA prompt byte-identically (instant env-only revert). Byte-identical for clasificación/regresión/serie_temporal/business. Golden oracle `golden_eval.check_clustering_eda_no_target` → `NodeEvalInputs.clustering_eda_no_target_ok` → `evaluate_downgrade_gate` (n/a on the current golden set — no clustering EDA narrative fixture; the deterministic teeth are the unit RED/GREEN). No new canonical/state key, no `case_sanitization`, no migration, no frontend. Tests: `tests/test_issue456_clustering_m2_eda.py`.

## ml_ds Clustering M3-content Narrative (Issue #457, EPIC #458 P1)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. The M3-content narrative (`m3_content_generator`, ml_ds → "Architect Engineer") used the generic `M3_EXPERIMENT_PROMPT`; it now selects `M3_CONTENT_PROMPT_CLUSTERING` (K-Means segmentation: how k is chosen via elbow + silhouette-vs-k in the abstract, qualitative silhouette reading, cluster→persona profiling, action per segment). Does NOT touch data/schema (#452), executor/notebook (#453/#454), M1 (#455), or M2 (#456).
- **Grounding scope correction (#336):** the issue body asked for a runtime validator against `m3_metrics_summary`, but by graph order `m3_content_generator` runs BEFORE `m3_notebook_executor`, so `m3_metrics_summary` is ALWAYS absent here (the #336 case: `reason="missing"`, `log.info`, no state warning). Anti-fabrication is therefore enforced at the PROMPT boundary (it forbids citing concrete metric values — silhouette/Davies-Bouldin/inertia/segment sizes); a runtime validator cannot fire. Real numeric anchoring of cluster metrics is a downstream M4/M5 follow-up (post-executor, Phase 3).
- **Prompt** (`prompts/clustering/M3_clustering/content.py`, mirrors `M3_clasificacion/content.py`): `M3_CONTENT_PROMPT_CLUSTERING = M3_EXPERIMENT_PROMPT + _M3_CLUSTERING_SEGMENTATION_BLOCK`. The block is BRACE-FREE → the placeholder contract == the base's 5 keys. OMITS `_NARRATIVE_GROUNDING_CLASSIFICATION_BLOCK` (`{computed_metrics_block}` is clf-only) and uses no `{target_column_name}`. Imports the base from `_shared` (circular-import constraint). Exported via `prompts/clustering/__init__.py`.
- **Gated dispatch** (pure helper `_effective_m3_content_prompts(primary_family)`, `graph.py`): the `else` arm of the ml_ds selection in `m3_content_generator` (clasificacion stays inline — it needs the resolved `_variant`) delegates to the helper, which overrides ONLY the `clustering` key of an `M3_CONTENT_PROMPT_BY_FAMILY` copy when `settings.mlds_clustering_m3_content`; otherwise returns the SAME object (identity → byte-identical). Pure (no LLM/state) → the kill-switch gate is unit-testable without the node's inline LLM + error-swallowing try/except (a `.format` error would silently degrade to `[M3_NOT_EXECUTED]`, so the render-smoke test is load-bearing). `M3_CONTENT_PROMPT_BY_FAMILY` is NOT mutated (clustering key stays generic — required by the existing `test_issue242`/`test_issue243` guards). The `profile=="ml_ds"` half is the enclosing branch; business+clustering never reaches the map (uses `M3_AUDIT_PROMPT`). Accepted limitation: a legacy DBSCAN/`mixed_legacy` replay would get the K-Means-framed narrative (replay-only; reduced catalog #451).
- Kill-switch `MLDS_CLUSTERING_M3_CONTENT` (Settings `mlds_clustering_m3_content`, default `true`; `backend/.env.example`) → off returns the generic prompt byte-identically. Byte-identical for clasificación/regresión/serie_temporal/business. Golden oracle `golden_eval.check_clustering_m3_content_no_metric_numbers` (NARROW zero-FP: a silhouette/silueta/Davies-Bouldin keyword adjacent to a ≥2-decimal value; integer k and round `0.5` thresholds are not flagged) → `NodeEvalInputs.clustering_m3_content_no_metric_ok` → `evaluate_downgrade_gate` (RED control: fabricated-metric prose fails; exercised via inline GREEN/RED — g07 is dataset-only and the live runner is unwired). No new canonical/state key, no `case_sanitization`, no migration, no frontend, architect SHA untouched. Tests: `tests/test_issue457_clustering_m3_content.py`.

## M3 Output-Grounded Notebook Questions (Issue #489, EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering` with an EXECUTED notebook. The 3 existing M3 questions are conceptual and (by graph order #336/#457) generated pre/parallel to the executor, so they cannot reference the student's real output. A teacher asked for 2 ADDITIONAL "output-grounded" questions that make the student interpret THEIR real notebook output (silhouette + cluster profiles). The notebook is DETERMINISTIC → the student's run == the server-side `m3_metrics_summary` (#453), so `solucion_esperada` (teacher-only) anchors to REAL values.
- **Dedicated POST-executor node `m3_notebook_questions_generator`** in the `m3_graph` subgraph BETWEEN `m3_notebook_executor` and the `m3_sync` barrier (the `executor→sync` edge is split into `executor→NODE→sync`; the parallel 3-question branch `m3_questions_generator→m3_sync` is UNTOUCHED). It does NOT reuse `m3_questions_generator` (pre-execution, no metrics) and does NOT touch its "EXACTAMENTE 3 … numero (1,2,3)" lock (prompt + the `numero`-identity guard in `_apply_m3_questions_coherence`, NOT `graph.py:1619` — that line is the M4 reprompt header, a false anchor in the issue) nor the #415 guard.
- **Anti-fan-in (load-bearing):** the node writes a SEPARATE state key `m3_notebook_questions` (`state.py`, single writer, overwrite reducer) — NEVER `m3_questions` (parallel-branch key; co-writing = `_last_value` race). The MERGE happens at the SINGLE canonical assembly point `frontend_output_adapter.py` (`m3Questions = _strip_question_metadata(m3_questions + m3_notebook_questions)`) → one variable-length array (numero 1..5) the frontend already renders. `m3_notebook_questions` is never allowlisted → no student leak; grading key `M3-Q4`/`M3-Q5` (numero forced 4/5), dynamic denominator → 5.
- **Gate (all AND; else noop `{}` → never fails the job):** kill-switch `m3_notebook_questions` + `_is_ml_ds_clustering` + `output_depth=="visual_plus_notebook"` + `not m3_notebook_degraded` + finite `m3_metrics_summary["silhouette"]` (via `_finite_metric` — more robust than the `modeling_status` string).
- **GLM routing (default-GLM, diverges from #486):** the node DEFAULTS to `z-ai/glm-5.2` (`NODE_M3_NOTEBOOK_QUESTIONS`, `resolve_node_model(..., "z-ai/glm-5.2")`) — runs on GLM out-of-the-box (the #486 nodes default Gemini + override). REUSES `_get_writer_llm` (the #486-proven GLM structured-output path; no new factory); Gemini stays as the LOAD-BEARING resilience fallback (`_build_llm` degrades to Gemini on missing key / build error / no credits → the node never lacks a client). Consequence (merge ≠ deploy): default-GLM ACTIVATES GLM at deploy → `OPENROUTER_API_KEY` + credits + the LIVE gate are DEPLOY prerequisites; CI without the key degrades to Gemini cleanly. Degrade mirrors `m3_questions_generator` (`except RuntimeError: raise; except Exception: return {…: []}`, distinct 402 log), NOT M5's transient-reraise.
- **Generation + grounding:** dedicated prompt `M3_NOTEBOOK_QUESTIONS_PROMPT_CLUSTERING` (`prompts/clustering/M3_clustering/notebook_questions.py`, re-exported through the clustering→prompts chain) with `GeneradorPreguntasOutput` structured output. The node INJECTS the real metrics (silhouette/n_clusters/cluster_sizes/target_k/schema feature names) so the LLM reproduces the true silhouette. **B4-b decision:** per-cluster means are NOT in `m3_metrics_summary` (the `cluster_profiles` cell prints but does not export them), so Q5 is QUALITATIVE (anchors n_clusters/cluster_sizes/target_k; the student reads the per-feature domination from their own table). The GUARANTEE is `_apply_m3_notebook_questions_grounding`, which reuses #469 `detect_unanchored_silhouette` over `enunciado`+`solucion_esperada` → a fabricated silhouette triggers reprompt-once-then-OMIT (never a false value, never a job failure).
- **Resume:** mirrors `m3_questions` EXACTLY — a `_RESUME_NODE_REQUIRED_OUTPUTS` entry + a `_GRAPH_AGENT_TO_CANONICAL_STEP` entry; NO `save_artifact`/`hydration_map` (m3_questions itself uses neither — those are storage-offload for narrative/EDA). No clobber by construction; re-runs from durable `m3_metrics_summary` when absent; overwrite reducer → no duplication.
- Kill-switch `M3_NOTEBOOK_QUESTIONS` (Settings `m3_notebook_questions`, default `true`; `backend/.env.example`) → off makes the node noop → byte-identical to the 3 questions. Golden oracle `golden_eval.check_m3_notebook_questions_grounded` (reuses `detect_unanchored_silhouette`) → `NodeEvalInputs.m3_notebook_questions_grounded_ok` → `evaluate_downgrade_gate` (n/a on the current golden set like #457/#469; deterministic teeth in the unit RED/GREEN). No new student-facing canonical/state key, no `case_sanitization`, no migration, no frontend, architect SHA untouched. Tests: `tests/test_issue489_m3_notebook_questions.py`. LIVE verification pending (merge ≠ deploy).

## ml_ds Clustering K-Means Notebook (Issue #454, EPIC #458 C3)

- Scope: `studentProfile == "ml_ds"` + family `clustering` with K-Means as the ONLY algorithm. The M3 clustering prompt used to MIX K-Means + DBSCAN; #454 adds a per-variant dispatch (mirror of the classification variant dispatch) so a K-Means case gets a K-Means-ONLY prompt. DBSCAN is hidden from the form (`ALGORITHM_CATALOG_REDUCED`) → K-Means is the only live pick; the mixed prompt is kept as the `mixed_legacy` replay fallback. `clasificacion`/`regresion`/`serie_temporal`/`business` are byte-identical (`prompts/__init__.py` is purely additive; the `graph.py` selection-site restructure preserves classification behaviour).
- `_resolve_clustering_notebook_variant(algoritmos)` (mirror of `_resolve_classification_notebook_variant`, but keys off the algorithm list only — no `algorithm_mode`): `["K-Means"]` (no DBSCAN) → `kmeans_only`; DBSCAN/both/unresolved → `mixed_legacy`. `ClusteringNotebookVariant = Literal["kmeans_only","mixed_legacy"]` (+ `Final` constants); `CLUSTERING_NOTEBOOK_PROMPT_BY_VARIANT["mixed_legacy"]` IS `M3_NOTEBOOK_ALGO_PROMPT_CLUSTERING` (same object → byte-identical). The selection site adds `elif family == "clustering" and settings.mlds_clustering_kmeans_notebook:`. `notebook_variant` is widened to `str | None`; `toc_cell` is computed inside the clasificacion branch (clustering has no TOC) to keep `TOC_MARKDOWN_CELL_BY_VARIANT.get` type-safe.
- `M3_NOTEBOOK_ALGO_PROMPT_CLUSTERING_KMEANS` emits 6 fully-written cells with sentinels: `scaler_features` (deterministic numeric-feature hygiene + StandardScaler + **imports `silhouette_score, davies_bouldin_score`** — the verbatim metrics cell uses both without importing them), `elbow` (inertia + silhouette-vs-k in ONE twin-axis figure; `best_k = argmax silhouette`), `kmeans_fit` (self-contained; re-derives `best_k`; `KMeans(n_init=10, random_state=42)`), `metrics_summary_json` (**verbatim** from the mixed prompt — #453 parser contract), `cluster_profiles` (table), `pca_scatter`. 2 figures, no DBSCAN/eps/k-distance/min_samples/NearestNeighbors in executable code, no `dir()/locals()/globals()` (executor denylist). Same 7 `.format()` placeholders, all literal braces `{{}}`-escaped.
- Lockstep validator dicts in `graph.py` are `dict[str, tuple[str, ...]]` (so the str-narrowed `.get(notebook_variant)` type-checks): sentinels = the 6 markers; APIs = `KMeans(`/`StandardScaler`/`silhouette_score` (PCA/davies_bouldin NOT required — execution-tolerant); prohibited = `DBSCAN(`/`NearestNeighbors(`. `mixed_legacy` has NO per-variant entries → family-level fallback → byte-identical replay. `_detect_unsafe_constructs` now also runs for `clustering + kmeans_only` (the executor scrubs clustering since #453; routes a stray `locals()` through a generation reprompt instead of a silent execution-time degrade; FP-safe). The `mixed_legacy` reprompt omits the variant line → byte-identical to pre-#454.
- Kill-switch `MLDS_CLUSTERING_KMEANS_NOTEBOOK` (Settings `mlds_clustering_kmeans_notebook`, default true; `backend/.env.example`) → off reverts to the mixed prompt + family-level validation byte-identically. Golden oracle `golden_eval.check_kmeans_notebook_shape` DELEGATES to `_validate_notebook_family_consistency('clustering', code, 'kmeans_only')` + a stripped `DBSCAN(` absence check → `NodeEvalInputs.kmeans_notebook_shape_ok` → `evaluate_downgrade_gate`. No new canonical key, no `case_sanitization`/migration/frontend change, architect untouched. Tests: `tests/test_issue454_kmeans_notebook.py`.

## ml_ds Clustering M4 Value Frame + Silhouette Grounding (Issue #469, EPIC #458)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Split of #467. For an EXPLORATORY segmentation M4 used to lead with a fabricated payback/break-even chart ("Mes N", no cash-flow model) and invent uplifts ("+8%/2%/5%"), and the silhouette grounding (`m4_grounding`) is clf-gated. Audit: the "Arquitecto Financiero" role does NOT survive on the lens-ON (NEUTRAL) path (#437); the M4↔M5 verdict was already closed by #467.
- Dedicated prompts (NOT node-level hints), mirroring #457: new `prompts/clustering/M4_clustering/` — `M4_CONTENT_PROMPT_CLUSTERING` = `M4_CONTENT_GENERATOR_PROMPT_NEUTRAL` + brace-free block (redefines §4.1 without AUC, §4.2 without uplift; §4.3-4.5 reused → no drift) and `M4_CHART_PROMPT_CLUSTERING` (standalone full rewrite, Gráfico 1 = value-by-segment, ZERO payback except an explicit ban line). Gated dict-key override via the pure helper `_effective_m4_clustering_prompts` (`lens_on ∧ ml_ds ∧ clustering ∧ switch`; identity object otherwise → byte-identical; chart override only on the NEUTRAL 2-chart path). `m4_questions_generator` gets a brace-free hint (reuses `build_clustering_verdict_hint`, prompt-only, no validator).
- Deterministic silhouette guarantee: `detect_unanchored_silhouette(prose, real)` (`clustering_decision.py`) flags a cited silhouette diverging from the real `m3_metrics_summary["silhouette"]`; zero-FP-leaning (assertion shape + band [0.30,0.95] + 0.45-floor exemption + negation guard), ReDoS-safe. The existing `_apply_clustering_m4_verdict_coherence` is EXTENDED (verdict #467 + silhouette #469 in ONE combined reprompt → avoids the two-guard clobber); reprompt-once-then-degrade, best-effort, logger-only. Byte-identical for the 5 #467 tests (no `m3_metrics_summary` → silhouette check inert).
- Kill-switch `MLDS_CLUSTERING_M4_VALUE_FRAME` (Settings `mlds_clustering_m4_value_frame`, default true; `backend/.env.example`), independent of `MLDS_CLUSTERING_DECISION_COHERENCE`. Off → byte-identical to post-#467. Golden oracles `check_m4_clustering_no_payback_chart` (reuses `m4_grounding.is_payback_chart`) / `check_m4_clustering_silhouette_grounded` → `NodeEvalInputs.m4_clustering_no_payback_ok` / `m4_clustering_silhouette_ok` → `evaluate_downgrade_gate`. No new canonical/state key, no `case_sanitization`/migration/frontend change, architect/SHA untouched; `m4_charts` stays 2 charts. LIVE acceptance gate required before the demo. Tests: `tests/test_issue469_m4_clustering_value_frame.py`.
## ml_ds Clustering De-supervise (Issue #466)

- Scope: `studentProfile == "ml_ds"` + family `clustering`. Kills the supervised-framing leak in three fronts; `clasificacion`/`regresion`/`serie_temporal`/`business` (including business+clustering) stay byte-identical. Audit note: `dummy_target` is LLM-emitted (`git grep` empty); the notebook does NOT hardcode `target_to_ignore`; removing the column alone does NOT kill the chart (`_identify_target_variable` falls back to "last numeric column" + `EDA_CHART_GENERATOR_PROMPT` hardcodes "variable objetivo" for ml_ds).
- **Frente 1 (data):** `_enforce_mlds_clustering_no_target` (sibling of `_enforce_mlds_classification_schema` #382, do NOT generalize) — pure copy-on-write, strict gate `enabled AND profile=="ml_ds" AND primary_family=="clustering"`, runs after `_enforce_mlds_classification_schema` at both `schema_designer` call sites (schema layer → the column never reaches the CSV). Detects a leaked target by (A) ANY `contract.target_column` (FP-proof, any role/shape — clustering has no target, incl. `clustering_target`), (B) a classification-target-named binary/unit-range column, or (C) a cluster-label artifact name `cluster*`/`kmeans*` (any shape — a k-valued `cluster_id` is the worst answer leak and is not binary), with `protected = period ∪ contract.feature_columns ∪ #452 segmentation columns`. Architect untouched (`_MLDS_ARCHITECT_PROMPT_SHA256` intact). Hardened via adversarial self-review.
- **Frente 2 (charts):** `datagen/eda_charts_clustering.py::generate_clustering_eda_charts` (mirror of the business builder; profile-agnostic, reusable by #317) emits 3 DATA-ONLY, PRE-MODEL charts (#317 spec): box distributions (motivate StandardScaler), correlation heatmap, 2D feature-pair scatter — NO cluster labels/centroids/elbow/k/silhouette. `_eda_clustering_python_path` reuses `_annotate_validate_emit` with the dedicated `EDA_ANNOTATE_ONLY_PROMPT_CLUSTERING` (the generic is classification-flavored). Dispatch `elif primary_family=="clustering" and profile=="ml_ds" and settings.mlds_clustering_charts`; runtime None → empty panel (5A, no LLM fallback); kill-switch off → generic LLM-JSON byte-identical.
- **Frente 3 (notebook):** family-gated `.replace()` of the §2.1 prose in `_prepare_m3_notebook_generation_context` drops "categoría a predecir" for clustering; `M3_NOTEBOOK_BASE_TEMPLATE` unchanged (non-clustering byte-identical), TOC untouched; drift-locked.
- Kill-switches `MLDS_CLUSTERING_NO_TARGET` (Frente 1+3) and `MLDS_CLUSTERING_CHARTS` (Frente 2). Golden oracles `check_clustering_no_target` + `check_clustering_eda_no_target_charts` → `evaluate_downgrade_gate`. No new canonical key/state, no `case_sanitization`/migration/frontend. #317 (business+clustering) deferred. Tests: `tests/test_issue466_clustering_no_target.py`. LIVE verification pending (merge ≠ deploy).

## M2 Clustering Box-Plot Readability + Payload Trim (Issue #487)

- Scope: `studentProfile == "ml_ds"` + family `clustering`, inside the #466 data-only builder (`datagen/eda_charts_clustering.py`). P3 polish. The "feature distributions" box used a linear Y axis → `monetary_value` (50–5000) squashed the other features (0–1/0–100) into flat lines at ~0, and emitted `y:[~1000 raw floats]` per feature under `boxpoints:False` (pure overhead). ONE refactor closes both (the trim was the deferred `task_c4e404f4`).
- **Refactor (`_box_stats`):** box plots emit PRE-COMPUTED server-side stats (`q1/median/q3` via pandas linear quantile = Plotly default `quartilemethod`; whiskers = most-extreme value within 1.5·IQR = the `boxpoints:False` render, outliers already hidden) instead of raw points → payload −97%, raw-box render identical. **`x:[col]` is LOAD-BEARING**: a pre-computed Plotly box with only `name` (no `x`) collapses every box onto x=0 (no axis names) — verified by a visual render-test; one box per trace preserves the per-feature color. The helper is total/defensive (empty→NaN no-throw; constant/IQR=0→flat box).
- **Presentation (#466 panel 3→4 charts):** adds `feature_distributions_scaled` — a **z-score** `(x-mean)/std(ddof=0)` box (= StandardScaler) so all features are comparable/legible (centered at 0); the RAW box is kept (disparity message intact). z-score is a DETERMINISTIC rescale of the real data (no fit/predict/labels) → within the #466 data-only/pre-model invariant. The standardized title avoids the oracle markers (`cluster/centroid/centroide/elbow/codo/silhouette/silueta/k=`). Reuses `_select_clustering_feature_columns` (same features as the raw box).
- **Scatter:** `_MAX_SCATTER_POINTS` 1000→500 (at 1000 rows `.head(1000)` never downsampled); `.head` kept (deterministic).
- **Reuses `MLDS_CLUSTERING_CHARTS`** (no new switch): OFF → builder bypass → LLM-JSON byte-identical. Validation chain traced and safe: `EDAChartSpec.traces: list[dict]` preserves the box keys (no `model_dump` silent-drop); `sanitizeTraces` unchanged (passthrough; string `x`-array not corrupted by `?? 0`); `EDAChartGeneratorOutput` has no count validator (4 ≤ the ≤5 `_annotate_validate_emit` cap). No new canonical key/state, no `case_sanitization`/migration, architect/SHA untouched. Other families/business/kill-switch-off byte-identical. Already-generated cases not reprocessed. Golden oracle `check_clustering_eda_no_target_charts` (count-agnostic) stays GREEN. Tests: `tests/test_issue466_clustering_no_target.py` (+5 for #487) and `plotlyChartUtils.test.ts` (+2 box passthrough). LIVE verification pending (merge ≠ deploy).

## Narrative Grounding (Issue #243)

- Narrative grounding applies only when `studentProfile == "ml_ds"` AND `family == "clasificacion"` for M3-content, M4, and M5. The other three families and the `business` profile keep their existing prompt strings and must not receive `{computed_metrics_block}`.
- Issue #239 populates `m3_metrics_summary` from executed classification notebooks for downstream grounding. If metrics are absent or anchorless, `build_computed_metrics_block(None)` still emits a clear fallback placeholder, validation is disabled for that run, and `narrative_grounding_warning` is written to in-memory LangGraph state (not persisted to `task_payload`).
- Issue #336 — By graph order, `m3_metrics_summary` is structurally absent at `m3_content_generator` time, so M3-content grounding is ALWAYS the disabled/fallback path. This is intentional (pre-execution design narrative, not a failure; the trigger is always `reason="missing"`). M3-content is therefore distinguished from M4/M5: it does NOT persist `narrative_grounding_warning` in state and emits a dedicated `logger.info`; M4/M5 keep `NARRATIVE_GROUNDING_WARNING` to signal a REAL post-executor missing/anchorless failure. `narrative_grounding_warning` is in-memory-only during the job (not in `task_payload`, not in canonical output, not teacher-facing); the actionable operator signal lives in the structured `logger.warning` (`graph.py` `_prepare_classification_narrative_grounding`, `extra={node, reason}`). Operators/QA treat the M3-content origin as benign/expected and only the M4/M5 origin as actionable.
- `validate_narrative_grounding` rejects citations with `CITA:` and unanchored model-metric numbers with `UNANCHORED:`. Business numbers from M2, Exhibits, or M4 are allowed. Numeric tolerance is ±2 percentage points for percentage-like values and ±2% relative for scalar values.
- The narrative nodes reprompt once with the `CITA:` / `UNANCHORED:` bullet list. A second violation raises `RuntimeError` so the job fails instead of shipping fabricated metrics.

## M4/M5 Narrative Variant Dispatch (Issue #330)

- The M4 and M5 narrative nodes (`m4_content_generator`, `m5_content_generator` in `backend/src/case_generator/graph.py`) are variant-aware for `studentProfile == "ml_ds"` AND `family == "clasificacion"`, mirroring the M3-content dispatch. They resolve teacher intent with `_resolve_classification_notebook_variant(algorithm_mode, algoritmos)` and override ONLY the `"clasificacion"` key of an `_effective_prompt_by_family` before calling `_select_narrative_prompt`. Every other case (the `business` profile, and the `regresion`/`clustering`/`serie_temporal` families) passes the original `M4_PROMPT_BY_FAMILY`/`M5_PROMPT_BY_FAMILY` unchanged.
- Symbols: `M4_NARRATIVE_PROMPT_CLASSIFICATION_BY_VARIANT` and `M5_NARRATIVE_PROMPT_CLASSIFICATION_BY_VARIANT`, each keyed by `lr_only` / `rf_only` / `lr_rf_contrast`. The `lr_rf_contrast` value IS the canonical `M4/M5_NARRATIVE_PROMPT_CLASSIFICATION` (the back-compat aliases `M4_PROMPT_CLASSIFICATION` / `M5_PROMPT_CLASSIFICATION` still point to it). M4's contrast block contrasts LR vs RF on the financial decision; `lr_only`/`rf_only` never name the unselected model. M5 keeps the decision-matrix header `| acción | KPI esperado | riesgo | modelo soporte |` byte-identical across all three variants and only varies the `modelo soporte` guidance line.
- Invocation contracts are unchanged: M4 still invokes `_invoke_narrative_with_grounding` (reprompt-once-then-fail on grounding) and M5 still invokes `_invoke_m5_content_with_contract(..., require_decision_matrix=_is_ml_ds_classification(...))`. The classification grounding block (`{computed_metrics_block}`) stays active in all three variants, and the three variants of each phase share the SAME placeholder set (base keys plus `{pregunta_eje}` and `{computed_metrics_block}`).
- The `business` swap `_maybe_business_classification_prompt` stays byte-identical; the variant override is exclusive to `ml_ds` + `clasificacion`. Do not reintroduce LR/RF naming into single-model variants.
- Issue #337 — defense-in-depth: a pure, asymmetric post-generation validator `detect_unselected_model_mentions(prose, variant)` (in `narrative_grounding.py`) flags the UNSELECTED model in `lr_only`/`rf_only` prose with the `MODELO_NO_SELECCIONADO:` prefix (prose-safe word-boundary regex over the full model names + Spanish equivalents; the bare `RF`/`LR` acronyms are intentionally NOT matched to avoid false positives). It feeds the SAME reprompt-once-then-`RuntimeError` loop as grounding (one failure policy, no parallel loop) and runs INDEPENDENT of `grounding_enabled`: it is evaluated BEFORE the grounding early-return inside `_invoke_narrative_with_grounding`, and UNCONDITIONALLY in M5's decision-matrix layer 2 (`leak_violations_2`, since a matrix reprompt can reintroduce a leak in the `modelo soporte` cell). It is the narrative mirror of the notebook `_validate_notebook_family_consistency` guard. `lr_rf_contrast` / `business` / non-classification pass `variant=None` → no-op (`[]`) → byte-identical (zero extra reprompt). M3-content is a documented fast-follow (its grounding is structurally disabled per #336; `variant` is not wired at its call site here).

## Validation Expectations

After changing this area, at minimum run:

```powershell
uv run --directory backend pytest -q
uv run --directory backend mypy src
```

If the change affects output behavior, also review whether prompt-facing docs, tests, or fixtures need to be updated.
